이 문단은 **ngsc** 빌드에서만 노출됩니다. NHN Government Secure Cloud 환경 전용 안내입니다.

| 항목 | 값 |
| --- | --- |
| 빌드 종류 | ngsc |
| 콘솔 URL | https://console.gov-nhncloud.com |

<a id="include-id"></a>
### include-id { #include-id }

이 섹션은 `mkdocs-test-ngsc.md` 에서 include 된 후에도 앵커 id(`include-id`)가 유지되는지 검증하기 위한 테스트 섹션입니다. ngsc 빌드에서 `#include-id` 링크로 접근할 수 있어야 합니다.
