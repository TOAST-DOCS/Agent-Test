<!-- pre-align:aligned sig=e2epreserve -->

<a id="preserve-e2e"></a>
# preserve-existing e2e 픽스처

이 문서는 자동 생성된 e2e 픽스처입니다 (20260904-001358).

<a id="preserve-sec-1"></a>
### 보존 섹션 1 { #preserve-sec-1 }

이 섹션은 preserve-existing e2e 픽스처의 1번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field1Id | String | 1번 필드의 식별자 |
| body.field1Name | String | 1번 필드의 이름 |
| body.field1Count | Integer | 1번 필드의 개수 |

1번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-2"></a>
### 보존 섹션 2 { #preserve-sec-2 }

이 섹션은 preserve-existing e2e 픽스처의 2번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field2Id | String | 2번 필드의 식별자 |
| body.field2Name | String | 2번 필드의 이름 |
| body.field2Count | Integer | 2번 필드의 개수 |

2번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-3"></a>
### 보존 섹션 3 { #preserve-sec-3 }

이 섹션은 preserve-existing e2e 픽스처의 3번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field3Id | String | 3번 필드의 식별자 |
| body.field3Name | String | 3번 필드의 이름 |
| body.field3Count | Integer | 3번 필드의 개수 |

3번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-4"></a>
### 보존 섹션 4 { #preserve-sec-4 }

이 섹션은 preserve-existing e2e 픽스처의 4번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field4Id | String | 4번 필드의 식별자 |
| body.field4Name | String | 4번 필드의 이름 |
| body.field4Count | Integer | 4번 필드의 개수 |

4번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-5"></a>
### 보존 섹션 5 { #preserve-sec-5 }

이 섹션은 preserve-existing e2e 픽스처의 5번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field5Id | String | 5번 필드의 식별자 |
| body.field5Name | String | 5번 필드의 이름 |
| body.field5Count | Integer | 5번 필드의 개수 |

```json
{
  "field5Id": "id-5",
  "field5Name": "name-5"
}
```

5번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-6"></a>
### 보존 섹션 6 { #preserve-sec-6 }

이 섹션은 preserve-existing e2e 픽스처의 6번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field6Id | String | 6번 필드의 식별자 |
| body.field6Name | String | 6번 필드의 이름 |
| body.field6Count | Integer | 6번 필드의 개수 |

6번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-7"></a>
### 보존 섹션 7 { #preserve-sec-7 }

이 섹션은 preserve-existing e2e 픽스처의 7번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field7Id | String | 7번 필드의 식별자 |
| body.field7Name | String | 7번 필드의 이름 |
| body.field7Count | Integer | 7번 필드의 개수 |

7번 섹션의 마지막 설명 문장을 20260904-001358 에 고쳤습니다. 이 섹션만 재번역되어야 하고
나머지 섹션은 기존 번역이 그대로 남아야 합니다.

<a id="preserve-sec-8"></a>
### 보존 섹션 8 { #preserve-sec-8 }

이 섹션은 preserve-existing e2e 픽스처의 8번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field8Id | String | 8번 필드의 식별자 |
| body.field8Name | String | 8번 필드의 이름 |
| body.field8Count | Integer | 8번 필드의 개수 |

8번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-9"></a>
### 보존 섹션 9 { #preserve-sec-9 }

이 섹션은 preserve-existing e2e 픽스처의 9번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field9Id | String | 9번 필드의 식별자 |
| body.field9Name | String | 9번 필드의 이름 |
| body.field9Count | Integer | 9번 필드의 개수 |

9번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-10"></a>
### 보존 섹션 10 { #preserve-sec-10 }

이 섹션은 preserve-existing e2e 픽스처의 10번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field10Id | String | 10번 필드의 식별자 |
| body.field10Name | String | 10번 필드의 이름 |
| body.field10Count | Integer | 10번 필드의 개수 |

```json
{
  "field10Id": "id-10",
  "field10Name": "name-10"
}
```

10번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-11"></a>
### 보존 섹션 11 { #preserve-sec-11 }

이 섹션은 preserve-existing e2e 픽스처의 11번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field11Id | String | 11번 필드의 식별자 |
| body.field11Name | String | 11번 필드의 이름 |
| body.field11Count | Integer | 11번 필드의 개수 |

11번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-12"></a>
### 보존 섹션 12 { #preserve-sec-12 }

이 섹션은 preserve-existing e2e 픽스처의 12번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field12Id | String | 12번 필드의 식별자 |
| body.field12Name | String | 12번 필드의 이름 |
| body.field12Count | Integer | 12번 필드의 개수 |

12번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-13"></a>
### 보존 섹션 13 { #preserve-sec-13 }

이 섹션은 preserve-existing e2e 픽스처의 13번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field13Id | String | 13번 필드의 식별자 |
| body.field13Name | String | 13번 필드의 이름 |
| body.field13Count | Integer | 13번 필드의 개수 |

13번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-14"></a>
### 보존 섹션 14 { #preserve-sec-14 }

이 섹션은 preserve-existing e2e 픽스처의 14번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field14Id | String | 14번 필드의 식별자 |
| body.field14Name | String | 14번 필드의 이름 |
| body.field14Count | Integer | 14번 필드의 개수 |

14번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-15"></a>
### 보존 섹션 15 { #preserve-sec-15 }

이 섹션은 preserve-existing e2e 픽스처의 15번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field15Id | String | 15번 필드의 식별자 |
| body.field15Name | String | 15번 필드의 이름 |
| body.field15Count | Integer | 15번 필드의 개수 |

```json
{
  "field15Id": "id-15",
  "field15Name": "name-15"
}
```

15번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-16"></a>
### 보존 섹션 16 { #preserve-sec-16 }

이 섹션은 preserve-existing e2e 픽스처의 16번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field16Id | String | 16번 필드의 식별자 |
| body.field16Name | String | 16번 필드의 이름 |
| body.field16Count | Integer | 16번 필드의 개수 |

16번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-17"></a>
### 보존 섹션 17 { #preserve-sec-17 }

이 섹션은 preserve-existing e2e 픽스처의 17번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field17Id | String | 17번 필드의 식별자 |
| body.field17Name | String | 17번 필드의 이름 |
| body.field17Count | Integer | 17번 필드의 개수 |

17번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-18"></a>
### 보존 섹션 18 { #preserve-sec-18 }

이 섹션은 preserve-existing e2e 픽스처의 18번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field18Id | String | 18번 필드의 식별자 |
| body.field18Name | String | 18번 필드의 이름 |
| body.field18Count | Integer | 18번 필드의 개수 |

18번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-19"></a>
### 보존 섹션 19 { #preserve-sec-19 }

이 섹션은 preserve-existing e2e 픽스처의 19번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field19Id | String | 19번 필드의 식별자 |
| body.field19Name | String | 19번 필드의 이름 |
| body.field19Count | Integer | 19번 필드의 개수 |

19번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-20"></a>
### 보존 섹션 20 { #preserve-sec-20 }

이 섹션은 preserve-existing e2e 픽스처의 20번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field20Id | String | 20번 필드의 식별자 |
| body.field20Name | String | 20번 필드의 이름 |
| body.field20Count | Integer | 20번 필드의 개수 |

```json
{
  "field20Id": "id-20",
  "field20Name": "name-20"
}
```

20번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-21"></a>
### 보존 섹션 21 { #preserve-sec-21 }

이 섹션은 preserve-existing e2e 픽스처의 21번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field21Id | String | 21번 필드의 식별자 |
| body.field21Name | String | 21번 필드의 이름 |
| body.field21Count | Integer | 21번 필드의 개수 |

21번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-22"></a>
### 보존 섹션 22 { #preserve-sec-22 }

이 섹션은 preserve-existing e2e 픽스처의 22번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field22Id | String | 22번 필드의 식별자 |
| body.field22Name | String | 22번 필드의 이름 |
| body.field22Count | Integer | 22번 필드의 개수 |

22번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-23"></a>
### 보존 섹션 23 { #preserve-sec-23 }

이 섹션은 preserve-existing e2e 픽스처의 23번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field23Id | String | 23번 필드의 식별자 |
| body.field23Name | String | 23번 필드의 이름 |
| body.field23Count | Integer | 23번 필드의 개수 |

23번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-24"></a>
### 보존 섹션 24 { #preserve-sec-24 }

이 섹션은 preserve-existing e2e 픽스처의 24번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field24Id | String | 24번 필드의 식별자 |
| body.field24Name | String | 24번 필드의 이름 |
| body.field24Count | Integer | 24번 필드의 개수 |

24번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-25"></a>
### 보존 섹션 25 { #preserve-sec-25 }

이 섹션은 preserve-existing e2e 픽스처의 25번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field25Id | String | 25번 필드의 식별자 |
| body.field25Name | String | 25번 필드의 이름 |
| body.field25Count | Integer | 25번 필드의 개수 |

```json
{
  "field25Id": "id-25",
  "field25Name": "name-25"
}
```

25번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-26"></a>
### 보존 섹션 26 { #preserve-sec-26 }

이 섹션은 preserve-existing e2e 픽스처의 26번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field26Id | String | 26번 필드의 식별자 |
| body.field26Name | String | 26번 필드의 이름 |
| body.field26Count | Integer | 26번 필드의 개수 |

26번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-27"></a>
### 보존 섹션 27 { #preserve-sec-27 }

이 섹션은 preserve-existing e2e 픽스처의 27번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field27Id | String | 27번 필드의 식별자 |
| body.field27Name | String | 27번 필드의 이름 |
| body.field27Count | Integer | 27번 필드의 개수 |

27번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-28"></a>
### 보존 섹션 28 { #preserve-sec-28 }

이 섹션은 preserve-existing e2e 픽스처의 28번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field28Id | String | 28번 필드의 식별자 |
| body.field28Name | String | 28번 필드의 이름 |
| body.field28Count | Integer | 28번 필드의 개수 |

28번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-29"></a>
### 보존 섹션 29 { #preserve-sec-29 }

이 섹션은 preserve-existing e2e 픽스처의 29번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field29Id | String | 29번 필드의 식별자 |
| body.field29Name | String | 29번 필드의 이름 |
| body.field29Count | Integer | 29번 필드의 개수 |

29번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-30"></a>
### 보존 섹션 30 { #preserve-sec-30 }

이 섹션은 preserve-existing e2e 픽스처의 30번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field30Id | String | 30번 필드의 식별자 |
| body.field30Name | String | 30번 필드의 이름 |
| body.field30Count | Integer | 30번 필드의 개수 |

```json
{
  "field30Id": "id-30",
  "field30Name": "name-30"
}
```

30번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-31"></a>
### 보존 섹션 31 { #preserve-sec-31 }

이 섹션은 preserve-existing e2e 픽스처의 31번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field31Id | String | 31번 필드의 식별자 |
| body.field31Name | String | 31번 필드의 이름 |
| body.field31Count | Integer | 31번 필드의 개수 |

31번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-32"></a>
### 보존 섹션 32 { #preserve-sec-32 }

이 섹션은 preserve-existing e2e 픽스처의 32번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field32Id | String | 32번 필드의 식별자 |
| body.field32Name | String | 32번 필드의 이름 |
| body.field32Count | Integer | 32번 필드의 개수 |

32번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-33"></a>
### 보존 섹션 33 { #preserve-sec-33 }

이 섹션은 preserve-existing e2e 픽스처의 33번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field33Id | String | 33번 필드의 식별자 |
| body.field33Name | String | 33번 필드의 이름 |
| body.field33Count | Integer | 33번 필드의 개수 |

33번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-34"></a>
### 보존 섹션 34 { #preserve-sec-34 }

이 섹션은 preserve-existing e2e 픽스처의 34번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field34Id | String | 34번 필드의 식별자 |
| body.field34Name | String | 34번 필드의 이름 |
| body.field34Count | Integer | 34번 필드의 개수 |

34번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-35"></a>
### 보존 섹션 35 { #preserve-sec-35 }

이 섹션은 preserve-existing e2e 픽스처의 35번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field35Id | String | 35번 필드의 식별자 |
| body.field35Name | String | 35번 필드의 이름 |
| body.field35Count | Integer | 35번 필드의 개수 |

```json
{
  "field35Id": "id-35",
  "field35Name": "name-35"
}
```

35번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-36"></a>
### 보존 섹션 36 { #preserve-sec-36 }

이 섹션은 preserve-existing e2e 픽스처의 36번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field36Id | String | 36번 필드의 식별자 |
| body.field36Name | String | 36번 필드의 이름 |
| body.field36Count | Integer | 36번 필드의 개수 |

36번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-37"></a>
### 보존 섹션 37 { #preserve-sec-37 }

이 섹션은 preserve-existing e2e 픽스처의 37번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field37Id | String | 37번 필드의 식별자 |
| body.field37Name | String | 37번 필드의 이름 |
| body.field37Count | Integer | 37번 필드의 개수 |

37번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-38"></a>
### 보존 섹션 38 { #preserve-sec-38 }

이 섹션은 preserve-existing e2e 픽스처의 38번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field38Id | String | 38번 필드의 식별자 |
| body.field38Name | String | 38번 필드의 이름 |
| body.field38Count | Integer | 38번 필드의 개수 |

38번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-39"></a>
### 보존 섹션 39 { #preserve-sec-39 }

이 섹션은 preserve-existing e2e 픽스처의 39번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field39Id | String | 39번 필드의 식별자 |
| body.field39Name | String | 39번 필드의 이름 |
| body.field39Count | Integer | 39번 필드의 개수 |

39번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-40"></a>
### 보존 섹션 40 { #preserve-sec-40 }

이 섹션은 preserve-existing e2e 픽스처의 40번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field40Id | String | 40번 필드의 식별자 |
| body.field40Name | String | 40번 필드의 이름 |
| body.field40Count | Integer | 40번 필드의 개수 |

```json
{
  "field40Id": "id-40",
  "field40Name": "name-40"
}
```

40번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-41"></a>
### 보존 섹션 41 { #preserve-sec-41 }

이 섹션은 preserve-existing e2e 픽스처의 41번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field41Id | String | 41번 필드의 식별자 |
| body.field41Name | String | 41번 필드의 이름 |
| body.field41Count | Integer | 41번 필드의 개수 |

41번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-42"></a>
### 보존 섹션 42 { #preserve-sec-42 }

이 섹션은 preserve-existing e2e 픽스처의 42번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field42Id | String | 42번 필드의 식별자 |
| body.field42Name | String | 42번 필드의 이름 |
| body.field42Count | Integer | 42번 필드의 개수 |

42번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-43"></a>
### 보존 섹션 43 { #preserve-sec-43 }

이 섹션은 preserve-existing e2e 픽스처의 43번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field43Id | String | 43번 필드의 식별자 |
| body.field43Name | String | 43번 필드의 이름 |
| body.field43Count | Integer | 43번 필드의 개수 |

43번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-44"></a>
### 보존 섹션 44 { #preserve-sec-44 }

이 섹션은 preserve-existing e2e 픽스처의 44번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field44Id | String | 44번 필드의 식별자 |
| body.field44Name | String | 44번 필드의 이름 |
| body.field44Count | Integer | 44번 필드의 개수 |

44번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-45"></a>
### 보존 섹션 45 { #preserve-sec-45 }

이 섹션은 preserve-existing e2e 픽스처의 45번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field45Id | String | 45번 필드의 식별자 |
| body.field45Name | String | 45번 필드의 이름 |
| body.field45Count | Integer | 45번 필드의 개수 |

```json
{
  "field45Id": "id-45",
  "field45Name": "name-45"
}
```

45번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-46"></a>
### 보존 섹션 46 { #preserve-sec-46 }

이 섹션은 preserve-existing e2e 픽스처의 46번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field46Id | String | 46번 필드의 식별자 |
| body.field46Name | String | 46번 필드의 이름 |
| body.field46Count | Integer | 46번 필드의 개수 |

46번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-47"></a>
### 보존 섹션 47 { #preserve-sec-47 }

이 섹션은 preserve-existing e2e 픽스처의 47번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field47Id | String | 47번 필드의 식별자 |
| body.field47Name | String | 47번 필드의 이름 |
| body.field47Count | Integer | 47번 필드의 개수 |

47번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-48"></a>
### 보존 섹션 48 { #preserve-sec-48 }

이 섹션은 preserve-existing e2e 픽스처의 48번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field48Id | String | 48번 필드의 식별자 |
| body.field48Name | String | 48번 필드의 이름 |
| body.field48Count | Integer | 48번 필드의 개수 |

48번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-49"></a>
### 보존 섹션 49 { #preserve-sec-49 }

이 섹션은 preserve-existing e2e 픽스처의 49번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field49Id | String | 49번 필드의 식별자 |
| body.field49Name | String | 49번 필드의 이름 |
| body.field49Count | Integer | 49번 필드의 개수 |

49번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-50"></a>
### 보존 섹션 50 { #preserve-sec-50 }

이 섹션은 preserve-existing e2e 픽스처의 50번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field50Id | String | 50번 필드의 식별자 |
| body.field50Name | String | 50번 필드의 이름 |
| body.field50Count | Integer | 50번 필드의 개수 |

```json
{
  "field50Id": "id-50",
  "field50Name": "name-50"
}
```

50번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-51"></a>
### 보존 섹션 51 { #preserve-sec-51 }

이 섹션은 preserve-existing e2e 픽스처의 51번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field51Id | String | 51번 필드의 식별자 |
| body.field51Name | String | 51번 필드의 이름 |
| body.field51Count | Integer | 51번 필드의 개수 |

51번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-52"></a>
### 보존 섹션 52 { #preserve-sec-52 }

이 섹션은 preserve-existing e2e 픽스처의 52번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field52Id | String | 52번 필드의 식별자 |
| body.field52Name | String | 52번 필드의 이름 |
| body.field52Count | Integer | 52번 필드의 개수 |

52번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-53"></a>
### 보존 섹션 53 { #preserve-sec-53 }

이 섹션은 preserve-existing e2e 픽스처의 53번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field53Id | String | 53번 필드의 식별자 |
| body.field53Name | String | 53번 필드의 이름 |
| body.field53Count | Integer | 53번 필드의 개수 |

53번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-54"></a>
### 보존 섹션 54 { #preserve-sec-54 }

이 섹션은 preserve-existing e2e 픽스처의 54번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field54Id | String | 54번 필드의 식별자 |
| body.field54Name | String | 54번 필드의 이름 |
| body.field54Count | Integer | 54번 필드의 개수 |

54번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-55"></a>
### 보존 섹션 55 { #preserve-sec-55 }

이 섹션은 preserve-existing e2e 픽스처의 55번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field55Id | String | 55번 필드의 식별자 |
| body.field55Name | String | 55번 필드의 이름 |
| body.field55Count | Integer | 55번 필드의 개수 |

```json
{
  "field55Id": "id-55",
  "field55Name": "name-55"
}
```

55번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-56"></a>
### 보존 섹션 56 { #preserve-sec-56 }

이 섹션은 preserve-existing e2e 픽스처의 56번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field56Id | String | 56번 필드의 식별자 |
| body.field56Name | String | 56번 필드의 이름 |
| body.field56Count | Integer | 56번 필드의 개수 |

56번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-57"></a>
### 보존 섹션 57 { #preserve-sec-57 }

이 섹션은 preserve-existing e2e 픽스처의 57번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field57Id | String | 57번 필드의 식별자 |
| body.field57Name | String | 57번 필드의 이름 |
| body.field57Count | Integer | 57번 필드의 개수 |

57번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-58"></a>
### 보존 섹션 58 { #preserve-sec-58 }

이 섹션은 preserve-existing e2e 픽스처의 58번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field58Id | String | 58번 필드의 식별자 |
| body.field58Name | String | 58번 필드의 이름 |
| body.field58Count | Integer | 58번 필드의 개수 |

58번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-59"></a>
### 보존 섹션 59 { #preserve-sec-59 }

이 섹션은 preserve-existing e2e 픽스처의 59번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field59Id | String | 59번 필드의 식별자 |
| body.field59Name | String | 59번 필드의 이름 |
| body.field59Count | Integer | 59번 필드의 개수 |

59번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-60"></a>
### 보존 섹션 60 { #preserve-sec-60 }

이 섹션은 preserve-existing e2e 픽스처의 60번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field60Id | String | 60번 필드의 식별자 |
| body.field60Name | String | 60번 필드의 이름 |
| body.field60Count | Integer | 60번 필드의 개수 |

```json
{
  "field60Id": "id-60",
  "field60Name": "name-60"
}
```

60번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-61"></a>
### 보존 섹션 61 { #preserve-sec-61 }

이 섹션은 preserve-existing e2e 픽스처의 61번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field61Id | String | 61번 필드의 식별자 |
| body.field61Name | String | 61번 필드의 이름 |
| body.field61Count | Integer | 61번 필드의 개수 |

61번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-62"></a>
### 보존 섹션 62 { #preserve-sec-62 }

이 섹션은 preserve-existing e2e 픽스처의 62번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field62Id | String | 62번 필드의 식별자 |
| body.field62Name | String | 62번 필드의 이름 |
| body.field62Count | Integer | 62번 필드의 개수 |

62번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-63"></a>
### 보존 섹션 63 { #preserve-sec-63 }

이 섹션은 preserve-existing e2e 픽스처의 63번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field63Id | String | 63번 필드의 식별자 |
| body.field63Name | String | 63번 필드의 이름 |
| body.field63Count | Integer | 63번 필드의 개수 |

63번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-64"></a>
### 보존 섹션 64 { #preserve-sec-64 }

이 섹션은 preserve-existing e2e 픽스처의 64번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field64Id | String | 64번 필드의 식별자 |
| body.field64Name | String | 64번 필드의 이름 |
| body.field64Count | Integer | 64번 필드의 개수 |

64번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-65"></a>
### 보존 섹션 65 { #preserve-sec-65 }

이 섹션은 preserve-existing e2e 픽스처의 65번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field65Id | String | 65번 필드의 식별자 |
| body.field65Name | String | 65번 필드의 이름 |
| body.field65Count | Integer | 65번 필드의 개수 |

```json
{
  "field65Id": "id-65",
  "field65Name": "name-65"
}
```

65번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-66"></a>
### 보존 섹션 66 { #preserve-sec-66 }

이 섹션은 preserve-existing e2e 픽스처의 66번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field66Id | String | 66번 필드의 식별자 |
| body.field66Name | String | 66번 필드의 이름 |
| body.field66Count | Integer | 66번 필드의 개수 |

66번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-67"></a>
### 보존 섹션 67 { #preserve-sec-67 }

이 섹션은 preserve-existing e2e 픽스처의 67번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field67Id | String | 67번 필드의 식별자 |
| body.field67Name | String | 67번 필드의 이름 |
| body.field67Count | Integer | 67번 필드의 개수 |

67번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-68"></a>
### 보존 섹션 68 { #preserve-sec-68 }

이 섹션은 preserve-existing e2e 픽스처의 68번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field68Id | String | 68번 필드의 식별자 |
| body.field68Name | String | 68번 필드의 이름 |
| body.field68Count | Integer | 68번 필드의 개수 |

68번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-69"></a>
### 보존 섹션 69 { #preserve-sec-69 }

이 섹션은 preserve-existing e2e 픽스처의 69번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field69Id | String | 69번 필드의 식별자 |
| body.field69Name | String | 69번 필드의 이름 |
| body.field69Count | Integer | 69번 필드의 개수 |

69번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-70"></a>
### 보존 섹션 70 { #preserve-sec-70 }

이 섹션은 preserve-existing e2e 픽스처의 70번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field70Id | String | 70번 필드의 식별자 |
| body.field70Name | String | 70번 필드의 이름 |
| body.field70Count | Integer | 70번 필드의 개수 |

```json
{
  "field70Id": "id-70",
  "field70Name": "name-70"
}
```

70번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-71"></a>
### 보존 섹션 71 { #preserve-sec-71 }

이 섹션은 preserve-existing e2e 픽스처의 71번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field71Id | String | 71번 필드의 식별자 |
| body.field71Name | String | 71번 필드의 이름 |
| body.field71Count | Integer | 71번 필드의 개수 |

71번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-72"></a>
### 보존 섹션 72 { #preserve-sec-72 }

이 섹션은 preserve-existing e2e 픽스처의 72번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field72Id | String | 72번 필드의 식별자 |
| body.field72Name | String | 72번 필드의 이름 |
| body.field72Count | Integer | 72번 필드의 개수 |

72번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-73"></a>
### 보존 섹션 73 { #preserve-sec-73 }

이 섹션은 preserve-existing e2e 픽스처의 73번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field73Id | String | 73번 필드의 식별자 |
| body.field73Name | String | 73번 필드의 이름 |
| body.field73Count | Integer | 73번 필드의 개수 |

73번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-74"></a>
### 보존 섹션 74 { #preserve-sec-74 }

이 섹션은 preserve-existing e2e 픽스처의 74번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field74Id | String | 74번 필드의 식별자 |
| body.field74Name | String | 74번 필드의 이름 |
| body.field74Count | Integer | 74번 필드의 개수 |

74번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-75"></a>
### 보존 섹션 75 { #preserve-sec-75 }

이 섹션은 preserve-existing e2e 픽스처의 75번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field75Id | String | 75번 필드의 식별자 |
| body.field75Name | String | 75번 필드의 이름 |
| body.field75Count | Integer | 75번 필드의 개수 |

```json
{
  "field75Id": "id-75",
  "field75Name": "name-75"
}
```

75번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-76"></a>
### 보존 섹션 76 { #preserve-sec-76 }

이 섹션은 preserve-existing e2e 픽스처의 76번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field76Id | String | 76번 필드의 식별자 |
| body.field76Name | String | 76번 필드의 이름 |
| body.field76Count | Integer | 76번 필드의 개수 |

76번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-77"></a>
### 보존 섹션 77 { #preserve-sec-77 }

이 섹션은 preserve-existing e2e 픽스처의 77번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field77Id | String | 77번 필드의 식별자 |
| body.field77Name | String | 77번 필드의 이름 |
| body.field77Count | Integer | 77번 필드의 개수 |

77번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-78"></a>
### 보존 섹션 78 { #preserve-sec-78 }

이 섹션은 preserve-existing e2e 픽스처의 78번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field78Id | String | 78번 필드의 식별자 |
| body.field78Name | String | 78번 필드의 이름 |
| body.field78Count | Integer | 78번 필드의 개수 |

78번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-79"></a>
### 보존 섹션 79 { #preserve-sec-79 }

이 섹션은 preserve-existing e2e 픽스처의 79번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field79Id | String | 79번 필드의 식별자 |
| body.field79Name | String | 79번 필드의 이름 |
| body.field79Count | Integer | 79번 필드의 개수 |

79번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-80"></a>
### 보존 섹션 80 { #preserve-sec-80 }

이 섹션은 preserve-existing e2e 픽스처의 80번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field80Id | String | 80번 필드의 식별자 |
| body.field80Name | String | 80번 필드의 이름 |
| body.field80Count | Integer | 80번 필드의 개수 |

```json
{
  "field80Id": "id-80",
  "field80Name": "name-80"
}
```

80번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-81"></a>
### 보존 섹션 81 { #preserve-sec-81 }

이 섹션은 preserve-existing e2e 픽스처의 81번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field81Id | String | 81번 필드의 식별자 |
| body.field81Name | String | 81번 필드의 이름 |
| body.field81Count | Integer | 81번 필드의 개수 |

81번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-82"></a>
### 보존 섹션 82 { #preserve-sec-82 }

이 섹션은 preserve-existing e2e 픽스처의 82번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field82Id | String | 82번 필드의 식별자 |
| body.field82Name | String | 82번 필드의 이름 |
| body.field82Count | Integer | 82번 필드의 개수 |

82번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-83"></a>
### 보존 섹션 83 { #preserve-sec-83 }

이 섹션은 preserve-existing e2e 픽스처의 83번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field83Id | String | 83번 필드의 식별자 |
| body.field83Name | String | 83번 필드의 이름 |
| body.field83Count | Integer | 83번 필드의 개수 |

83번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-84"></a>
### 보존 섹션 84 { #preserve-sec-84 }

이 섹션은 preserve-existing e2e 픽스처의 84번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field84Id | String | 84번 필드의 식별자 |
| body.field84Name | String | 84번 필드의 이름 |
| body.field84Count | Integer | 84번 필드의 개수 |

84번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-85"></a>
### 보존 섹션 85 { #preserve-sec-85 }

이 섹션은 preserve-existing e2e 픽스처의 85번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field85Id | String | 85번 필드의 식별자 |
| body.field85Name | String | 85번 필드의 이름 |
| body.field85Count | Integer | 85번 필드의 개수 |

```json
{
  "field85Id": "id-85",
  "field85Name": "name-85"
}
```

85번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-86"></a>
### 보존 섹션 86 { #preserve-sec-86 }

이 섹션은 preserve-existing e2e 픽스처의 86번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field86Id | String | 86번 필드의 식별자 |
| body.field86Name | String | 86번 필드의 이름 |
| body.field86Count | Integer | 86번 필드의 개수 |

86번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-87"></a>
### 보존 섹션 87 { #preserve-sec-87 }

이 섹션은 preserve-existing e2e 픽스처의 87번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field87Id | String | 87번 필드의 식별자 |
| body.field87Name | String | 87번 필드의 이름 |
| body.field87Count | Integer | 87번 필드의 개수 |

87번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-88"></a>
### 보존 섹션 88 { #preserve-sec-88 }

이 섹션은 preserve-existing e2e 픽스처의 88번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field88Id | String | 88번 필드의 식별자 |
| body.field88Name | String | 88번 필드의 이름 |
| body.field88Count | Integer | 88번 필드의 개수 |

88번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-89"></a>
### 보존 섹션 89 { #preserve-sec-89 }

이 섹션은 preserve-existing e2e 픽스처의 89번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field89Id | String | 89번 필드의 식별자 |
| body.field89Name | String | 89번 필드의 이름 |
| body.field89Count | Integer | 89번 필드의 개수 |

89번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-90"></a>
### 보존 섹션 90 { #preserve-sec-90 }

이 섹션은 preserve-existing e2e 픽스처의 90번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field90Id | String | 90번 필드의 식별자 |
| body.field90Name | String | 90번 필드의 이름 |
| body.field90Count | Integer | 90번 필드의 개수 |

```json
{
  "field90Id": "id-90",
  "field90Name": "name-90"
}
```

90번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-91"></a>
### 보존 섹션 91 { #preserve-sec-91 }

이 섹션은 preserve-existing e2e 픽스처의 91번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field91Id | String | 91번 필드의 식별자 |
| body.field91Name | String | 91번 필드의 이름 |
| body.field91Count | Integer | 91번 필드의 개수 |

91번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-92"></a>
### 보존 섹션 92 { #preserve-sec-92 }

이 섹션은 preserve-existing e2e 픽스처의 92번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field92Id | String | 92번 필드의 식별자 |
| body.field92Name | String | 92번 필드의 이름 |
| body.field92Count | Integer | 92번 필드의 개수 |

92번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-93"></a>
### 보존 섹션 93 { #preserve-sec-93 }

이 섹션은 preserve-existing e2e 픽스처의 93번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field93Id | String | 93번 필드의 식별자 |
| body.field93Name | String | 93번 필드의 이름 |
| body.field93Count | Integer | 93번 필드의 개수 |

93번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-94"></a>
### 보존 섹션 94 { #preserve-sec-94 }

이 섹션은 preserve-existing e2e 픽스처의 94번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field94Id | String | 94번 필드의 식별자 |
| body.field94Name | String | 94번 필드의 이름 |
| body.field94Count | Integer | 94번 필드의 개수 |

94번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-95"></a>
### 보존 섹션 95 { #preserve-sec-95 }

이 섹션은 preserve-existing e2e 픽스처의 95번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field95Id | String | 95번 필드의 식별자 |
| body.field95Name | String | 95번 필드의 이름 |
| body.field95Count | Integer | 95번 필드의 개수 |

```json
{
  "field95Id": "id-95",
  "field95Name": "name-95"
}
```

95번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-96"></a>
### 보존 섹션 96 { #preserve-sec-96 }

이 섹션은 preserve-existing e2e 픽스처의 96번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field96Id | String | 96번 필드의 식별자 |
| body.field96Name | String | 96번 필드의 이름 |
| body.field96Count | Integer | 96번 필드의 개수 |

96번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-97"></a>
### 보존 섹션 97 { #preserve-sec-97 }

이 섹션은 preserve-existing e2e 픽스처의 97번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field97Id | String | 97번 필드의 식별자 |
| body.field97Name | String | 97번 필드의 이름 |
| body.field97Count | Integer | 97번 필드의 개수 |

97번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-98"></a>
### 보존 섹션 98 { #preserve-sec-98 }

이 섹션은 preserve-existing e2e 픽스처의 98번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field98Id | String | 98번 필드의 식별자 |
| body.field98Name | String | 98번 필드의 이름 |
| body.field98Count | Integer | 98번 필드의 개수 |

98번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-99"></a>
### 보존 섹션 99 { #preserve-sec-99 }

이 섹션은 preserve-existing e2e 픽스처의 99번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field99Id | String | 99번 필드의 식별자 |
| body.field99Name | String | 99번 필드의 이름 |
| body.field99Count | Integer | 99번 필드의 개수 |

99번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-100"></a>
### 보존 섹션 100 { #preserve-sec-100 }

이 섹션은 preserve-existing e2e 픽스처의 100번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field100Id | String | 100번 필드의 식별자 |
| body.field100Name | String | 100번 필드의 이름 |
| body.field100Count | Integer | 100번 필드의 개수 |

```json
{
  "field100Id": "id-100",
  "field100Name": "name-100"
}
```

100번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-101"></a>
### 보존 섹션 101 { #preserve-sec-101 }

이 섹션은 preserve-existing e2e 픽스처의 101번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field101Id | String | 101번 필드의 식별자 |
| body.field101Name | String | 101번 필드의 이름 |
| body.field101Count | Integer | 101번 필드의 개수 |

101번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-102"></a>
### 보존 섹션 102 { #preserve-sec-102 }

이 섹션은 preserve-existing e2e 픽스처의 102번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field102Id | String | 102번 필드의 식별자 |
| body.field102Name | String | 102번 필드의 이름 |
| body.field102Count | Integer | 102번 필드의 개수 |

102번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-103"></a>
### 보존 섹션 103 { #preserve-sec-103 }

이 섹션은 preserve-existing e2e 픽스처의 103번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field103Id | String | 103번 필드의 식별자 |
| body.field103Name | String | 103번 필드의 이름 |
| body.field103Count | Integer | 103번 필드의 개수 |

103번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-104"></a>
### 보존 섹션 104 { #preserve-sec-104 }

이 섹션은 preserve-existing e2e 픽스처의 104번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field104Id | String | 104번 필드의 식별자 |
| body.field104Name | String | 104번 필드의 이름 |
| body.field104Count | Integer | 104번 필드의 개수 |

104번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-105"></a>
### 보존 섹션 105 { #preserve-sec-105 }

이 섹션은 preserve-existing e2e 픽스처의 105번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field105Id | String | 105번 필드의 식별자 |
| body.field105Name | String | 105번 필드의 이름 |
| body.field105Count | Integer | 105번 필드의 개수 |

```json
{
  "field105Id": "id-105",
  "field105Name": "name-105"
}
```

105번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-106"></a>
### 보존 섹션 106 { #preserve-sec-106 }

이 섹션은 preserve-existing e2e 픽스처의 106번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field106Id | String | 106번 필드의 식별자 |
| body.field106Name | String | 106번 필드의 이름 |
| body.field106Count | Integer | 106번 필드의 개수 |

106번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-107"></a>
### 보존 섹션 107 { #preserve-sec-107 }

이 섹션은 preserve-existing e2e 픽스처의 107번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field107Id | String | 107번 필드의 식별자 |
| body.field107Name | String | 107번 필드의 이름 |
| body.field107Count | Integer | 107번 필드의 개수 |

107번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-108"></a>
### 보존 섹션 108 { #preserve-sec-108 }

이 섹션은 preserve-existing e2e 픽스처의 108번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field108Id | String | 108번 필드의 식별자 |
| body.field108Name | String | 108번 필드의 이름 |
| body.field108Count | Integer | 108번 필드의 개수 |

108번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-109"></a>
### 보존 섹션 109 { #preserve-sec-109 }

이 섹션은 preserve-existing e2e 픽스처의 109번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field109Id | String | 109번 필드의 식별자 |
| body.field109Name | String | 109번 필드의 이름 |
| body.field109Count | Integer | 109번 필드의 개수 |

109번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-110"></a>
### 보존 섹션 110 { #preserve-sec-110 }

이 섹션은 preserve-existing e2e 픽스처의 110번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field110Id | String | 110번 필드의 식별자 |
| body.field110Name | String | 110번 필드의 이름 |
| body.field110Count | Integer | 110번 필드의 개수 |

```json
{
  "field110Id": "id-110",
  "field110Name": "name-110"
}
```

110번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-111"></a>
### 보존 섹션 111 { #preserve-sec-111 }

이 섹션은 preserve-existing e2e 픽스처의 111번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field111Id | String | 111번 필드의 식별자 |
| body.field111Name | String | 111번 필드의 이름 |
| body.field111Count | Integer | 111번 필드의 개수 |

111번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-112"></a>
### 보존 섹션 112 { #preserve-sec-112 }

이 섹션은 preserve-existing e2e 픽스처의 112번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field112Id | String | 112번 필드의 식별자 |
| body.field112Name | String | 112번 필드의 이름 |
| body.field112Count | Integer | 112번 필드의 개수 |

112번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-113"></a>
### 보존 섹션 113 { #preserve-sec-113 }

이 섹션은 preserve-existing e2e 픽스처의 113번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field113Id | String | 113번 필드의 식별자 |
| body.field113Name | String | 113번 필드의 이름 |
| body.field113Count | Integer | 113번 필드의 개수 |

113번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-114"></a>
### 보존 섹션 114 { #preserve-sec-114 }

이 섹션은 preserve-existing e2e 픽스처의 114번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field114Id | String | 114번 필드의 식별자 |
| body.field114Name | String | 114번 필드의 이름 |
| body.field114Count | Integer | 114번 필드의 개수 |

114번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-115"></a>
### 보존 섹션 115 { #preserve-sec-115 }

이 섹션은 preserve-existing e2e 픽스처의 115번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field115Id | String | 115번 필드의 식별자 |
| body.field115Name | String | 115번 필드의 이름 |
| body.field115Count | Integer | 115번 필드의 개수 |

```json
{
  "field115Id": "id-115",
  "field115Name": "name-115"
}
```

115번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-116"></a>
### 보존 섹션 116 { #preserve-sec-116 }

이 섹션은 preserve-existing e2e 픽스처의 116번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field116Id | String | 116번 필드의 식별자 |
| body.field116Name | String | 116번 필드의 이름 |
| body.field116Count | Integer | 116번 필드의 개수 |

116번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-117"></a>
### 보존 섹션 117 { #preserve-sec-117 }

이 섹션은 preserve-existing e2e 픽스처의 117번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field117Id | String | 117번 필드의 식별자 |
| body.field117Name | String | 117번 필드의 이름 |
| body.field117Count | Integer | 117번 필드의 개수 |

117번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-118"></a>
### 보존 섹션 118 { #preserve-sec-118 }

이 섹션은 preserve-existing e2e 픽스처의 118번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field118Id | String | 118번 필드의 식별자 |
| body.field118Name | String | 118번 필드의 이름 |
| body.field118Count | Integer | 118번 필드의 개수 |

118번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-119"></a>
### 보존 섹션 119 { #preserve-sec-119 }

이 섹션은 preserve-existing e2e 픽스처의 119번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field119Id | String | 119번 필드의 식별자 |
| body.field119Name | String | 119번 필드의 이름 |
| body.field119Count | Integer | 119번 필드의 개수 |

119번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-120"></a>
### 보존 섹션 120 { #preserve-sec-120 }

이 섹션은 preserve-existing e2e 픽스처의 120번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field120Id | String | 120번 필드의 식별자 |
| body.field120Name | String | 120번 필드의 이름 |
| body.field120Count | Integer | 120번 필드의 개수 |

```json
{
  "field120Id": "id-120",
  "field120Name": "name-120"
}
```

120번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-121"></a>
### 보존 섹션 121 { #preserve-sec-121 }

이 섹션은 preserve-existing e2e 픽스처의 121번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field121Id | String | 121번 필드의 식별자 |
| body.field121Name | String | 121번 필드의 이름 |
| body.field121Count | Integer | 121번 필드의 개수 |

121번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-122"></a>
### 보존 섹션 122 { #preserve-sec-122 }

이 섹션은 preserve-existing e2e 픽스처의 122번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field122Id | String | 122번 필드의 식별자 |
| body.field122Name | String | 122번 필드의 이름 |
| body.field122Count | Integer | 122번 필드의 개수 |

122번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-123"></a>
### 보존 섹션 123 { #preserve-sec-123 }

이 섹션은 preserve-existing e2e 픽스처의 123번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field123Id | String | 123번 필드의 식별자 |
| body.field123Name | String | 123번 필드의 이름 |
| body.field123Count | Integer | 123번 필드의 개수 |

123번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-124"></a>
### 보존 섹션 124 { #preserve-sec-124 }

이 섹션은 preserve-existing e2e 픽스처의 124번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field124Id | String | 124번 필드의 식별자 |
| body.field124Name | String | 124번 필드의 이름 |
| body.field124Count | Integer | 124번 필드의 개수 |

124번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-125"></a>
### 보존 섹션 125 { #preserve-sec-125 }

이 섹션은 preserve-existing e2e 픽스처의 125번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field125Id | String | 125번 필드의 식별자 |
| body.field125Name | String | 125번 필드의 이름 |
| body.field125Count | Integer | 125번 필드의 개수 |

```json
{
  "field125Id": "id-125",
  "field125Name": "name-125"
}
```

125번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-126"></a>
### 보존 섹션 126 { #preserve-sec-126 }

이 섹션은 preserve-existing e2e 픽스처의 126번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field126Id | String | 126번 필드의 식별자 |
| body.field126Name | String | 126번 필드의 이름 |
| body.field126Count | Integer | 126번 필드의 개수 |

126번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-127"></a>
### 보존 섹션 127 { #preserve-sec-127 }

이 섹션은 preserve-existing e2e 픽스처의 127번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field127Id | String | 127번 필드의 식별자 |
| body.field127Name | String | 127번 필드의 이름 |
| body.field127Count | Integer | 127번 필드의 개수 |

127번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-128"></a>
### 보존 섹션 128 { #preserve-sec-128 }

이 섹션은 preserve-existing e2e 픽스처의 128번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field128Id | String | 128번 필드의 식별자 |
| body.field128Name | String | 128번 필드의 이름 |
| body.field128Count | Integer | 128번 필드의 개수 |

128번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-129"></a>
### 보존 섹션 129 { #preserve-sec-129 }

이 섹션은 preserve-existing e2e 픽스처의 129번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field129Id | String | 129번 필드의 식별자 |
| body.field129Name | String | 129번 필드의 이름 |
| body.field129Count | Integer | 129번 필드의 개수 |

129번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-130"></a>
### 보존 섹션 130 { #preserve-sec-130 }

이 섹션은 preserve-existing e2e 픽스처의 130번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field130Id | String | 130번 필드의 식별자 |
| body.field130Name | String | 130번 필드의 이름 |
| body.field130Count | Integer | 130번 필드의 개수 |

```json
{
  "field130Id": "id-130",
  "field130Name": "name-130"
}
```

130번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-131"></a>
### 보존 섹션 131 { #preserve-sec-131 }

이 섹션은 preserve-existing e2e 픽스처의 131번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field131Id | String | 131번 필드의 식별자 |
| body.field131Name | String | 131번 필드의 이름 |
| body.field131Count | Integer | 131번 필드의 개수 |

131번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-132"></a>
### 보존 섹션 132 { #preserve-sec-132 }

이 섹션은 preserve-existing e2e 픽스처의 132번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field132Id | String | 132번 필드의 식별자 |
| body.field132Name | String | 132번 필드의 이름 |
| body.field132Count | Integer | 132번 필드의 개수 |

132번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-133"></a>
### 보존 섹션 133 { #preserve-sec-133 }

이 섹션은 preserve-existing e2e 픽스처의 133번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field133Id | String | 133번 필드의 식별자 |
| body.field133Name | String | 133번 필드의 이름 |
| body.field133Count | Integer | 133번 필드의 개수 |

133번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-134"></a>
### 보존 섹션 134 { #preserve-sec-134 }

이 섹션은 preserve-existing e2e 픽스처의 134번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field134Id | String | 134번 필드의 식별자 |
| body.field134Name | String | 134번 필드의 이름 |
| body.field134Count | Integer | 134번 필드의 개수 |

134번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-135"></a>
### 보존 섹션 135 { #preserve-sec-135 }

이 섹션은 preserve-existing e2e 픽스처의 135번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field135Id | String | 135번 필드의 식별자 |
| body.field135Name | String | 135번 필드의 이름 |
| body.field135Count | Integer | 135번 필드의 개수 |

```json
{
  "field135Id": "id-135",
  "field135Name": "name-135"
}
```

135번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-136"></a>
### 보존 섹션 136 { #preserve-sec-136 }

이 섹션은 preserve-existing e2e 픽스처의 136번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field136Id | String | 136번 필드의 식별자 |
| body.field136Name | String | 136번 필드의 이름 |
| body.field136Count | Integer | 136번 필드의 개수 |

136번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-137"></a>
### 보존 섹션 137 { #preserve-sec-137 }

이 섹션은 preserve-existing e2e 픽스처의 137번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field137Id | String | 137번 필드의 식별자 |
| body.field137Name | String | 137번 필드의 이름 |
| body.field137Count | Integer | 137번 필드의 개수 |

137번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-138"></a>
### 보존 섹션 138 { #preserve-sec-138 }

이 섹션은 preserve-existing e2e 픽스처의 138번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field138Id | String | 138번 필드의 식별자 |
| body.field138Name | String | 138번 필드의 이름 |
| body.field138Count | Integer | 138번 필드의 개수 |

138번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-139"></a>
### 보존 섹션 139 { #preserve-sec-139 }

이 섹션은 preserve-existing e2e 픽스처의 139번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field139Id | String | 139번 필드의 식별자 |
| body.field139Name | String | 139번 필드의 이름 |
| body.field139Count | Integer | 139번 필드의 개수 |

139번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-140"></a>
### 보존 섹션 140 { #preserve-sec-140 }

이 섹션은 preserve-existing e2e 픽스처의 140번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field140Id | String | 140번 필드의 식별자 |
| body.field140Name | String | 140번 필드의 이름 |
| body.field140Count | Integer | 140번 필드의 개수 |

```json
{
  "field140Id": "id-140",
  "field140Name": "name-140"
}
```

140번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-141"></a>
### 보존 섹션 141 { #preserve-sec-141 }

이 섹션은 preserve-existing e2e 픽스처의 141번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field141Id | String | 141번 필드의 식별자 |
| body.field141Name | String | 141번 필드의 이름 |
| body.field141Count | Integer | 141번 필드의 개수 |

141번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-142"></a>
### 보존 섹션 142 { #preserve-sec-142 }

이 섹션은 preserve-existing e2e 픽스처의 142번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field142Id | String | 142번 필드의 식별자 |
| body.field142Name | String | 142번 필드의 이름 |
| body.field142Count | Integer | 142번 필드의 개수 |

142번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-143"></a>
### 보존 섹션 143 { #preserve-sec-143 }

이 섹션은 preserve-existing e2e 픽스처의 143번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field143Id | String | 143번 필드의 식별자 |
| body.field143Name | String | 143번 필드의 이름 |
| body.field143Count | Integer | 143번 필드의 개수 |

143번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-144"></a>
### 보존 섹션 144 { #preserve-sec-144 }

이 섹션은 preserve-existing e2e 픽스처의 144번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field144Id | String | 144번 필드의 식별자 |
| body.field144Name | String | 144번 필드의 이름 |
| body.field144Count | Integer | 144번 필드의 개수 |

144번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-145"></a>
### 보존 섹션 145 { #preserve-sec-145 }

이 섹션은 preserve-existing e2e 픽스처의 145번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field145Id | String | 145번 필드의 식별자 |
| body.field145Name | String | 145번 필드의 이름 |
| body.field145Count | Integer | 145번 필드의 개수 |

```json
{
  "field145Id": "id-145",
  "field145Name": "name-145"
}
```

145번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-146"></a>
### 보존 섹션 146 { #preserve-sec-146 }

이 섹션은 preserve-existing e2e 픽스처의 146번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field146Id | String | 146번 필드의 식별자 |
| body.field146Name | String | 146번 필드의 이름 |
| body.field146Count | Integer | 146번 필드의 개수 |

146번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-147"></a>
### 보존 섹션 147 { #preserve-sec-147 }

이 섹션은 preserve-existing e2e 픽스처의 147번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field147Id | String | 147번 필드의 식별자 |
| body.field147Name | String | 147번 필드의 이름 |
| body.field147Count | Integer | 147번 필드의 개수 |

147번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-148"></a>
### 보존 섹션 148 { #preserve-sec-148 }

이 섹션은 preserve-existing e2e 픽스처의 148번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field148Id | String | 148번 필드의 식별자 |
| body.field148Name | String | 148번 필드의 이름 |
| body.field148Count | Integer | 148번 필드의 개수 |

148번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-149"></a>
### 보존 섹션 149 { #preserve-sec-149 }

이 섹션은 preserve-existing e2e 픽스처의 149번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field149Id | String | 149번 필드의 식별자 |
| body.field149Name | String | 149번 필드의 이름 |
| body.field149Count | Integer | 149번 필드의 개수 |

149번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-150"></a>
### 보존 섹션 150 { #preserve-sec-150 }

이 섹션은 preserve-existing e2e 픽스처의 150번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field150Id | String | 150번 필드의 식별자 |
| body.field150Name | String | 150번 필드의 이름 |
| body.field150Count | Integer | 150번 필드의 개수 |

```json
{
  "field150Id": "id-150",
  "field150Name": "name-150"
}
```

150번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-151"></a>
### 보존 섹션 151 { #preserve-sec-151 }

이 섹션은 preserve-existing e2e 픽스처의 151번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field151Id | String | 151번 필드의 식별자 |
| body.field151Name | String | 151번 필드의 이름 |
| body.field151Count | Integer | 151번 필드의 개수 |

151번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-152"></a>
### 보존 섹션 152 { #preserve-sec-152 }

이 섹션은 preserve-existing e2e 픽스처의 152번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field152Id | String | 152번 필드의 식별자 |
| body.field152Name | String | 152번 필드의 이름 |
| body.field152Count | Integer | 152번 필드의 개수 |

152번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-153"></a>
### 보존 섹션 153 { #preserve-sec-153 }

이 섹션은 preserve-existing e2e 픽스처의 153번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field153Id | String | 153번 필드의 식별자 |
| body.field153Name | String | 153번 필드의 이름 |
| body.field153Count | Integer | 153번 필드의 개수 |

153번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-154"></a>
### 보존 섹션 154 { #preserve-sec-154 }

이 섹션은 preserve-existing e2e 픽스처의 154번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field154Id | String | 154번 필드의 식별자 |
| body.field154Name | String | 154번 필드의 이름 |
| body.field154Count | Integer | 154번 필드의 개수 |

154번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-155"></a>
### 보존 섹션 155 { #preserve-sec-155 }

이 섹션은 preserve-existing e2e 픽스처의 155번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field155Id | String | 155번 필드의 식별자 |
| body.field155Name | String | 155번 필드의 이름 |
| body.field155Count | Integer | 155번 필드의 개수 |

```json
{
  "field155Id": "id-155",
  "field155Name": "name-155"
}
```

155번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-156"></a>
### 보존 섹션 156 { #preserve-sec-156 }

이 섹션은 preserve-existing e2e 픽스처의 156번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field156Id | String | 156번 필드의 식별자 |
| body.field156Name | String | 156번 필드의 이름 |
| body.field156Count | Integer | 156번 필드의 개수 |

156번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-157"></a>
### 보존 섹션 157 { #preserve-sec-157 }

이 섹션은 preserve-existing e2e 픽스처의 157번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field157Id | String | 157번 필드의 식별자 |
| body.field157Name | String | 157번 필드의 이름 |
| body.field157Count | Integer | 157번 필드의 개수 |

157번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-158"></a>
### 보존 섹션 158 { #preserve-sec-158 }

이 섹션은 preserve-existing e2e 픽스처의 158번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field158Id | String | 158번 필드의 식별자 |
| body.field158Name | String | 158번 필드의 이름 |
| body.field158Count | Integer | 158번 필드의 개수 |

158번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-159"></a>
### 보존 섹션 159 { #preserve-sec-159 }

이 섹션은 preserve-existing e2e 픽스처의 159번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field159Id | String | 159번 필드의 식별자 |
| body.field159Name | String | 159번 필드의 이름 |
| body.field159Count | Integer | 159번 필드의 개수 |

159번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-160"></a>
### 보존 섹션 160 { #preserve-sec-160 }

이 섹션은 preserve-existing e2e 픽스처의 160번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field160Id | String | 160번 필드의 식별자 |
| body.field160Name | String | 160번 필드의 이름 |
| body.field160Count | Integer | 160번 필드의 개수 |

```json
{
  "field160Id": "id-160",
  "field160Name": "name-160"
}
```

160번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-161"></a>
### 보존 섹션 161 { #preserve-sec-161 }

이 섹션은 preserve-existing e2e 픽스처의 161번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field161Id | String | 161번 필드의 식별자 |
| body.field161Name | String | 161번 필드의 이름 |
| body.field161Count | Integer | 161번 필드의 개수 |

161번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-162"></a>
### 보존 섹션 162 { #preserve-sec-162 }

이 섹션은 preserve-existing e2e 픽스처의 162번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field162Id | String | 162번 필드의 식별자 |
| body.field162Name | String | 162번 필드의 이름 |
| body.field162Count | Integer | 162번 필드의 개수 |

162번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-163"></a>
### 보존 섹션 163 { #preserve-sec-163 }

이 섹션은 preserve-existing e2e 픽스처의 163번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field163Id | String | 163번 필드의 식별자 |
| body.field163Name | String | 163번 필드의 이름 |
| body.field163Count | Integer | 163번 필드의 개수 |

163번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-164"></a>
### 보존 섹션 164 { #preserve-sec-164 }

이 섹션은 preserve-existing e2e 픽스처의 164번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field164Id | String | 164번 필드의 식별자 |
| body.field164Name | String | 164번 필드의 이름 |
| body.field164Count | Integer | 164번 필드의 개수 |

164번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-165"></a>
### 보존 섹션 165 { #preserve-sec-165 }

이 섹션은 preserve-existing e2e 픽스처의 165번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field165Id | String | 165번 필드의 식별자 |
| body.field165Name | String | 165번 필드의 이름 |
| body.field165Count | Integer | 165번 필드의 개수 |

```json
{
  "field165Id": "id-165",
  "field165Name": "name-165"
}
```

165번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-166"></a>
### 보존 섹션 166 { #preserve-sec-166 }

이 섹션은 preserve-existing e2e 픽스처의 166번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field166Id | String | 166번 필드의 식별자 |
| body.field166Name | String | 166번 필드의 이름 |
| body.field166Count | Integer | 166번 필드의 개수 |

166번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-167"></a>
### 보존 섹션 167 { #preserve-sec-167 }

이 섹션은 preserve-existing e2e 픽스처의 167번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field167Id | String | 167번 필드의 식별자 |
| body.field167Name | String | 167번 필드의 이름 |
| body.field167Count | Integer | 167번 필드의 개수 |

167번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-168"></a>
### 보존 섹션 168 { #preserve-sec-168 }

이 섹션은 preserve-existing e2e 픽스처의 168번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field168Id | String | 168번 필드의 식별자 |
| body.field168Name | String | 168번 필드의 이름 |
| body.field168Count | Integer | 168번 필드의 개수 |

168번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-169"></a>
### 보존 섹션 169 { #preserve-sec-169 }

이 섹션은 preserve-existing e2e 픽스처의 169번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field169Id | String | 169번 필드의 식별자 |
| body.field169Name | String | 169번 필드의 이름 |
| body.field169Count | Integer | 169번 필드의 개수 |

169번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-170"></a>
### 보존 섹션 170 { #preserve-sec-170 }

이 섹션은 preserve-existing e2e 픽스처의 170번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field170Id | String | 170번 필드의 식별자 |
| body.field170Name | String | 170번 필드의 이름 |
| body.field170Count | Integer | 170번 필드의 개수 |

```json
{
  "field170Id": "id-170",
  "field170Name": "name-170"
}
```

170번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-171"></a>
### 보존 섹션 171 { #preserve-sec-171 }

이 섹션은 preserve-existing e2e 픽스처의 171번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field171Id | String | 171번 필드의 식별자 |
| body.field171Name | String | 171번 필드의 이름 |
| body.field171Count | Integer | 171번 필드의 개수 |

171번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-172"></a>
### 보존 섹션 172 { #preserve-sec-172 }

이 섹션은 preserve-existing e2e 픽스처의 172번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field172Id | String | 172번 필드의 식별자 |
| body.field172Name | String | 172번 필드의 이름 |
| body.field172Count | Integer | 172번 필드의 개수 |

172번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-173"></a>
### 보존 섹션 173 { #preserve-sec-173 }

이 섹션은 preserve-existing e2e 픽스처의 173번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field173Id | String | 173번 필드의 식별자 |
| body.field173Name | String | 173번 필드의 이름 |
| body.field173Count | Integer | 173번 필드의 개수 |

173번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-174"></a>
### 보존 섹션 174 { #preserve-sec-174 }

이 섹션은 preserve-existing e2e 픽스처의 174번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field174Id | String | 174번 필드의 식별자 |
| body.field174Name | String | 174번 필드의 이름 |
| body.field174Count | Integer | 174번 필드의 개수 |

174번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-175"></a>
### 보존 섹션 175 { #preserve-sec-175 }

이 섹션은 preserve-existing e2e 픽스처의 175번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field175Id | String | 175번 필드의 식별자 |
| body.field175Name | String | 175번 필드의 이름 |
| body.field175Count | Integer | 175번 필드의 개수 |

```json
{
  "field175Id": "id-175",
  "field175Name": "name-175"
}
```

175번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-176"></a>
### 보존 섹션 176 { #preserve-sec-176 }

이 섹션은 preserve-existing e2e 픽스처의 176번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field176Id | String | 176번 필드의 식별자 |
| body.field176Name | String | 176번 필드의 이름 |
| body.field176Count | Integer | 176번 필드의 개수 |

176번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-177"></a>
### 보존 섹션 177 { #preserve-sec-177 }

이 섹션은 preserve-existing e2e 픽스처의 177번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field177Id | String | 177번 필드의 식별자 |
| body.field177Name | String | 177번 필드의 이름 |
| body.field177Count | Integer | 177번 필드의 개수 |

177번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-178"></a>
### 보존 섹션 178 { #preserve-sec-178 }

이 섹션은 preserve-existing e2e 픽스처의 178번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field178Id | String | 178번 필드의 식별자 |
| body.field178Name | String | 178번 필드의 이름 |
| body.field178Count | Integer | 178번 필드의 개수 |

178번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-179"></a>
### 보존 섹션 179 { #preserve-sec-179 }

이 섹션은 preserve-existing e2e 픽스처의 179번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field179Id | String | 179번 필드의 식별자 |
| body.field179Name | String | 179번 필드의 이름 |
| body.field179Count | Integer | 179번 필드의 개수 |

179번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-180"></a>
### 보존 섹션 180 { #preserve-sec-180 }

이 섹션은 preserve-existing e2e 픽스처의 180번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field180Id | String | 180번 필드의 식별자 |
| body.field180Name | String | 180번 필드의 이름 |
| body.field180Count | Integer | 180번 필드의 개수 |

```json
{
  "field180Id": "id-180",
  "field180Name": "name-180"
}
```

180번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-181"></a>
### 보존 섹션 181 { #preserve-sec-181 }

이 섹션은 preserve-existing e2e 픽스처의 181번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field181Id | String | 181번 필드의 식별자 |
| body.field181Name | String | 181번 필드의 이름 |
| body.field181Count | Integer | 181번 필드의 개수 |

181번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-182"></a>
### 보존 섹션 182 { #preserve-sec-182 }

이 섹션은 preserve-existing e2e 픽스처의 182번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field182Id | String | 182번 필드의 식별자 |
| body.field182Name | String | 182번 필드의 이름 |
| body.field182Count | Integer | 182번 필드의 개수 |

182번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-183"></a>
### 보존 섹션 183 { #preserve-sec-183 }

이 섹션은 preserve-existing e2e 픽스처의 183번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field183Id | String | 183번 필드의 식별자 |
| body.field183Name | String | 183번 필드의 이름 |
| body.field183Count | Integer | 183번 필드의 개수 |

183번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-184"></a>
### 보존 섹션 184 { #preserve-sec-184 }

이 섹션은 preserve-existing e2e 픽스처의 184번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field184Id | String | 184번 필드의 식별자 |
| body.field184Name | String | 184번 필드의 이름 |
| body.field184Count | Integer | 184번 필드의 개수 |

184번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-185"></a>
### 보존 섹션 185 { #preserve-sec-185 }

이 섹션은 preserve-existing e2e 픽스처의 185번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field185Id | String | 185번 필드의 식별자 |
| body.field185Name | String | 185번 필드의 이름 |
| body.field185Count | Integer | 185번 필드의 개수 |

```json
{
  "field185Id": "id-185",
  "field185Name": "name-185"
}
```

185번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-186"></a>
### 보존 섹션 186 { #preserve-sec-186 }

이 섹션은 preserve-existing e2e 픽스처의 186번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field186Id | String | 186번 필드의 식별자 |
| body.field186Name | String | 186번 필드의 이름 |
| body.field186Count | Integer | 186번 필드의 개수 |

186번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-187"></a>
### 보존 섹션 187 { #preserve-sec-187 }

이 섹션은 preserve-existing e2e 픽스처의 187번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field187Id | String | 187번 필드의 식별자 |
| body.field187Name | String | 187번 필드의 이름 |
| body.field187Count | Integer | 187번 필드의 개수 |

187번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-188"></a>
### 보존 섹션 188 { #preserve-sec-188 }

이 섹션은 preserve-existing e2e 픽스처의 188번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field188Id | String | 188번 필드의 식별자 |
| body.field188Name | String | 188번 필드의 이름 |
| body.field188Count | Integer | 188번 필드의 개수 |

188번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-189"></a>
### 보존 섹션 189 { #preserve-sec-189 }

이 섹션은 preserve-existing e2e 픽스처의 189번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field189Id | String | 189번 필드의 식별자 |
| body.field189Name | String | 189번 필드의 이름 |
| body.field189Count | Integer | 189번 필드의 개수 |

189번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-190"></a>
### 보존 섹션 190 { #preserve-sec-190 }

이 섹션은 preserve-existing e2e 픽스처의 190번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field190Id | String | 190번 필드의 식별자 |
| body.field190Name | String | 190번 필드의 이름 |
| body.field190Count | Integer | 190번 필드의 개수 |

```json
{
  "field190Id": "id-190",
  "field190Name": "name-190"
}
```

190번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-191"></a>
### 보존 섹션 191 { #preserve-sec-191 }

이 섹션은 preserve-existing e2e 픽스처의 191번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field191Id | String | 191번 필드의 식별자 |
| body.field191Name | String | 191번 필드의 이름 |
| body.field191Count | Integer | 191번 필드의 개수 |

191번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-192"></a>
### 보존 섹션 192 { #preserve-sec-192 }

이 섹션은 preserve-existing e2e 픽스처의 192번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field192Id | String | 192번 필드의 식별자 |
| body.field192Name | String | 192번 필드의 이름 |
| body.field192Count | Integer | 192번 필드의 개수 |

192번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-193"></a>
### 보존 섹션 193 { #preserve-sec-193 }

이 섹션은 preserve-existing e2e 픽스처의 193번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field193Id | String | 193번 필드의 식별자 |
| body.field193Name | String | 193번 필드의 이름 |
| body.field193Count | Integer | 193번 필드의 개수 |

193번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-194"></a>
### 보존 섹션 194 { #preserve-sec-194 }

이 섹션은 preserve-existing e2e 픽스처의 194번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field194Id | String | 194번 필드의 식별자 |
| body.field194Name | String | 194번 필드의 이름 |
| body.field194Count | Integer | 194번 필드의 개수 |

194번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-195"></a>
### 보존 섹션 195 { #preserve-sec-195 }

이 섹션은 preserve-existing e2e 픽스처의 195번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field195Id | String | 195번 필드의 식별자 |
| body.field195Name | String | 195번 필드의 이름 |
| body.field195Count | Integer | 195번 필드의 개수 |

```json
{
  "field195Id": "id-195",
  "field195Name": "name-195"
}
```

195번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-196"></a>
### 보존 섹션 196 { #preserve-sec-196 }

이 섹션은 preserve-existing e2e 픽스처의 196번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field196Id | String | 196번 필드의 식별자 |
| body.field196Name | String | 196번 필드의 이름 |
| body.field196Count | Integer | 196번 필드의 개수 |

196번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-197"></a>
### 보존 섹션 197 { #preserve-sec-197 }

이 섹션은 preserve-existing e2e 픽스처의 197번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field197Id | String | 197번 필드의 식별자 |
| body.field197Name | String | 197번 필드의 이름 |
| body.field197Count | Integer | 197번 필드의 개수 |

197번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-198"></a>
### 보존 섹션 198 { #preserve-sec-198 }

이 섹션은 preserve-existing e2e 픽스처의 198번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field198Id | String | 198번 필드의 식별자 |
| body.field198Name | String | 198번 필드의 이름 |
| body.field198Count | Integer | 198번 필드의 개수 |

198번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-199"></a>
### 보존 섹션 199 { #preserve-sec-199 }

이 섹션은 preserve-existing e2e 픽스처의 199번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field199Id | String | 199번 필드의 식별자 |
| body.field199Name | String | 199번 필드의 이름 |
| body.field199Count | Integer | 199번 필드의 개수 |

199번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-200"></a>
### 보존 섹션 200 { #preserve-sec-200 }

이 섹션은 preserve-existing e2e 픽스처의 200번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field200Id | String | 200번 필드의 식별자 |
| body.field200Name | String | 200번 필드의 이름 |
| body.field200Count | Integer | 200번 필드의 개수 |

```json
{
  "field200Id": "id-200",
  "field200Name": "name-200"
}
```

200번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-201"></a>
### 보존 섹션 201 { #preserve-sec-201 }

이 섹션은 preserve-existing e2e 픽스처의 201번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field201Id | String | 201번 필드의 식별자 |
| body.field201Name | String | 201번 필드의 이름 |
| body.field201Count | Integer | 201번 필드의 개수 |

201번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-202"></a>
### 보존 섹션 202 { #preserve-sec-202 }

이 섹션은 preserve-existing e2e 픽스처의 202번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field202Id | String | 202번 필드의 식별자 |
| body.field202Name | String | 202번 필드의 이름 |
| body.field202Count | Integer | 202번 필드의 개수 |

202번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-203"></a>
### 보존 섹션 203 { #preserve-sec-203 }

이 섹션은 preserve-existing e2e 픽스처의 203번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field203Id | String | 203번 필드의 식별자 |
| body.field203Name | String | 203번 필드의 이름 |
| body.field203Count | Integer | 203번 필드의 개수 |

203번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-204"></a>
### 보존 섹션 204 { #preserve-sec-204 }

이 섹션은 preserve-existing e2e 픽스처의 204번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field204Id | String | 204번 필드의 식별자 |
| body.field204Name | String | 204번 필드의 이름 |
| body.field204Count | Integer | 204번 필드의 개수 |

204번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-205"></a>
### 보존 섹션 205 { #preserve-sec-205 }

이 섹션은 preserve-existing e2e 픽스처의 205번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field205Id | String | 205번 필드의 식별자 |
| body.field205Name | String | 205번 필드의 이름 |
| body.field205Count | Integer | 205번 필드의 개수 |

```json
{
  "field205Id": "id-205",
  "field205Name": "name-205"
}
```

205번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-206"></a>
### 보존 섹션 206 { #preserve-sec-206 }

이 섹션은 preserve-existing e2e 픽스처의 206번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field206Id | String | 206번 필드의 식별자 |
| body.field206Name | String | 206번 필드의 이름 |
| body.field206Count | Integer | 206번 필드의 개수 |

206번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-207"></a>
### 보존 섹션 207 { #preserve-sec-207 }

이 섹션은 preserve-existing e2e 픽스처의 207번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field207Id | String | 207번 필드의 식별자 |
| body.field207Name | String | 207번 필드의 이름 |
| body.field207Count | Integer | 207번 필드의 개수 |

207번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-208"></a>
### 보존 섹션 208 { #preserve-sec-208 }

이 섹션은 preserve-existing e2e 픽스처의 208번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field208Id | String | 208번 필드의 식별자 |
| body.field208Name | String | 208번 필드의 이름 |
| body.field208Count | Integer | 208번 필드의 개수 |

208번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-209"></a>
### 보존 섹션 209 { #preserve-sec-209 }

이 섹션은 preserve-existing e2e 픽스처의 209번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field209Id | String | 209번 필드의 식별자 |
| body.field209Name | String | 209번 필드의 이름 |
| body.field209Count | Integer | 209번 필드의 개수 |

209번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-210"></a>
### 보존 섹션 210 { #preserve-sec-210 }

이 섹션은 preserve-existing e2e 픽스처의 210번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field210Id | String | 210번 필드의 식별자 |
| body.field210Name | String | 210번 필드의 이름 |
| body.field210Count | Integer | 210번 필드의 개수 |

```json
{
  "field210Id": "id-210",
  "field210Name": "name-210"
}
```

210번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-211"></a>
### 보존 섹션 211 { #preserve-sec-211 }

이 섹션은 preserve-existing e2e 픽스처의 211번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field211Id | String | 211번 필드의 식별자 |
| body.field211Name | String | 211번 필드의 이름 |
| body.field211Count | Integer | 211번 필드의 개수 |

211번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-212"></a>
### 보존 섹션 212 { #preserve-sec-212 }

이 섹션은 preserve-existing e2e 픽스처의 212번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field212Id | String | 212번 필드의 식별자 |
| body.field212Name | String | 212번 필드의 이름 |
| body.field212Count | Integer | 212번 필드의 개수 |

212번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-213"></a>
### 보존 섹션 213 { #preserve-sec-213 }

이 섹션은 preserve-existing e2e 픽스처의 213번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field213Id | String | 213번 필드의 식별자 |
| body.field213Name | String | 213번 필드의 이름 |
| body.field213Count | Integer | 213번 필드의 개수 |

213번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-214"></a>
### 보존 섹션 214 { #preserve-sec-214 }

이 섹션은 preserve-existing e2e 픽스처의 214번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field214Id | String | 214번 필드의 식별자 |
| body.field214Name | String | 214번 필드의 이름 |
| body.field214Count | Integer | 214번 필드의 개수 |

214번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-215"></a>
### 보존 섹션 215 { #preserve-sec-215 }

이 섹션은 preserve-existing e2e 픽스처의 215번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field215Id | String | 215번 필드의 식별자 |
| body.field215Name | String | 215번 필드의 이름 |
| body.field215Count | Integer | 215번 필드의 개수 |

```json
{
  "field215Id": "id-215",
  "field215Name": "name-215"
}
```

215번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-216"></a>
### 보존 섹션 216 { #preserve-sec-216 }

이 섹션은 preserve-existing e2e 픽스처의 216번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field216Id | String | 216번 필드의 식별자 |
| body.field216Name | String | 216번 필드의 이름 |
| body.field216Count | Integer | 216번 필드의 개수 |

216번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-217"></a>
### 보존 섹션 217 { #preserve-sec-217 }

이 섹션은 preserve-existing e2e 픽스처의 217번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field217Id | String | 217번 필드의 식별자 |
| body.field217Name | String | 217번 필드의 이름 |
| body.field217Count | Integer | 217번 필드의 개수 |

217번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-218"></a>
### 보존 섹션 218 { #preserve-sec-218 }

이 섹션은 preserve-existing e2e 픽스처의 218번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field218Id | String | 218번 필드의 식별자 |
| body.field218Name | String | 218번 필드의 이름 |
| body.field218Count | Integer | 218번 필드의 개수 |

218번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-219"></a>
### 보존 섹션 219 { #preserve-sec-219 }

이 섹션은 preserve-existing e2e 픽스처의 219번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field219Id | String | 219번 필드의 식별자 |
| body.field219Name | String | 219번 필드의 이름 |
| body.field219Count | Integer | 219번 필드의 개수 |

219번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-220"></a>
### 보존 섹션 220 { #preserve-sec-220 }

이 섹션은 preserve-existing e2e 픽스처의 220번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field220Id | String | 220번 필드의 식별자 |
| body.field220Name | String | 220번 필드의 이름 |
| body.field220Count | Integer | 220번 필드의 개수 |

```json
{
  "field220Id": "id-220",
  "field220Name": "name-220"
}
```

220번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-221"></a>
### 보존 섹션 221 { #preserve-sec-221 }

이 섹션은 preserve-existing e2e 픽스처의 221번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field221Id | String | 221번 필드의 식별자 |
| body.field221Name | String | 221번 필드의 이름 |
| body.field221Count | Integer | 221번 필드의 개수 |

221번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-222"></a>
### 보존 섹션 222 { #preserve-sec-222 }

이 섹션은 preserve-existing e2e 픽스처의 222번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field222Id | String | 222번 필드의 식별자 |
| body.field222Name | String | 222번 필드의 이름 |
| body.field222Count | Integer | 222번 필드의 개수 |

222번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-223"></a>
### 보존 섹션 223 { #preserve-sec-223 }

이 섹션은 preserve-existing e2e 픽스처의 223번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field223Id | String | 223번 필드의 식별자 |
| body.field223Name | String | 223번 필드의 이름 |
| body.field223Count | Integer | 223번 필드의 개수 |

223번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-224"></a>
### 보존 섹션 224 { #preserve-sec-224 }

이 섹션은 preserve-existing e2e 픽스처의 224번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field224Id | String | 224번 필드의 식별자 |
| body.field224Name | String | 224번 필드의 이름 |
| body.field224Count | Integer | 224번 필드의 개수 |

224번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-225"></a>
### 보존 섹션 225 { #preserve-sec-225 }

이 섹션은 preserve-existing e2e 픽스처의 225번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field225Id | String | 225번 필드의 식별자 |
| body.field225Name | String | 225번 필드의 이름 |
| body.field225Count | Integer | 225번 필드의 개수 |

```json
{
  "field225Id": "id-225",
  "field225Name": "name-225"
}
```

225번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-226"></a>
### 보존 섹션 226 { #preserve-sec-226 }

이 섹션은 preserve-existing e2e 픽스처의 226번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field226Id | String | 226번 필드의 식별자 |
| body.field226Name | String | 226번 필드의 이름 |
| body.field226Count | Integer | 226번 필드의 개수 |

226번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-227"></a>
### 보존 섹션 227 { #preserve-sec-227 }

이 섹션은 preserve-existing e2e 픽스처의 227번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field227Id | String | 227번 필드의 식별자 |
| body.field227Name | String | 227번 필드의 이름 |
| body.field227Count | Integer | 227번 필드의 개수 |

227번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-228"></a>
### 보존 섹션 228 { #preserve-sec-228 }

이 섹션은 preserve-existing e2e 픽스처의 228번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field228Id | String | 228번 필드의 식별자 |
| body.field228Name | String | 228번 필드의 이름 |
| body.field228Count | Integer | 228번 필드의 개수 |

228번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-229"></a>
### 보존 섹션 229 { #preserve-sec-229 }

이 섹션은 preserve-existing e2e 픽스처의 229번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field229Id | String | 229번 필드의 식별자 |
| body.field229Name | String | 229번 필드의 이름 |
| body.field229Count | Integer | 229번 필드의 개수 |

229번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-230"></a>
### 보존 섹션 230 { #preserve-sec-230 }

이 섹션은 preserve-existing e2e 픽스처의 230번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field230Id | String | 230번 필드의 식별자 |
| body.field230Name | String | 230번 필드의 이름 |
| body.field230Count | Integer | 230번 필드의 개수 |

```json
{
  "field230Id": "id-230",
  "field230Name": "name-230"
}
```

230번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-231"></a>
### 보존 섹션 231 { #preserve-sec-231 }

이 섹션은 preserve-existing e2e 픽스처의 231번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field231Id | String | 231번 필드의 식별자 |
| body.field231Name | String | 231번 필드의 이름 |
| body.field231Count | Integer | 231번 필드의 개수 |

231번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-232"></a>
### 보존 섹션 232 { #preserve-sec-232 }

이 섹션은 preserve-existing e2e 픽스처의 232번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field232Id | String | 232번 필드의 식별자 |
| body.field232Name | String | 232번 필드의 이름 |
| body.field232Count | Integer | 232번 필드의 개수 |

232번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-233"></a>
### 보존 섹션 233 { #preserve-sec-233 }

이 섹션은 preserve-existing e2e 픽스처의 233번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field233Id | String | 233번 필드의 식별자 |
| body.field233Name | String | 233번 필드의 이름 |
| body.field233Count | Integer | 233번 필드의 개수 |

233번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-234"></a>
### 보존 섹션 234 { #preserve-sec-234 }

이 섹션은 preserve-existing e2e 픽스처의 234번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field234Id | String | 234번 필드의 식별자 |
| body.field234Name | String | 234번 필드의 이름 |
| body.field234Count | Integer | 234번 필드의 개수 |

234번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-235"></a>
### 보존 섹션 235 { #preserve-sec-235 }

이 섹션은 preserve-existing e2e 픽스처의 235번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field235Id | String | 235번 필드의 식별자 |
| body.field235Name | String | 235번 필드의 이름 |
| body.field235Count | Integer | 235번 필드의 개수 |

```json
{
  "field235Id": "id-235",
  "field235Name": "name-235"
}
```

235번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-236"></a>
### 보존 섹션 236 { #preserve-sec-236 }

이 섹션은 preserve-existing e2e 픽스처의 236번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field236Id | String | 236번 필드의 식별자 |
| body.field236Name | String | 236번 필드의 이름 |
| body.field236Count | Integer | 236번 필드의 개수 |

236번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-237"></a>
### 보존 섹션 237 { #preserve-sec-237 }

이 섹션은 preserve-existing e2e 픽스처의 237번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field237Id | String | 237번 필드의 식별자 |
| body.field237Name | String | 237번 필드의 이름 |
| body.field237Count | Integer | 237번 필드의 개수 |

237번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-238"></a>
### 보존 섹션 238 { #preserve-sec-238 }

이 섹션은 preserve-existing e2e 픽스처의 238번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field238Id | String | 238번 필드의 식별자 |
| body.field238Name | String | 238번 필드의 이름 |
| body.field238Count | Integer | 238번 필드의 개수 |

238번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-239"></a>
### 보존 섹션 239 { #preserve-sec-239 }

이 섹션은 preserve-existing e2e 픽스처의 239번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field239Id | String | 239번 필드의 식별자 |
| body.field239Name | String | 239번 필드의 이름 |
| body.field239Count | Integer | 239번 필드의 개수 |

239번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.

<a id="preserve-sec-240"></a>
### 보존 섹션 240 { #preserve-sec-240 }

이 섹션은 preserve-existing e2e 픽스처의 240번째 섹션입니다. 아래 표와 예제는
섹션마다 필드 이름이 달라 서로 구분됩니다.

| 값 | 타입 | 설명 |
|---|---|---|
| body.field240Id | String | 240번 필드의 식별자 |
| body.field240Name | String | 240번 필드의 이름 |
| body.field240Count | Integer | 240번 필드의 개수 |

```json
{
  "field240Id": "id-240",
  "field240Name": "name-240"
}
```

240번 섹션의 마지막 설명 문장입니다. 이 문장은 재번역 여부를 줄 단위로 판정할 때
쓰입니다.
