<!-- machine_translated: true -->

<!-- pre-align:aligned sig=17d7019d2237 -->

<!-- pre-align:aligned sig=e2epreserve -->

<a id="preserve-e2e"></a>
# preserve-existing e2e フィクスチャ

この文書は自動生成された e2e フィクスチャです (20260903-234410)。

<a id="preserve-sec-1"></a>
### 保存セクション 1 { #preserve-sec-1 }

このセクションは preserve-existing e2e フィクスチャの 1 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field1Id | String | フィールド 1 の識別子 |
| body.field1Name | String | フィールド 1 の名前 |
| body.field1Count | Integer | フィールド 1 の個数 |

セクション 1 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-2"></a>
### 保存セクション 2 { #preserve-sec-2 }

このセクションは preserve-existing e2e フィクスチャの 2 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field2Id | String | フィールド 2 の識別子 |
| body.field2Name | String | フィールド 2 の名前 |
| body.field2Count | Integer | フィールド 2 の個数 |

セクション 2 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-3"></a>
### 保存セクション 3 { #preserve-sec-3 }

このセクションは preserve-existing e2e フィクスチャの 3 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field3Id | String | フィールド 3 の識別子 |
| body.field3Name | String | フィールド 3 の名前 |
| body.field3Count | Integer | フィールド 3 の個数 |

セクション 3 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-4"></a>
### 保存セクション 4 { #preserve-sec-4 }

このセクションは preserve-existing e2e フィクスチャの 4 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field4Id | String | フィールド 4 の識別子 |
| body.field4Name | String | フィールド 4 の名前 |
| body.field4Count | Integer | フィールド 4 の個数 |

セクション 4 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-5"></a>
### 保存セクション 5 { #preserve-sec-5 }

このセクションは preserve-existing e2e フィクスチャの 5 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field5Id | String | フィールド 5 の識別子 |
| body.field5Name | String | フィールド 5 の名前 |
| body.field5Count | Integer | フィールド 5 の個数 |

```json
{
  "field5Id": "id-5",
  "field5Name": "name-5"
}
```

セクション 5 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-6"></a>
### 保存セクション 6 { #preserve-sec-6 }

このセクションは preserve-existing e2e フィクスチャの 6 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field6Id | String | フィールド 6 の識別子 |
| body.field6Name | String | フィールド 6 の名前 |
| body.field6Count | Integer | フィールド 6 の個数 |

セクション 6 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-7"></a>
### 保存セクション 7 { #preserve-sec-7 }

このセクションは preserve-existing e2e フィクスチャの 7 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field7Id | String | フィールド 7 の識別子 |
| body.field7Name | String | フィールド 7 の名前 |
| body.field7Count | Integer | フィールド 7 の個数 |

セクション 7 の最後の説明文を 20260903-234410 で変更しました。このセクションのみ再翻訳される必要があり、残りのセクションは既存の翻訳がそのまま残る必要があります。

<a id="preserve-sec-8"></a>
### 保存セクション 8 { #preserve-sec-8 }

このセクションは preserve-existing e2e フィクスチャの 8 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field8Id | String | フィールド 8 の識別子 |
| body.field8Name | String | フィールド 8 の名前 |
| body.field8Count | Integer | フィールド 8 の個数 |

セクション 8 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-9"></a>
### 保存セクション 9 { #preserve-sec-9 }

このセクションは preserve-existing e2e フィクスチャの 9 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field9Id | String | フィールド 9 の識別子 |
| body.field9Name | String | フィールド 9 の名前 |
| body.field9Count | Integer | フィールド 9 の個数 |

セクション 9 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-10"></a>
### 保存セクション 10 { #preserve-sec-10 }

このセクションは preserve-existing e2e フィクスチャの 10 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field10Id | String | フィールド 10 の識別子 |
| body.field10Name | String | フィールド 10 の名前 |
| body.field10Count | Integer | フィールド 10 の個数 |

```json
{
  "field10Id": "id-10",
  "field10Name": "name-10"
}
```

セクション 10 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-11"></a>
### 保存セクション 11 { #preserve-sec-11 }

このセクションは preserve-existing e2e フィクスチャの 11 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field11Id | String | フィールド 11 の識別子 |
| body.field11Name | String | フィールド 11 の名前 |
| body.field11Count | Integer | フィールド 11 の個数 |

セクション 11 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-12"></a>
### 保存セクション 12 { #preserve-sec-12 }

このセクションは preserve-existing e2e フィクスチャの 12 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field12Id | String | フィールド 12 の識別子 |
| body.field12Name | String | フィールド 12 の名前 |
| body.field12Count | Integer | フィールド 12 の個数 |

セクション 12 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-13"></a>
### 保存セクション 13 { #preserve-sec-13 }

このセクションは preserve-existing e2e フィクスチャの 13 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field13Id | String | フィールド 13 の識別子 |
| body.field13Name | String | フィールド 13 の名前 |
| body.field13Count | Integer | フィールド 13 の個数 |

セクション 13 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-14"></a>
### 保存セクション 14 { #preserve-sec-14 }

このセクションは preserve-existing e2e フィクスチャの 14 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field14Id | String | フィールド 14 の識別子 |
| body.field14Name | String | フィールド 14 の名前 |
| body.field14Count | Integer | フィールド 14 の個数 |

セクション 14 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-15"></a>
### 保存セクション 15 { #preserve-sec-15 }

このセクションは preserve-existing e2e フィクスチャの 15 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field15Id | String | フィールド 15 の識別子 |
| body.field15Name | String | フィールド 15 の名前 |
| body.field15Count | Integer | フィールド 15 の個数 |

```json
{
  "field15Id": "id-15",
  "field15Name": "name-15"
}
```

セクション 15 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-16"></a>
### 保存セクション 16 { #preserve-sec-16 }

このセクションは preserve-existing e2e フィクスチャの 16 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field16Id | String | フィールド 16 の識別子 |
| body.field16Name | String | フィールド 16 の名前 |
| body.field16Count | Integer | フィールド 16 の個数 |

セクション 16 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-17"></a>
### 保存セクション 17 { #preserve-sec-17 }

このセクションは preserve-existing e2e フィクスチャの 17 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field17Id | String | フィールド 17 の識別子 |
| body.field17Name | String | フィールド 17 の名前 |
| body.field17Count | Integer | フィールド 17 の個数 |

セクション 17 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-18"></a>
### 保存セクション 18 { #preserve-sec-18 }

このセクションは preserve-existing e2e フィクスチャの 18 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field18Id | String | フィールド 18 の識別子 |
| body.field18Name | String | フィールド 18 の名前 |
| body.field18Count | Integer | フィールド 18 の個数 |

セクション 18 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-19"></a>
### 保存セクション 19 { #preserve-sec-19 }

このセクションは preserve-existing e2e フィクスチャの 19 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field19Id | String | フィールド 19 の識別子 |
| body.field19Name | String | フィールド 19 の名前 |
| body.field19Count | Integer | フィールド 19 の個数 |

セクション 19 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-20"></a>
### 保存セクション 20 { #preserve-sec-20 }

このセクションは preserve-existing e2e フィクスチャの 20 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field20Id | String | フィールド 20 の識別子 |
| body.field20Name | String | フィールド 20 の名前 |
| body.field20Count | Integer | フィールド 20 の個数 |

```json
{
  "field20Id": "id-20",
  "field20Name": "name-20"
}
```

セクション 20 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-21"></a>
### 保存セクション 21 { #preserve-sec-21 }

このセクションは preserve-existing e2e フィクスチャの 21 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field21Id | String | フィールド 21 の識別子 |
| body.field21Name | String | フィールド 21 の名前 |
| body.field21Count | Integer | フィールド 21 の個数 |

セクション 21 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-22"></a>
### 保存セクション 22 { #preserve-sec-22 }

このセクションは preserve-existing e2e フィクスチャの 22 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field22Id | String | フィールド 22 の識別子 |
| body.field22Name | String | フィールド 22 の名前 |
| body.field22Count | Integer | フィールド 22 の個数 |

セクション 22 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-23"></a>
### 保存セクション 23 { #preserve-sec-23 }

このセクションは preserve-existing e2e フィクスチャの 23 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field23Id | String | フィールド 23 の識別子 |
| body.field23Name | String | フィールド 23 の名前 |
| body.field23Count | Integer | フィールド 23 の個数 |

セクション 23 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-24"></a>
### 保存セクション 24 { #preserve-sec-24 }

このセクションは preserve-existing e2e フィクスチャの 24 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field24Id | String | フィールド 24 の識別子 |
| body.field24Name | String | フィールド 24 の名前 |
| body.field24Count | Integer | フィールド 24 の個数 |

セクション 24 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-25"></a>
### 保存セクション 25 { #preserve-sec-25 }

このセクションは preserve-existing e2e フィクスチャの 25 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field25Id | String | フィールド 25 の識別子 |
| body.field25Name | String | フィールド 25 の名前 |
| body.field25Count | Integer | フィールド 25 の個数 |

```json
{
  "field25Id": "id-25",
  "field25Name": "name-25"
}
```

セクション 25 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-26"></a>
### 保存セクション 26 { #preserve-sec-26 }

このセクションは preserve-existing e2e フィクスチャの 26 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field26Id | String | フィールド 26 の識別子 |
| body.field26Name | String | フィールド 26 の名前 |
| body.field26Count | Integer | フィールド 26 の個数 |

セクション 26 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-27"></a>
### 保存セクション 27 { #preserve-sec-27 }

このセクションは preserve-existing e2e フィクスチャの 27 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field27Id | String | フィールド 27 の識別子 |
| body.field27Name | String | フィールド 27 の名前 |
| body.field27Count | Integer | フィールド 27 の個数 |

セクション 27 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-28"></a>
### 保存セクション 28 { #preserve-sec-28 }

このセクションは preserve-existing e2e フィクスチャの 28 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field28Id | String | フィールド 28 の識別子 |
| body.field28Name | String | フィールド 28 の名前 |
| body.field28Count | Integer | フィールド 28 の個数 |

セクション 28 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-29"></a>
### 保存セクション 29 { #preserve-sec-29 }

このセクションは preserve-existing e2e フィクスチャの 29 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field29Id | String | フィールド 29 の識別子 |
| body.field29Name | String | フィールド 29 の名前 |
| body.field29Count | Integer | フィールド 29 の個数 |

セクション 29 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-30"></a>
### 保存セクション 30 { #preserve-sec-30 }

このセクションは preserve-existing e2e フィクスチャの 30 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field30Id | String | フィールド 30 の識別子 |
| body.field30Name | String | フィールド 30 の名前 |
| body.field30Count | Integer | フィールド 30 の個数 |

```json
{
  "field30Id": "id-30",
  "field30Name": "name-30"
}
```

セクション 30 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-31"></a>
### 保存セクション 31 { #preserve-sec-31 }

このセクションは preserve-existing e2e フィクスチャの 31 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field31Id | String | フィールド 31 の識別子 |
| body.field31Name | String | フィールド 31 の名前 |
| body.field31Count | Integer | フィールド 31 の個数 |

セクション 31 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-32"></a>
### 保存セクション 32 { #preserve-sec-32 }

このセクションは preserve-existing e2e フィクスチャの 32 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field32Id | String | フィールド 32 の識別子 |
| body.field32Name | String | フィールド 32 の名前 |
| body.field32Count | Integer | フィールド 32 の個数 |

セクション 32 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-33"></a>
### 保存セクション 33 { #preserve-sec-33 }

このセクションは preserve-existing e2e フィクスチャの 33 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field33Id | String | フィールド 33 の識別子 |
| body.field33Name | String | フィールド 33 の名前 |
| body.field33Count | Integer | フィールド 33 の個数 |

セクション 33 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-34"></a>
### 保存セクション 34 { #preserve-sec-34 }

このセクションは preserve-existing e2e フィクスチャの 34 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field34Id | String | フィールド 34 の識別子 |
| body.field34Name | String | フィールド 34 の名前 |
| body.field34Count | Integer | フィールド 34 の個数 |

セクション 34 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-35"></a>
### 保存セクション 35 { #preserve-sec-35 }

このセクションは preserve-existing e2e フィクスチャの 35 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field35Id | String | フィールド 35 の識別子 |
| body.field35Name | String | フィールド 35 の名前 |
| body.field35Count | Integer | フィールド 35 の個数 |

```json
{
  "field35Id": "id-35",
  "field35Name": "name-35"
}
```

セクション 35 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-36"></a>
### 保存セクション 36 { #preserve-sec-36 }

このセクションは preserve-existing e2e フィクスチャの 36 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field36Id | String | フィールド 36 の識別子 |
| body.field36Name | String | フィールド 36 の名前 |
| body.field36Count | Integer | フィールド 36 の個数 |

セクション 36 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-37"></a>
### 保存セクション 37 { #preserve-sec-37 }

このセクションは preserve-existing e2e フィクスチャの 37 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field37Id | String | フィールド 37 の識別子 |
| body.field37Name | String | フィールド 37 の名前 |
| body.field37Count | Integer | フィールド 37 の個数 |

セクション 37 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-38"></a>
### 保存セクション 38 { #preserve-sec-38 }

このセクションは preserve-existing e2e フィクスチャの 38 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field38Id | String | フィールド 38 の識別子 |
| body.field38Name | String | フィールド 38 の名前 |
| body.field38Count | Integer | フィールド 38 の個数 |

セクション 38 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-39"></a>
### 保存セクション 39 { #preserve-sec-39 }

このセクションは preserve-existing e2e フィクスチャの 39 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field39Id | String | フィールド 39 の識別子 |
| body.field39Name | String | フィールド 39 の名前 |
| body.field39Count | Integer | フィールド 39 の個数 |

セクション 39 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-40"></a>
### 保存セクション 40 { #preserve-sec-40 }

このセクションは preserve-existing e2e フィクスチャの 40 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field40Id | String | フィールド 40 の識別子 |
| body.field40Name | String | フィールド 40 の名前 |
| body.field40Count | Integer | フィールド 40 の個数 |

```json
{
  "field40Id": "id-40",
  "field40Name": "name-40"
}
```

セクション 40 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-41"></a>
### 保存セクション 41 { #preserve-sec-41 }

このセクションは preserve-existing e2e フィクスチャの 41 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field41Id | String | フィールド 41 の識別子 |
| body.field41Name | String | フィールド 41 の名前 |
| body.field41Count | Integer | フィールド 41 の個数 |

セクション 41 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-42"></a>
### 保存セクション 42 { #preserve-sec-42 }

このセクションは preserve-existing e2e フィクスチャの 42 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field42Id | String | フィールド 42 の識別子 |
| body.field42Name | String | フィールド 42 の名前 |
| body.field42Count | Integer | フィールド 42 の個数 |

セクション 42 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-43"></a>
### 保存セクション 43 { #preserve-sec-43 }

このセクションは preserve-existing e2e フィクスチャの 43 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field43Id | String | フィールド 43 の識別子 |
| body.field43Name | String | フィールド 43 の名前 |
| body.field43Count | Integer | フィールド 43 の個数 |

セクション 43 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-44"></a>
### 保存セクション 44 { #preserve-sec-44 }

このセクションは preserve-existing e2e フィクスチャの 44 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field44Id | String | フィールド 44 の識別子 |
| body.field44Name | String | フィールド 44 の名前 |
| body.field44Count | Integer | フィールド 44 の個数 |

セクション 44 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-45"></a>
### 保存セクション 45 { #preserve-sec-45 }

このセクションは preserve-existing e2e フィクスチャの 45 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field45Id | String | フィールド 45 の識別子 |
| body.field45Name | String | フィールド 45 の名前 |
| body.field45Count | Integer | フィールド 45 の個数 |

```json
{
  "field45Id": "id-45",
  "field45Name": "name-45"
}
```

セクション 45 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-46"></a>
### 保存セクション 46 { #preserve-sec-46 }

このセクションは preserve-existing e2e フィクスチャの 46 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field46Id | String | フィールド 46 の識別子 |
| body.field46Name | String | フィールド 46 の名前 |
| body.field46Count | Integer | フィールド 46 の個数 |

セクション 46 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-47"></a>
### 保存セクション 47 { #preserve-sec-47 }

このセクションは preserve-existing e2e フィクスチャの 47 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field47Id | String | フィールド 47 の識別子 |
| body.field47Name | String | フィールド 47 の名前 |
| body.field47Count | Integer | フィールド 47 の個数 |

セクション 47 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-48"></a>
### 保存セクション 48 { #preserve-sec-48 }

このセクションは preserve-existing e2e フィクスチャの 48 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field48Id | String | フィールド 48 の識別子 |
| body.field48Name | String | フィールド 48 の名前 |
| body.field48Count | Integer | フィールド 48 の個数 |

セクション 48 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-49"></a>
### 保存セクション 49 { #preserve-sec-49 }

このセクションは preserve-existing e2e フィクスチャの 49 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field49Id | String | フィールド 49 の識別子 |
| body.field49Name | String | フィールド 49 の名前 |
| body.field49Count | Integer | フィールド 49 の個数 |

セクション 49 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-50"></a>
### 保存セクション 50 { #preserve-sec-50 }

このセクションは preserve-existing e2e フィクスチャの 50 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field50Id | String | フィールド 50 の識別子 |
| body.field50Name | String | フィールド 50 の名前 |
| body.field50Count | Integer | フィールド 50 の個数 |

```json
{
  "field50Id": "id-50",
  "field50Name": "name-50"
}
```

セクション 50 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-51"></a>
### 保存セクション 51 { #preserve-sec-51 }

このセクションは preserve-existing e2e フィクスチャの 51 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field51Id | String | フィールド 51 の識別子 |
| body.field51Name | String | フィールド 51 の名前 |
| body.field51Count | Integer | フィールド 51 の個数 |

セクション 51 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-52"></a>
### 保存セクション 52 { #preserve-sec-52 }

このセクションは preserve-existing e2e フィクスチャの 52 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field52Id | String | フィールド 52 の識別子 |
| body.field52Name | String | フィールド 52 の名前 |
| body.field52Count | Integer | フィールド 52 の個数 |

セクション 52 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-53"></a>
### 保存セクション 53 { #preserve-sec-53 }

このセクションは preserve-existing e2e フィクスチャの 53 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field53Id | String | フィールド 53 の識別子 |
| body.field53Name | String | フィールド 53 の名前 |
| body.field53Count | Integer | フィールド 53 の個数 |

セクション 53 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-54"></a>
### 保存セクション 54 { #preserve-sec-54 }

このセクションは preserve-existing e2e フィクスチャの 54 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field54Id | String | フィールド 54 の識別子 |
| body.field54Name | String | フィールド 54 の名前 |
| body.field54Count | Integer | フィールド 54 の個数 |

セクション 54 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-55"></a>
### 保存セクション 55 { #preserve-sec-55 }

このセクションは preserve-existing e2e フィクスチャの 55 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field55Id | String | フィールド 55 の識別子 |
| body.field55Name | String | フィールド 55 の名前 |
| body.field55Count | Integer | フィールド 55 の個数 |

```json
{
  "field55Id": "id-55",
  "field55Name": "name-55"
}
```

セクション 55 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-56"></a>
### 保存セクション 56 { #preserve-sec-56 }

このセクションは preserve-existing e2e フィクスチャの 56 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field56Id | String | フィールド 56 の識別子 |
| body.field56Name | String | フィールド 56 の名前 |
| body.field56Count | Integer | フィールド 56 の個数 |

セクション 56 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-57"></a>
### 保存セクション 57 { #preserve-sec-57 }

このセクションは preserve-existing e2e フィクスチャの 57 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field57Id | String | フィールド 57 の識別子 |
| body.field57Name | String | フィールド 57 の名前 |
| body.field57Count | Integer | フィールド 57 の個数 |

セクション 57 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-58"></a>
### 保存セクション 58 { #preserve-sec-58 }

このセクションは preserve-existing e2e フィクスチャの 58 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field58Id | String | フィールド 58 の識別子 |
| body.field58Name | String | フィールド 58 の名前 |
| body.field58Count | Integer | フィールド 58 の個数 |

セクション 58 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-59"></a>
### 保存セクション 59 { #preserve-sec-59 }

このセクションは preserve-existing e2e フィクスチャの 59 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field59Id | String | フィールド 59 の識別子 |
| body.field59Name | String | フィールド 59 の名前 |
| body.field59Count | Integer | フィールド 59 の個数 |

セクション 59 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-60"></a>
### 保存セクション 60 { #preserve-sec-60 }

このセクションは preserve-existing e2e フィクスチャの 60 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field60Id | String | フィールド 60 の識別子 |
| body.field60Name | String | フィールド 60 の名前 |
| body.field60Count | Integer | フィールド 60 の個数 |

```json
{
  "field60Id": "id-60",
  "field60Name": "name-60"
}
```

セクション 60 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-61"></a>
### 保存セクション 61 { #preserve-sec-61 }

このセクションは preserve-existing e2e フィクスチャの 61 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field61Id | String | フィールド 61 の識別子 |
| body.field61Name | String | フィールド 61 の名前 |
| body.field61Count | Integer | フィールド 61 の個数 |

セクション 61 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-62"></a>
### 保存セクション 62 { #preserve-sec-62 }

このセクションは preserve-existing e2e フィクスチャの 62 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field62Id | String | フィールド 62 の識別子 |
| body.field62Name | String | フィールド 62 の名前 |
| body.field62Count | Integer | フィールド 62 の個数 |

セクション 62 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-63"></a>
### 保存セクション 63 { #preserve-sec-63 }

このセクションは preserve-existing e2e フィクスチャの 63 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field63Id | String | フィールド 63 の識別子 |
| body.field63Name | String | フィールド 63 の名前 |
| body.field63Count | Integer | フィールド 63 の個数 |

セクション 63 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-64"></a>
### 保存セクション 64 { #preserve-sec-64 }

このセクションは preserve-existing e2e フィクスチャの 64 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field64Id | String | フィールド 64 の識別子 |
| body.field64Name | String | フィールド 64 の名前 |
| body.field64Count | Integer | フィールド 64 の個数 |

セクション 64 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-65"></a>
### 保存セクション 65 { #preserve-sec-65 }

このセクションは preserve-existing e2e フィクスチャの 65 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field65Id | String | フィールド 65 の識別子 |
| body.field65Name | String | フィールド 65 の名前 |
| body.field65Count | Integer | フィールド 65 の個数 |

```json
{
  "field65Id": "id-65",
  "field65Name": "name-65"
}
```

セクション 65 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-66"></a>
### 保存セクション 66 { #preserve-sec-66 }

このセクションは preserve-existing e2e フィクスチャの 66 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field66Id | String | フィールド 66 の識別子 |
| body.field66Name | String | フィールド 66 の名前 |
| body.field66Count | Integer | フィールド 66 の個数 |

セクション 66 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-67"></a>
### 保存セクション 67 { #preserve-sec-67 }

このセクションは preserve-existing e2e フィクスチャの 67 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field67Id | String | フィールド 67 の識別子 |
| body.field67Name | String | フィールド 67 の名前 |
| body.field67Count | Integer | フィールド 67 の個数 |

セクション 67 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-68"></a>
### 保存セクション 68 { #preserve-sec-68 }

このセクションは preserve-existing e2e フィクスチャの 68 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field68Id | String | フィールド 68 の識別子 |
| body.field68Name | String | フィールド 68 の名前 |
| body.field68Count | Integer | フィールド 68 の個数 |

セクション 68 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-69"></a>
### 保存セクション 69 { #preserve-sec-69 }

このセクションは preserve-existing e2e フィクスチャの 69 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field69Id | String | フィールド 69 の識別子 |
| body.field69Name | String | フィールド 69 の名前 |
| body.field69Count | Integer | フィールド 69 の個数 |

セクション 69 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-70"></a>
### 保存セクション 70 { #preserve-sec-70 }

このセクションは preserve-existing e2e フィクスチャの 70 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field70Id | String | フィールド 70 の識別子 |
| body.field70Name | String | フィールド 70 の名前 |
| body.field70Count | Integer | フィールド 70 の個数 |

```json
{
  "field70Id": "id-70",
  "field70Name": "name-70"
}
```

セクション 70 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-71"></a>
### 保存セクション 71 { #preserve-sec-71 }

このセクションは preserve-existing e2e フィクスチャの 71 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field71Id | String | フィールド 71 の識別子 |
| body.field71Name | String | フィールド 71 の名前 |
| body.field71Count | Integer | フィールド 71 の個数 |

セクション 71 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-72"></a>
### 保存セクション 72 { #preserve-sec-72 }

このセクションは preserve-existing e2e フィクスチャの 72 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field72Id | String | フィールド 72 の識別子 |
| body.field72Name | String | フィールド 72 の名前 |
| body.field72Count | Integer | フィールド 72 の個数 |

セクション 72 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-73"></a>
### 保存セクション 73 { #preserve-sec-73 }

このセクションは preserve-existing e2e フィクスチャの 73 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field73Id | String | フィールド 73 の識別子 |
| body.field73Name | String | フィールド 73 の名前 |
| body.field73Count | Integer | フィールド 73 の個数 |

セクション 73 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-74"></a>
### 保存セクション 74 { #preserve-sec-74 }

このセクションは preserve-existing e2e フィクスチャの 74 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field74Id | String | フィールド 74 の識別子 |
| body.field74Name | String | フィールド 74 の名前 |
| body.field74Count | Integer | フィールド 74 の個数 |

セクション 74 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-75"></a>
### 保存セクション 75 { #preserve-sec-75 }

このセクションは preserve-existing e2e フィクスチャの 75 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field75Id | String | フィールド 75 の識別子 |
| body.field75Name | String | フィールド 75 の名前 |
| body.field75Count | Integer | フィールド 75 の個数 |

```json
{
  "field75Id": "id-75",
  "field75Name": "name-75"
}
```

セクション 75 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-76"></a>
### 保存セクション 76 { #preserve-sec-76 }

このセクションは preserve-existing e2e フィクスチャの 76 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field76Id | String | フィールド 76 の識別子 |
| body.field76Name | String | フィールド 76 の名前 |
| body.field76Count | Integer | フィールド 76 の個数 |

セクション 76 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-77"></a>
### 保存セクション 77 { #preserve-sec-77 }

このセクションは preserve-existing e2e フィクスチャの 77 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field77Id | String | フィールド 77 の識別子 |
| body.field77Name | String | フィールド 77 の名前 |
| body.field77Count | Integer | フィールド 77 の個数 |

セクション 77 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-78"></a>
### 保存セクション 78 { #preserve-sec-78 }

このセクションは preserve-existing e2e フィクスチャの 78 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field78Id | String | フィールド 78 の識別子 |
| body.field78Name | String | フィールド 78 の名前 |
| body.field78Count | Integer | フィールド 78 の個数 |

セクション 78 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-79"></a>
### 保存セクション 79 { #preserve-sec-79 }

このセクションは preserve-existing e2e フィクスチャの 79 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field79Id | String | フィールド 79 の識別子 |
| body.field79Name | String | フィールド 79 の名前 |
| body.field79Count | Integer | フィールド 79 の個数 |

セクション 79 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-80"></a>
### 保存セクション 80 { #preserve-sec-80 }

このセクションは preserve-existing e2e フィクスチャの 80 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field80Id | String | フィールド 80 の識別子 |
| body.field80Name | String | フィールド 80 の名前 |
| body.field80Count | Integer | フィールド 80 の個数 |

```json
{
  "field80Id": "id-80",
  "field80Name": "name-80"
}
```

セクション 80 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-81"></a>
### 保存セクション 81 { #preserve-sec-81 }

このセクションは preserve-existing e2e フィクスチャの 81 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field81Id | String | フィールド 81 の識別子 |
| body.field81Name | String | フィールド 81 の名前 |
| body.field81Count | Integer | フィールド 81 の個数 |

セクション 81 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-82"></a>
### 保存セクション 82 { #preserve-sec-82 }

このセクションは preserve-existing e2e フィクスチャの 82 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field82Id | String | フィールド 82 の識別子 |
| body.field82Name | String | フィールド 82 の名前 |
| body.field82Count | Integer | フィールド 82 の個数 |

セクション 82 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-83"></a>
### 保存セクション 83 { #preserve-sec-83 }

このセクションは preserve-existing e2e フィクスチャの 83 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field83Id | String | フィールド 83 の識別子 |
| body.field83Name | String | フィールド 83 の名前 |
| body.field83Count | Integer | フィールド 83 の個数 |

セクション 83 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-84"></a>
### 保存セクション 84 { #preserve-sec-84 }

このセクションは preserve-existing e2e フィクスチャの 84 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field84Id | String | フィールド 84 の識別子 |
| body.field84Name | String | フィールド 84 の名前 |
| body.field84Count | Integer | フィールド 84 の個数 |

セクション 84 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-85"></a>
### 保存セクション 85 { #preserve-sec-85 }

このセクションは preserve-existing e2e フィクスチャの 85 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field85Id | String | フィールド 85 の識別子 |
| body.field85Name | String | フィールド 85 の名前 |
| body.field85Count | Integer | フィールド 85 の個数 |

```json
{
  "field85Id": "id-85",
  "field85Name": "name-85"
}
```

セクション 85 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-86"></a>
### 保存セクション 86 { #preserve-sec-86 }

このセクションは preserve-existing e2e フィクスチャの 86 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field86Id | String | フィールド 86 の識別子 |
| body.field86Name | String | フィールド 86 の名前 |
| body.field86Count | Integer | フィールド 86 の個数 |

セクション 86 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-87"></a>
### 保存セクション 87 { #preserve-sec-87 }

このセクションは preserve-existing e2e フィクスチャの 87 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field87Id | String | フィールド 87 の識別子 |
| body.field87Name | String | フィールド 87 の名前 |
| body.field87Count | Integer | フィールド 87 の個数 |

セクション 87 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-88"></a>
### 保存セクション 88 { #preserve-sec-88 }

このセクションは preserve-existing e2e フィクスチャの 88 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field88Id | String | フィールド 88 の識別子 |
| body.field88Name | String | フィールド 88 の名前 |
| body.field88Count | Integer | フィールド 88 の個数 |

セクション 88 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-89"></a>
### 保存セクション 89 { #preserve-sec-89 }

このセクションは preserve-existing e2e フィクスチャの 89 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field89Id | String | フィールド 89 の識別子 |
| body.field89Name | String | フィールド 89 の名前 |
| body.field89Count | Integer | フィールド 89 の個数 |

セクション 89 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-90"></a>
### 保存セクション 90 { #preserve-sec-90 }

このセクションは preserve-existing e2e フィクスチャの 90 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field90Id | String | フィールド 90 の識別子 |
| body.field90Name | String | フィールド 90 の名前 |
| body.field90Count | Integer | フィールド 90 の個数 |

```json
{
  "field90Id": "id-90",
  "field90Name": "name-90"
}
```

セクション 90 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-91"></a>
### 保存セクション 91 { #preserve-sec-91 }

このセクションは preserve-existing e2e フィクスチャの 91 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field91Id | String | フィールド 91 の識別子 |
| body.field91Name | String | フィールド 91 の名前 |
| body.field91Count | Integer | フィールド 91 の個数 |

セクション 91 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-92"></a>
### 保存セクション 92 { #preserve-sec-92 }

このセクションは preserve-existing e2e フィクスチャの 92 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field92Id | String | フィールド 92 の識別子 |
| body.field92Name | String | フィールド 92 の名前 |
| body.field92Count | Integer | フィールド 92 の個数 |

セクション 92 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-93"></a>
### 保存セクション 93 { #preserve-sec-93 }

このセクションは preserve-existing e2e フィクスチャの 93 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field93Id | String | フィールド 93 の識別子 |
| body.field93Name | String | フィールド 93 の名前 |
| body.field93Count | Integer | フィールド 93 の個数 |

セクション 93 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-94"></a>
### 保存セクション 94 { #preserve-sec-94 }

このセクションは preserve-existing e2e フィクスチャの 94 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field94Id | String | フィールド 94 の識別子 |
| body.field94Name | String | フィールド 94 の名前 |
| body.field94Count | Integer | フィールド 94 の個数 |

セクション 94 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-95"></a>
### 保存セクション 95 { #preserve-sec-95 }

このセクションは preserve-existing e2e フィクスチャの 95 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field95Id | String | フィールド 95 の識別子 |
| body.field95Name | String | フィールド 95 の名前 |
| body.field95Count | Integer | フィールド 95 の個数 |

```json
{
  "field95Id": "id-95",
  "field95Name": "name-95"
}
```

セクション 95 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-96"></a>
### 保存セクション 96 { #preserve-sec-96 }

このセクションは preserve-existing e2e フィクスチャの 96 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field96Id | String | フィールド 96 の識別子 |
| body.field96Name | String | フィールド 96 の名前 |
| body.field96Count | Integer | フィールド 96 の個数 |

セクション 96 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-97"></a>
### 保存セクション 97 { #preserve-sec-97 }

このセクションは preserve-existing e2e フィクスチャの 97 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field97Id | String | フィールド 97 の識別子 |
| body.field97Name | String | フィールド 97 の名前 |
| body.field97Count | Integer | フィールド 97 の個数 |

セクション 97 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-98"></a>
### 保存セクション 98 { #preserve-sec-98 }

このセクションは preserve-existing e2e フィクスチャの 98 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field98Id | String | フィールド 98 の識別子 |
| body.field98Name | String | フィールド 98 の名前 |
| body.field98Count | Integer | フィールド 98 の個数 |

セクション 98 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-99"></a>
### 保存セクション 99 { #preserve-sec-99 }

このセクションは preserve-existing e2e フィクスチャの 99 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field99Id | String | フィールド 99 の識別子 |
| body.field99Name | String | フィールド 99 の名前 |
| body.field99Count | Integer | フィールド 99 の個数 |

セクション 99 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-100"></a>
### 保存セクション 100 { #preserve-sec-100 }

このセクションは preserve-existing e2e フィクスチャの 100 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field100Id | String | フィールド 100 の識別子 |
| body.field100Name | String | フィールド 100 の名前 |
| body.field100Count | Integer | フィールド 100 の個数 |

```json
{
  "field100Id": "id-100",
  "field100Name": "name-100"
}
```

セクション 100 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-101"></a>
### 保存セクション 101 { #preserve-sec-101 }

このセクションは preserve-existing e2e フィクスチャの 101 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field101Id | String | フィールド 101 の識別子 |
| body.field101Name | String | フィールド 101 の名前 |
| body.field101Count | Integer | フィールド 101 の個数 |

セクション 101 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-102"></a>
### 保存セクション 102 { #preserve-sec-102 }

このセクションは preserve-existing e2e フィクスチャの 102 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field102Id | String | フィールド 102 の識別子 |
| body.field102Name | String | フィールド 102 の名前 |
| body.field102Count | Integer | フィールド 102 の個数 |

セクション 102 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-103"></a>
### 保存セクション 103 { #preserve-sec-103 }

このセクションは preserve-existing e2e フィクスチャの 103 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field103Id | String | フィールド 103 の識別子 |
| body.field103Name | String | フィールド 103 の名前 |
| body.field103Count | Integer | フィールド 103 の個数 |

セクション 103 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-104"></a>
### 保存セクション 104 { #preserve-sec-104 }

このセクションは preserve-existing e2e フィクスチャの 104 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field104Id | String | フィールド 104 の識別子 |
| body.field104Name | String | フィールド 104 の名前 |
| body.field104Count | Integer | フィールド 104 の個数 |

104番 セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-105"></a>
### 保存セクション 105 { #preserve-sec-105 }

このセクションは preserve-existing e2e フィクスチャの 105 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field105Id | String | フィールド 105 の識別子 |
| body.field105Name | String | フィールド 105 の名前 |
| body.field105Count | Integer | フィールド 105 の個数 |

```json
{
  "field105Id": "id-105",
  "field105Name": "name-105"
}
```

セクション 105 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-106"></a>
### 保存セクション 106 { #preserve-sec-106 }

このセクションは preserve-existing e2e フィクスチャの 106 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field106Id | String | フィールド 106 の識別子 |
| body.field106Name | String | フィールド 106 の名前 |
| body.field106Count | Integer | フィールド 106 の個数 |

セクション 106 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-107"></a>
### 保存セクション 107 { #preserve-sec-107 }

このセクションは preserve-existing e2e フィクスチャの 107 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field107Id | String | フィールド 107 の識別子 |
| body.field107Name | String | フィールド 107 の名前 |
| body.field107Count | Integer | フィールド 107 の個数 |

セクション 107 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-108"></a>
### 保存セクション 108 { #preserve-sec-108 }

このセクションは preserve-existing e2e フィクスチャの 108 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field108Id | String | フィールド 108 の識別子 |
| body.field108Name | String | フィールド 108 の名前 |
| body.field108Count | Integer | フィールド 108 の個数 |

セクション 108 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-109"></a>
### 保存セクション 109 { #preserve-sec-109 }

このセクションは preserve-existing e2e フィクスチャの 109 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field109Id | String | フィールド 109 の識別子 |
| body.field109Name | String | フィールド 109 の名前 |
| body.field109Count | Integer | フィールド 109 の個数 |

セクション 109 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-110"></a>
### 保存セクション 110 { #preserve-sec-110 }

このセクションは preserve-existing e2e フィクスチャの 110 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field110Id | String | フィールド 110 の識別子 |
| body.field110Name | String | フィールド 110 の名前 |
| body.field110Count | Integer | フィールド 110 の個数 |

```json
{
  "field110Id": "id-110",
  "field110Name": "name-110"
}
```

セクション 110 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-111"></a>
### 保存セクション 111 { #preserve-sec-111 }

このセクションは preserve-existing e2e フィクスチャの 111 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field111Id | String | フィールド 111 の識別子 |
| body.field111Name | String | フィールド 111 の名前 |
| body.field111Count | Integer | フィールド 111 の個数 |

セクション 111 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-112"></a>
### 保存セクション 112 { #preserve-sec-112 }

このセクションは preserve-existing e2e フィクスチャの 112 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field112Id | String | フィールド 112 の識別子 |
| body.field112Name | String | フィールド 112 の名前 |
| body.field112Count | Integer | フィールド 112 の個数 |

セクション 112 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-113"></a>
### 保存セクション 113 { #preserve-sec-113 }

このセクションは preserve-existing e2e フィクスチャの 113 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field113Id | String | フィールド 113 の識別子 |
| body.field113Name | String | フィールド 113 の名前 |
| body.field113Count | Integer | フィールド 113 の個数 |

セクション 113 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-114"></a>
### 保存セクション 114 { #preserve-sec-114 }

このセクションは preserve-existing e2e フィクスチャの 114 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field114Id | String | フィールド 114 の識別子 |
| body.field114Name | String | フィールド 114 の名前 |
| body.field114Count | Integer | フィールド 114 の個数 |

セクション 114 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-115"></a>
### 保存セクション 115 { #preserve-sec-115 }

このセクションは preserve-existing e2e フィクスチャの 115 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field115Id | String | フィールド 115 の識別子 |
| body.field115Name | String | フィールド 115 の名前 |
| body.field115Count | Integer | フィールド 115 の個数 |

```json
{
  "field115Id": "id-115",
  "field115Name": "name-115"
}
```

セクション 115 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-116"></a>
### 保存セクション 116 { #preserve-sec-116 }

このセクションは preserve-existing e2e フィクスチャの 116 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field116Id | String | フィールド 116 の識別子 |
| body.field116Name | String | フィールド 116 の名前 |
| body.field116Count | Integer | フィールド 116 の個数 |

セクション 116 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-117"></a>
### 保存セクション 117 { #preserve-sec-117 }

このセクションは preserve-existing e2e フィクスチャの 117 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field117Id | String | フィールド 117 の識別子 |
| body.field117Name | String | フィールド 117 の名前 |
| body.field117Count | Integer | フィールド 117 の個数 |

セクション 117 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-118"></a>
### 保存セクション 118 { #preserve-sec-118 }

このセクションは preserve-existing e2e フィクスチャの 118 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field118Id | String | フィールド 118 の識別子 |
| body.field118Name | String | フィールド 118 の名前 |
| body.field118Count | Integer | フィールド 118 の個数 |

セクション 118 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-119"></a>
### 保存セクション 119 { #preserve-sec-119 }

このセクションは preserve-existing e2e フィクスチャの 119 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field119Id | String | フィールド 119 の識別子 |
| body.field119Name | String | フィールド 119 の名前 |
| body.field119Count | Integer | フィールド 119 の個数 |

セクション 119 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-120"></a>
### 保存セクション 120 { #preserve-sec-120 }

このセクションは preserve-existing e2e フィクスチャの 120 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field120Id | String | フィールド 120 の識別子 |
| body.field120Name | String | フィールド 120 の名前 |
| body.field120Count | Integer | フィールド 120 の個数 |

```json
{
  "field120Id": "id-120",
  "field120Name": "name-120"
}
```

セクション 120 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-121"></a>
### 保存セクション 121 { #preserve-sec-121 }

このセクションは preserve-existing e2e フィクスチャの 121 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field121Id | String | フィールド 121 の識別子 |
| body.field121Name | String | フィールド 121 の名前 |
| body.field121Count | Integer | フィールド 121 の個数 |

セクション 121 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-122"></a>
### 保存セクション 122 { #preserve-sec-122 }

このセクションは preserve-existing e2e フィクスチャの 122 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field122Id | String | フィールド 122 の識別子 |
| body.field122Name | String | フィールド 122 の名前 |
| body.field122Count | Integer | フィールド 122 の個数 |

セクション 122 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-123"></a>
### 保存セクション 123 { #preserve-sec-123 }

このセクションは preserve-existing e2e フィクスチャの 123 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field123Id | String | フィールド 123 の識別子 |
| body.field123Name | String | フィールド 123 の名前 |
| body.field123Count | Integer | フィールド 123 の個数 |

セクション 123 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-124"></a>
### 保存セクション 124 { #preserve-sec-124 }

このセクションは preserve-existing e2e フィクスチャの 124 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field124Id | String | フィールド 124 の識別子 |
| body.field124Name | String | フィールド 124 の名前 |
| body.field124Count | Integer | フィールド 124 の個数 |

セクション 124 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-125"></a>
### 保存セクション 125 { #preserve-sec-125 }

このセクションは preserve-existing e2e フィクスチャの 125 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field125Id | String | フィールド 125 の識別子 |
| body.field125Name | String | フィールド 125 の名前 |
| body.field125Count | Integer | フィールド 125 の個数 |

```json
{
  "field125Id": "id-125",
  "field125Name": "name-125"
}
```

セクション 125 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-126"></a>
### 保存セクション 126 { #preserve-sec-126 }

このセクションは preserve-existing e2e フィクスチャの 126 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field126Id | String | フィールド 126 の識別子 |
| body.field126Name | String | フィールド 126 の名前 |
| body.field126Count | Integer | フィールド 126 の個数 |

セクション 126 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-127"></a>
### 保存セクション 127 { #preserve-sec-127 }

このセクションは preserve-existing e2e フィクスチャの 127 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field127Id | String | フィールド 127 の識別子 |
| body.field127Name | String | フィールド 127 の名前 |
| body.field127Count | Integer | フィールド 127 の個数 |

セクション 127 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-128"></a>
### 保存セクション 128 { #preserve-sec-128 }

このセクションは preserve-existing e2e フィクスチャの 128 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field128Id | String | フィールド 128 の識別子 |
| body.field128Name | String | フィールド 128 の名前 |
| body.field128Count | Integer | フィールド 128 の個数 |

セクション 128 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-129"></a>
### 保存セクション 129 { #preserve-sec-129 }

このセクションは preserve-existing e2e フィクスチャの 129 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field129Id | String | フィールド 129 の識別子 |
| body.field129Name | String | フィールド 129 の名前 |
| body.field129Count | Integer | フィールド 129 の個数 |

セクション 129 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために使われます。

<a id="preserve-sec-130"></a>
### 保存セクション 130 { #preserve-sec-130 }

このセクションは preserve-existing e2e フィクスチャの 130 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field130Id | String | フィールド 130 の識別子 |
| body.field130Name | String | フィールド 130 の名前 |
| body.field130Count | Integer | フィールド 130 の個数 |

```json
{
  "field130Id": "id-130",
  "field130Name": "name-130"
}
```

セクション 130 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-131"></a>
### 保存セクション 131 { #preserve-sec-131 }

このセクションは preserve-existing e2e フィクスチャの 131 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field131Id | String | フィールド 131 の識別子 |
| body.field131Name | String | フィールド 131 の名前 |
| body.field131Count | Integer | フィールド 131 の個数 |

セクション 131 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-132"></a>
### 保存セクション 132 { #preserve-sec-132 }

このセクションは preserve-existing e2e フィクスチャの 132 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field132Id | String | フィールド 132 の識別子 |
| body.field132Name | String | フィールド 132 の名前 |
| body.field132Count | Integer | フィールド 132 の個数 |

セクション 132 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-133"></a>
### 保存セクション 133 { #preserve-sec-133 }

このセクションは preserve-existing e2e フィクスチャの 133 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field133Id | String | フィールド 133 の識別子 |
| body.field133Name | String | フィールド 133 の名前 |
| body.field133Count | Integer | フィールド 133 の個数 |

セクション 133 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-134"></a>
### 保存セクション 134 { #preserve-sec-134 }

このセクションは preserve-existing e2e フィクスチャの 134 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field134Id | String | フィールド 134 の識別子 |
| body.field134Name | String | フィールド 134 の名前 |
| body.field134Count | Integer | フィールド 134 の個数 |

セクション 134 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-135"></a>
### 保存セクション 135 { #preserve-sec-135 }

このセクションは preserve-existing e2e フィクスチャの 135 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field135Id | String | フィールド 135 の識別子 |
| body.field135Name | String | フィールド 135 の名前 |
| body.field135Count | Integer | フィールド 135 の個数 |

```json
{
  "field135Id": "id-135",
  "field135Name": "name-135"
}
```

セクション 135 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-136"></a>
### 保存セクション 136 { #preserve-sec-136 }

このセクションは preserve-existing e2e フィクスチャの 136 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field136Id | String | フィールド 136 の識別子 |
| body.field136Name | String | フィールド 136 の名前 |
| body.field136Count | Integer | フィールド 136 の個数 |

セクション 136 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-137"></a>
### 保存セクション 137 { #preserve-sec-137 }

このセクションは preserve-existing e2e フィクスチャの 137 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field137Id | String | フィールド 137 の識別子 |
| body.field137Name | String | フィールド 137 の名前 |
| body.field137Count | Integer | フィールド 137 の個数 |

セクション 137 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-138"></a>
### 保存セクション 138 { #preserve-sec-138 }

このセクションは preserve-existing e2e フィクスチャの 138 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field138Id | String | フィールド 138 の識別子 |
| body.field138Name | String | フィールド 138 の名前 |
| body.field138Count | Integer | フィールド 138 の個数 |

セクション 138 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-139"></a>
### 保存セクション 139 { #preserve-sec-139 }

このセクションは preserve-existing e2e フィクスチャの 139 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field139Id | String | フィールド 139 の識別子 |
| body.field139Name | String | フィールド 139 の名前 |
| body.field139Count | Integer | フィールド 139 の個数 |

セクション 139 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-140"></a>
### 保存セクション 140 { #preserve-sec-140 }

このセクションは preserve-existing e2e フィクスチャの 140 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field140Id | String | フィールド 140 の識別子 |
| body.field140Name | String | フィールド 140 の名前 |
| body.field140Count | Integer | フィールド 140 の個数 |

```json
{
  "field140Id": "id-140",
  "field140Name": "name-140"
}
```

セクション 140 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-141"></a>
### 保存セクション 141 { #preserve-sec-141 }

このセクションは preserve-existing e2e フィクスチャの 141 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field141Id | String | フィールド 141 の識別子 |
| body.field141Name | String | フィールド 141 の名前 |
| body.field141Count | Integer | フィールド 141 の個数 |

セクション 141 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-142"></a>
### 保存セクション 142 { #preserve-sec-142 }

このセクションは preserve-existing e2e フィクスチャの 142 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field142Id | String | フィールド 142 の識別子 |
| body.field142Name | String | フィールド 142 の名前 |
| body.field142Count | Integer | フィールド 142 の個数 |

セクション 142 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-143"></a>
### 保存セクション 143 { #preserve-sec-143 }

このセクションは preserve-existing e2e フィクスチャの 143 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field143Id | String | フィールド 143 の識別子 |
| body.field143Name | String | フィールド 143 の名前 |
| body.field143Count | Integer | フィールド 143 の個数 |

セクション 143 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-144"></a>
### 保存セクション 144 { #preserve-sec-144 }

このセクションは preserve-existing e2e フィクスチャの 144 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field144Id | String | フィールド 144 の識別子 |
| body.field144Name | String | フィールド 144 の名前 |
| body.field144Count | Integer | フィールド 144 の個数 |

セクション 144 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-145"></a>
### 保存セクション 145 { #preserve-sec-145 }

このセクションは preserve-existing e2e フィクスチャの 145 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field145Id | String | フィールド 145 の識別子 |
| body.field145Name | String | フィールド 145 の名前 |
| body.field145Count | Integer | フィールド 145 の個数 |

```json
{
  "field145Id": "id-145",
  "field145Name": "name-145"
}
```

セクション 145 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-146"></a>
### 保存セクション 146 { #preserve-sec-146 }

このセクションは preserve-existing e2e フィクスチャの 146 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field146Id | String | フィールド 146 の識別子 |
| body.field146Name | String | フィールド 146 の名前 |
| body.field146Count | Integer | フィールド 146 の個数 |

セクション 146 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-147"></a>
### 保存セクション 147 { #preserve-sec-147 }

このセクションは preserve-existing e2e フィクスチャの 147 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field147Id | String | フィールド 147 の識別子 |
| body.field147Name | String | フィールド 147 の名前 |
| body.field147Count | Integer | フィールド 147 の個数 |

セクション 147 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-148"></a>
### 保存セクション 148 { #preserve-sec-148 }

このセクションは preserve-existing e2e フィクスチャの 148 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field148Id | String | フィールド 148 の識別子 |
| body.field148Name | String | フィールド 148 の名前 |
| body.field148Count | Integer | フィールド 148 の個数 |

セクション 148 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-149"></a>
### 保存セクション 149 { #preserve-sec-149 }

このセクションは preserve-existing e2e フィクスチャの 149 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field149Id | String | フィールド 149 の識別子 |
| body.field149Name | String | フィールド 149 の名前 |
| body.field149Count | Integer | フィールド 149 の個数 |

セクション 149 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-150"></a>
### 保存セクション 150 { #preserve-sec-150 }

このセクションは preserve-existing e2e フィクスチャの 150 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field150Id | String | フィールド 150 の識別子 |
| body.field150Name | String | フィールド 150 の名前 |
| body.field150Count | Integer | フィールド 150 の個数 |

```json
{
  "field150Id": "id-150",
  "field150Name": "name-150"
}
```

セクション 150 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-151"></a>
### 保存セクション 151 { #preserve-sec-151 }

このセクションは preserve-existing e2e フィクスチャの 151 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field151Id | String | フィールド 151 の識別子 |
| body.field151Name | String | フィールド 151 の名前 |
| body.field151Count | Integer | フィールド 151 の個数 |

セクション 151 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-152"></a>
### 保存セクション 152 { #preserve-sec-152 }

このセクションは preserve-existing e2e フィクスチャの 152 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field152Id | String | フィールド 152 の識別子 |
| body.field152Name | String | フィールド 152 の名前 |
| body.field152Count | Integer | フィールド 152 の個数 |

セクション 152 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-153"></a>
### 保存セクション 153 { #preserve-sec-153 }

このセクションは preserve-existing e2e フィクスチャの 153 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field153Id | String | フィールド 153 の識別子 |
| body.field153Name | String | フィールド 153 の名前 |
| body.field153Count | Integer | フィールド 153 の個数 |

セクション 153 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-154"></a>
### 保存セクション 154 { #preserve-sec-154 }

このセクションは preserve-existing e2e フィクスチャの 154 番目のセクションです。以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field154Id | String | フィールド 154 の識別子 |
| body.field154Name | String | フィールド 154 の名前 |
| body.field154Count | Integer | フィールド 154 の個数 |

セクション 154 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-155"></a>
### 保存セクション 155 { #preserve-sec-155 }

このセクションは preserve-existing e2e フィクスチャの 155 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field155Id | String | フィールド 155 の識別子 |
| body.field155Name | String | フィールド 155 の名前 |
| body.field155Count | Integer | フィールド 155 の個数 |

```json
{
  "field155Id": "id-155",
  "field155Name": "name-155"
}
```

セクション 155 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-156"></a>
### 保存セクション 156 { #preserve-sec-156 }

このセクションは preserve-existing e2e フィクスチャの 156 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field156Id | String | フィールド 156 の識別子 |
| body.field156Name | String | フィールド 156 の名前 |
| body.field156Count | Integer | フィールド 156 の個数 |

セクション 156 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-157"></a>
### 保存セクション 157 { #preserve-sec-157 }

このセクションは preserve-existing e2e フィクスチャの 157 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field157Id | String | フィールド 157 の識別子 |
| body.field157Name | String | フィールド 157 の名前 |
| body.field157Count | Integer | フィールド 157 の個数 |

セクション 157 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-158"></a>
### 保存セクション 158 { #preserve-sec-158 }

このセクションは preserve-existing e2e フィクスチャの 158 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field158Id | String | フィールド 158 の識別子 |
| body.field158Name | String | フィールド 158 の名前 |
| body.field158Count | Integer | フィールド 158 の個数 |

セクション 158 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-159"></a>
### 保存セクション 159 { #preserve-sec-159 }

このセクションは preserve-existing e2e フィクスチャの 159 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field159Id | String | フィールド 159 の識別子 |
| body.field159Name | String | フィールド 159 の名前 |
| body.field159Count | Integer | フィールド 159 の個数 |

セクション 159 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-160"></a>
### 保存セクション 160 { #preserve-sec-160 }

このセクションは preserve-existing e2e フィクスチャの 160 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field160Id | String | フィールド 160 の識別子 |
| body.field160Name | String | フィールド 160 の名前 |
| body.field160Count | Integer | フィールド 160 の個数 |

```json
{
  "field160Id": "id-160",
  "field160Name": "name-160"
}
```

セクション 160 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-161"></a>
### 保存セクション 161 { #preserve-sec-161 }

このセクションは preserve-existing e2e フィクスチャの 161 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field161Id | String | フィールド 161 の識別子 |
| body.field161Name | String | フィールド 161 の名前 |
| body.field161Count | Integer | フィールド 161 の個数 |

セクション 161 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-162"></a>
### 保存セクション 162 { #preserve-sec-162 }

このセクションは preserve-existing e2e フィクスチャの 162 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field162Id | String | フィールド 162 の識別子 |
| body.field162Name | String | フィールド 162 の名前 |
| body.field162Count | Integer | フィールド 162 の個数 |

セクション 162 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-163"></a>
### 保存セクション 163 { #preserve-sec-163 }

このセクションは preserve-existing e2e フィクスチャの 163 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field163Id | String | フィールド 163 の識別子 |
| body.field163Name | String | フィールド 163 の名前 |
| body.field163Count | Integer | フィールド 163 の個数 |

セクション 163 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-164"></a>
### 保存セクション 164 { #preserve-sec-164 }

このセクションは preserve-existing e2e フィクスチャの 164 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field164Id | String | フィールド 164 の識別子 |
| body.field164Name | String | フィールド 164 の名前 |
| body.field164Count | Integer | フィールド 164 の個数 |

セクション 164 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-165"></a>
### 保存セクション 165 { #preserve-sec-165 }

このセクションは preserve-existing e2e フィクスチャの 165 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field165Id | String | フィールド 165 の識別子 |
| body.field165Name | String | フィールド 165 の名前 |
| body.field165Count | Integer | フィールド 165 の個数 |

```json
{
  "field165Id": "id-165",
  "field165Name": "name-165"
}
```

セクション 165 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-166"></a>
### 保存セクション 166 { #preserve-sec-166 }

このセクションは preserve-existing e2e フィクスチャの 166 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field166Id | String | フィールド 166 の識別子 |
| body.field166Name | String | フィールド 166 の名前 |
| body.field166Count | Integer | フィールド 166 の個数 |

セクション 166 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-167"></a>
### 保存セクション 167 { #preserve-sec-167 }

このセクションは preserve-existing e2e フィクスチャの 167 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field167Id | String | フィールド 167 の識別子 |
| body.field167Name | String | フィールド 167 の名前 |
| body.field167Count | Integer | フィールド 167 の個数 |

セクション 167 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-168"></a>
### 保存セクション 168 { #preserve-sec-168 }

このセクションは preserve-existing e2e フィクスチャの 168 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field168Id | String | フィールド 168 の識別子 |
| body.field168Name | String | フィールド 168 の名前 |
| body.field168Count | Integer | フィールド 168 の個数 |

セクション 168 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-169"></a>
### 保存セクション 169 { #preserve-sec-169 }

このセクションは preserve-existing e2e フィクスチャの 169 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field169Id | String | フィールド 169 の識別子 |
| body.field169Name | String | フィールド 169 の名前 |
| body.field169Count | Integer | フィールド 169 の個数 |

セクション 169 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-170"></a>
### 保存セクション 170 { #preserve-sec-170 }

このセクションは preserve-existing e2e フィクスチャの 170 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field170Id | String | フィールド 170 の識別子 |
| body.field170Name | String | フィールド 170 の名前 |
| body.field170Count | Integer | フィールド 170 の個数 |

```json
{
  "field170Id": "id-170",
  "field170Name": "name-170"
}
```

セクション 170 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-171"></a>
### 保存セクション 171 { #preserve-sec-171 }

このセクションは preserve-existing e2e フィクスチャの 171 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field171Id | String | フィールド 171 の識別子 |
| body.field171Name | String | フィールド 171 の名前 |
| body.field171Count | Integer | フィールド 171 の個数 |

セクション 171 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-172"></a>
### 保存セクション 172 { #preserve-sec-172 }

このセクションは preserve-existing e2e フィクスチャの 172 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field172Id | String | フィールド 172 の識別子 |
| body.field172Name | String | フィールド 172 の名前 |
| body.field172Count | Integer | フィールド 172 の個数 |

セクション 172 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-173"></a>
### 保存セクション 173 { #preserve-sec-173 }

このセクションは preserve-existing e2e フィクスチャの 173 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field173Id | String | フィールド 173 の識別子 |
| body.field173Name | String | フィールド 173 の名前 |
| body.field173Count | Integer | フィールド 173 の個数 |

セクション 173 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-174"></a>
### 保存セクション 174 { #preserve-sec-174 }

このセクションは preserve-existing e2e フィクスチャの 174 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field174Id | String | フィールド 174 の識別子 |
| body.field174Name | String | フィールド 174 の名前 |
| body.field174Count | Integer | フィールド 174 の個数 |

セクション 174 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-175"></a>
### 保存セクション 175 { #preserve-sec-175 }

このセクションは preserve-existing e2e フィクスチャの 175 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field175Id | String | フィールド 175 の識別子 |
| body.field175Name | String | フィールド 175 の名前 |
| body.field175Count | Integer | フィールド 175 の個数 |

```json
{
  "field175Id": "id-175",
  "field175Name": "name-175"
}
```

セクション 175 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-176"></a>
### 保存セクション 176 { #preserve-sec-176 }

このセクションは preserve-existing e2e フィクスチャの 176 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field176Id | String | フィールド 176 の識別子 |
| body.field176Name | String | フィールド 176 の名前 |
| body.field176Count | Integer | フィールド 176 の個数 |

セクション 176 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-177"></a>
### 保存セクション 177 { #preserve-sec-177 }

このセクションは preserve-existing e2e フィクスチャの 177 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field177Id | String | フィールド 177 の識別子 |
| body.field177Name | String | フィールド 177 の名前 |
| body.field177Count | Integer | フィールド 177 の個数 |

セクション 177 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-178"></a>
### 保存セクション 178 { #preserve-sec-178 }

このセクションは preserve-existing e2e フィクスチャの 178 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field178Id | String | フィールド 178 の識別子 |
| body.field178Name | String | フィールド 178 の名前 |
| body.field178Count | Integer | フィールド 178 の個数 |

セクション 178 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-179"></a>
### 保存セクション 179 { #preserve-sec-179 }

このセクションは preserve-existing e2e フィクスチャの 179 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field179Id | String | フィールド 179 の識別子 |
| body.field179Name | String | フィールド 179 の名前 |
| body.field179Count | Integer | フィールド 179 の個数 |

セクション 179 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-180"></a>
### 保存セクション 180 { #preserve-sec-180 }

このセクションは preserve-existing e2e フィクスチャの 180 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field180Id | String | フィールド 180 の識別子 |
| body.field180Name | String | フィールド 180 の名前 |
| body.field180Count | Integer | フィールド 180 の個数 |

```json
{
  "field180Id": "id-180",
  "field180Name": "name-180"
}
```

セクション 180 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-181"></a>
### 保存セクション 181 { #preserve-sec-181 }

このセクションは preserve-existing e2e フィクスチャの 181 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field181Id | String | フィールド 181 の識別子 |
| body.field181Name | String | フィールド 181 の名前 |
| body.field181Count | Integer | フィールド 181 の個数 |

セクション 181 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-182"></a>
### 保存セクション 182 { #preserve-sec-182 }

このセクションは preserve-existing e2e フィクスチャの 182 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field182Id | String | フィールド 182 の識別子 |
| body.field182Name | String | フィールド 182 の名前 |
| body.field182Count | Integer | フィールド 182 の個数 |

セクション 182 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-183"></a>
### 保存セクション 183 { #preserve-sec-183 }

このセクションは preserve-existing e2e フィクスチャの 183 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field183Id | String | フィールド 183 の識別子 |
| body.field183Name | String | フィールド 183 の名前 |
| body.field183Count | Integer | フィールド 183 の個数 |

セクション 183 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-184"></a>
### 保存セクション 184 { #preserve-sec-184 }

このセクションは preserve-existing e2e フィクスチャの 184 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field184Id | String | フィールド 184 の識別子 |
| body.field184Name | String | フィールド 184 の名前 |
| body.field184Count | Integer | フィールド 184 の個数 |

セクション 184 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-185"></a>
### 保存セクション 185 { #preserve-sec-185 }

このセクションは preserve-existing e2e フィクスチャの 185 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field185Id | String | フィールド 185 の識別子 |
| body.field185Name | String | フィールド 185 の名前 |
| body.field185Count | Integer | フィールド 185 の個数 |

```json
{
  "field185Id": "id-185",
  "field185Name": "name-185"
}
```

セクション 185 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-186"></a>
### 保存セクション 186 { #preserve-sec-186 }

このセクションは preserve-existing e2e フィクスチャの 186 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field186Id | String | フィールド 186 の識別子 |
| body.field186Name | String | フィールド 186 の名前 |
| body.field186Count | Integer | フィールド 186 の個数 |

セクション 186 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-187"></a>
### 保存セクション 187 { #preserve-sec-187 }

このセクションは preserve-existing e2e フィクスチャの 187 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field187Id | String | フィールド 187 の識別子 |
| body.field187Name | String | フィールド 187 の名前 |
| body.field187Count | Integer | フィールド 187 の個数 |

セクション 187 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-188"></a>
### 保存セクション 188 { #preserve-sec-188 }

このセクションは preserve-existing e2e フィクスチャの 188 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field188Id | String | フィールド 188 の識別子 |
| body.field188Name | String | フィールド 188 の名前 |
| body.field188Count | Integer | フィールド 188 の個数 |

セクション 188 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-189"></a>
### 保存セクション 189 { #preserve-sec-189 }

このセクションは preserve-existing e2e フィクスチャの 189 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field189Id | String | フィールド 189 の識別子 |
| body.field189Name | String | フィールド 189 の名前 |
| body.field189Count | Integer | フィールド 189 の個数 |

セクション 189 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-190"></a>
### 保存セクション 190 { #preserve-sec-190 }

このセクションは preserve-existing e2e フィクスチャの 190 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field190Id | String | フィールド 190 の識別子 |
| body.field190Name | String | フィールド 190 の名前 |
| body.field190Count | Integer | フィールド 190 の個数 |

```json
{
  "field190Id": "id-190",
  "field190Name": "name-190"
}
```

セクション 190 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-191"></a>
### 保存セクション 191 { #preserve-sec-191 }

このセクションは preserve-existing e2e フィクスチャの 191 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field191Id | String | フィールド 191 の識別子 |
| body.field191Name | String | フィールド 191 の名前 |
| body.field191Count | Integer | フィールド 191 の個数 |

セクション 191 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-192"></a>
### 保存セクション 192 { #preserve-sec-192 }

このセクションは preserve-existing e2e フィクスチャの 192 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field192Id | String | フィールド 192 の識別子 |
| body.field192Name | String | フィールド 192 の名前 |
| body.field192Count | Integer | フィールド 192 の個数 |

セクション 192 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-193"></a>
### 保存セクション 193 { #preserve-sec-193 }

このセクションは preserve-existing e2e フィクスチャの 193 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field193Id | String | フィールド 193 の識別子 |
| body.field193Name | String | フィールド 193 の名前 |
| body.field193Count | Integer | フィールド 193 の個数 |

セクション 193 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-194"></a>
### 保存セクション 194 { #preserve-sec-194 }

このセクションは preserve-existing e2e フィクスチャの 194 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field194Id | String | フィールド 194 の識別子 |
| body.field194Name | String | フィールド 194 の名前 |
| body.field194Count | Integer | フィールド 194 の個数 |

セクション 194 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-195"></a>
### 保存セクション 195 { #preserve-sec-195 }

このセクションは preserve-existing e2e フィクスチャの 195 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field195Id | String | フィールド 195 の識別子 |
| body.field195Name | String | フィールド 195 の名前 |
| body.field195Count | Integer | フィールド 195 の個数 |

```json
{
  "field195Id": "id-195",
  "field195Name": "name-195"
}
```

セクション 195 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-196"></a>
### 保存セクション 196 { #preserve-sec-196 }

このセクションは preserve-existing e2e フィクスチャの 196 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field196Id | String | フィールド 196 の識別子 |
| body.field196Name | String | フィールド 196 の名前 |
| body.field196Count | Integer | フィールド 196 の個数 |

セクション 196 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-197"></a>
### 保存セクション 197 { #preserve-sec-197 }

このセクションは preserve-existing e2e フィクスチャの 197 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field197Id | String | フィールド 197 の識別子 |
| body.field197Name | String | フィールド 197 の名前 |
| body.field197Count | Integer | フィールド 197 の個数 |

セクション 197 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-198"></a>
### 保存セクション 198 { #preserve-sec-198 }

このセクションは preserve-existing e2e フィクスチャの 198 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field198Id | String | フィールド 198 の識別子 |
| body.field198Name | String | フィールド 198 の名前 |
| body.field198Count | Integer | フィールド 198 の個数 |

セクション 198 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-199"></a>
### 保存セクション 199 { #preserve-sec-199 }

このセクションは preserve-existing e2e フィクスチャの 199 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field199Id | String | フィールド 199 の識別子 |
| body.field199Name | String | フィールド 199 の名前 |
| body.field199Count | Integer | フィールド 199 の個数 |

セクション 199 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-200"></a>
### 保存セクション 200 { #preserve-sec-200 }

このセクションは preserve-existing e2e フィクスチャの 200 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field200Id | String | フィールド 200 の識別子 |
| body.field200Name | String | フィールド 200 の名前 |
| body.field200Count | Integer | フィールド 200 の個数 |

```json
{
  "field200Id": "id-200",
  "field200Name": "name-200"
}
```

セクション 200 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-201"></a>
### 保存セクション 201 { #preserve-sec-201 }

このセクションは preserve-existing e2e フィクスチャの 201 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field201Id | String | フィールド 201 の識別子 |
| body.field201Name | String | フィールド 201 の名前 |
| body.field201Count | Integer | フィールド 201 の個数 |

セクション 201 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-202"></a>
### 保存セクション 202 { #preserve-sec-202 }

このセクションは preserve-existing e2e フィクスチャの 202 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field202Id | String | フィールド 202 の識別子 |
| body.field202Name | String | フィールド 202 の名前 |
| body.field202Count | Integer | フィールド 202 の個数 |

セクション 202 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-203"></a>
### 保存セクション 203 { #preserve-sec-203 }

このセクションは preserve-existing e2e フィクスチャの 203 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field203Id | String | フィールド 203 の識別子 |
| body.field203Name | String | フィールド 203 の名前 |
| body.field203Count | Integer | フィールド 203 の個数 |

セクション 203 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-204"></a>
### 保存セクション 204 { #preserve-sec-204 }

このセクションは preserve-existing e2e フィクスチャの 204 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field204Id | String | フィールド 204 の識別子 |
| body.field204Name | String | フィールド 204 の名前 |
| body.field204Count | Integer | フィールド 204 の個数 |

セクション 204 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-205"></a>
### 保存セクション 205 { #preserve-sec-205 }

このセクションは preserve-existing e2e フィクスチャの 205 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field205Id | String | フィールド 205 の識別子 |
| body.field205Name | String | フィールド 205 の名前 |
| body.field205Count | Integer | フィールド 205 の個数 |

```json
{
  "field205Id": "id-205",
  "field205Name": "name-205"
}
```

セクション 205 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-206"></a>
### 保存セクション 206 { #preserve-sec-206 }

このセクションは preserve-existing e2e フィクスチャの 206 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field206Id | String | フィールド 206 の識別子 |
| body.field206Name | String | フィールド 206 の名前 |
| body.field206Count | Integer | フィールド 206 の個数 |

セクション 206 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-207"></a>
### 保存セクション 207 { #preserve-sec-207 }

このセクションは preserve-existing e2e フィクスチャの 207 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field207Id | String | フィールド 207 の識別子 |
| body.field207Name | String | フィールド 207 の名前 |
| body.field207Count | Integer | フィールド 207 の個数 |

セクション 207 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-208"></a>
### 保存セクション 208 { #preserve-sec-208 }

このセクションは preserve-existing e2e フィクスチャの 208 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field208Id | String | フィールド 208 の識別子 |
| body.field208Name | String | フィールド 208 の名前 |
| body.field208Count | Integer | フィールド 208 の個数 |

セクション 208 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-209"></a>
### 保存セクション 209 { #preserve-sec-209 }

このセクションは preserve-existing e2e フィクスチャの 209 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field209Id | String | フィールド 209 の識別子 |
| body.field209Name | String | フィールド 209 の名前 |
| body.field209Count | Integer | フィールド 209 の個数 |

セクション 209 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-210"></a>
### 保存セクション 210 { #preserve-sec-210 }

このセクションは preserve-existing e2e フィクスチャの 210 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field210Id | String | フィールド 210 の識別子 |
| body.field210Name | String | フィールド 210 の名前 |
| body.field210Count | Integer | フィールド 210 の個数 |

```json
{
  "field210Id": "id-210",
  "field210Name": "name-210"
}
```

セクション 210 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-211"></a>
### 保存セクション 211 { #preserve-sec-211 }

このセクションは preserve-existing e2e フィクスチャの 211 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field211Id | String | フィールド 211 の識別子 |
| body.field211Name | String | フィールド 211 の名前 |
| body.field211Count | Integer | フィールド 211 の個数 |

セクション 211 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-212"></a>
### 保存セクション 212 { #preserve-sec-212 }

このセクションは preserve-existing e2e フィクスチャの 212 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field212Id | String | フィールド 212 の識別子 |
| body.field212Name | String | フィールド 212 の名前 |
| body.field212Count | Integer | フィールド 212 の個数 |

セクション 212 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-213"></a>
### 保存セクション 213 { #preserve-sec-213 }

このセクションは preserve-existing e2e フィクスチャの 213 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field213Id | String | フィールド 213 の識別子 |
| body.field213Name | String | フィールド 213 の名前 |
| body.field213Count | Integer | フィールド 213 の個数 |

セクション 213 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-214"></a>
### 保存セクション 214 { #preserve-sec-214 }

このセクションは preserve-existing e2e フィクスチャの 214 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field214Id | String | フィールド 214 の識別子 |
| body.field214Name | String | フィールド 214 の名前 |
| body.field214Count | Integer | フィールド 214 の個数 |

セクション 214 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-215"></a>
### 保存セクション 215 { #preserve-sec-215 }

このセクションは preserve-existing e2e フィクスチャの 215 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field215Id | String | フィールド 215 の識別子 |
| body.field215Name | String | フィールド 215 の名前 |
| body.field215Count | Integer | フィールド 215 の個数 |

```json
{
  "field215Id": "id-215",
  "field215Name": "name-215"
}
```

セクション 215 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-216"></a>
### 保存セクション 216 { #preserve-sec-216 }

このセクションは preserve-existing e2e フィクスチャの 216 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field216Id | String | フィールド 216 の識別子 |
| body.field216Name | String | フィールド 216 の名前 |
| body.field216Count | Integer | フィールド 216 の個数 |

セクション 216 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-217"></a>
### 保存セクション 217 { #preserve-sec-217 }

このセクションは preserve-existing e2e フィクスチャの 217 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field217Id | String | フィールド 217 の識別子 |
| body.field217Name | String | フィールド 217 の名前 |
| body.field217Count | Integer | フィールド 217 の個数 |

セクション 217 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-218"></a>
### 保存セクション 218 { #preserve-sec-218 }

このセクションは preserve-existing e2e フィクスチャの 218 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field218Id | String | フィールド 218 の識別子 |
| body.field218Name | String | フィールド 218 の名前 |
| body.field218Count | Integer | フィールド 218 の個数 |

セクション 218 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-219"></a>
### 保存セクション 219 { #preserve-sec-219 }

このセクションは preserve-existing e2e フィクスチャの 219 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field219Id | String | フィールド 219 の識別子 |
| body.field219Name | String | フィールド 219 の名前 |
| body.field219Count | Integer | フィールド 219 の個数 |

セクション 219 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-220"></a>
### 保存セクション 220 { #preserve-sec-220 }

このセクションは preserve-existing e2e フィクスチャの 220 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field220Id | String | フィールド 220 の識別子 |
| body.field220Name | String | フィールド 220 の名前 |
| body.field220Count | Integer | フィールド 220 の個数 |

```json
{
  "field220Id": "id-220",
  "field220Name": "name-220"
}
```

セクション 220 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-221"></a>
### 保存セクション 221 { #preserve-sec-221 }

このセクションは preserve-existing e2e フィクスチャの 221 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field221Id | String | フィールド 221 の識別子 |
| body.field221Name | String | フィールド 221 の名前 |
| body.field221Count | Integer | フィールド 221 の個数 |

セクション 221 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-222"></a>
### 保存セクション 222 { #preserve-sec-222 }

このセクションは preserve-existing e2e フィクスチャの 222 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field222Id | String | フィールド 222 の識別子 |
| body.field222Name | String | フィールド 222 の名前 |
| body.field222Count | Integer | フィールド 222 の個数 |

セクション 222 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-223"></a>
### 保存セクション 223 { #preserve-sec-223 }

このセクションは preserve-existing e2e フィクスチャの 223 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field223Id | String | フィールド 223 の識別子 |
| body.field223Name | String | フィールド 223 の名前 |
| body.field223Count | Integer | フィールド 223 の個数 |

セクション 223 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-224"></a>
### 保存セクション 224 { #preserve-sec-224 }

このセクションは preserve-existing e2e フィクスチャの 224 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field224Id | String | フィールド 224 の識別子 |
| body.field224Name | String | フィールド 224 の名前 |
| body.field224Count | Integer | フィールド 224 の個数 |

セクション 224 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-225"></a>
### 保存セクション 225 { #preserve-sec-225 }

このセクションは preserve-existing e2e フィクスチャの 225 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field225Id | String | フィールド 225 の識別子 |
| body.field225Name | String | フィールド 225 の名前 |
| body.field225Count | Integer | フィールド 225 の個数 |

```json
{
  "field225Id": "id-225",
  "field225Name": "name-225"
}
```

セクション 225 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-226"></a>
### 保存セクション 226 { #preserve-sec-226 }

このセクションは preserve-existing e2e フィクスチャの 226 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field226Id | String | フィールド 226 の識別子 |
| body.field226Name | String | フィールド 226 の名前 |
| body.field226Count | Integer | フィールド 226 の個数 |

セクション 226 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-227"></a>
### 保存セクション 227 { #preserve-sec-227 }

このセクションは preserve-existing e2e フィクスチャの 227 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field227Id | String | フィールド 227 の識別子 |
| body.field227Name | String | フィールド 227 の名前 |
| body.field227Count | Integer | フィールド 227 の個数 |

セクション 227 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-228"></a>
### 保存セクション 228 { #preserve-sec-228 }

このセクションは preserve-existing e2e フィクスチャの 228 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field228Id | String | フィールド 228 の識別子 |
| body.field228Name | String | フィールド 228 の名前 |
| body.field228Count | Integer | フィールド 228 の個数 |

セクション 228 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-229"></a>
### 保存セクション 229 { #preserve-sec-229 }

このセクションは preserve-existing e2e フィクスチャの 229 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field229Id | String | フィールド 229 の識別子 |
| body.field229Name | String | フィールド 229 の名前 |
| body.field229Count | Integer | フィールド 229 の個数 |

セクション 229 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-230"></a>
### 保存セクション 230 { #preserve-sec-230 }

このセクションは preserve-existing e2e フィクスチャの 230 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field230Id | String | フィールド 230 の識別子 |
| body.field230Name | String | フィールド 230 の名前 |
| body.field230Count | Integer | フィールド 230 の個数 |

```json
{
  "field230Id": "id-230",
  "field230Name": "name-230"
}
```

セクション 230 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-231"></a>
### 保存セクション 231 { #preserve-sec-231 }

このセクションは preserve-existing e2e フィクスチャの 231 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field231Id | String | フィールド 231 の識別子 |
| body.field231Name | String | フィールド 231 の名前 |
| body.field231Count | Integer | フィールド 231 の個数 |

セクション 231 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-232"></a>
### 保存セクション 232 { #preserve-sec-232 }

このセクションは preserve-existing e2e フィクスチャの 232 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field232Id | String | フィールド 232 の識別子 |
| body.field232Name | String | フィールド 232 の名前 |
| body.field232Count | Integer | フィールド 232 の個数 |

セクション 232 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-233"></a>
### 保存セクション 233 { #preserve-sec-233 }

このセクションは preserve-existing e2e フィクスチャの 233 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field233Id | String | フィールド 233 の識別子 |
| body.field233Name | String | フィールド 233 の名前 |
| body.field233Count | Integer | フィールド 233 の個数 |

セクション 233 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-234"></a>
### 保存セクション 234 { #preserve-sec-234 }

このセクションは preserve-existing e2e フィクスチャの 234 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field234Id | String | フィールド 234 の識別子 |
| body.field234Name | String | フィールド 234 の名前 |
| body.field234Count | Integer | フィールド 234 の個数 |

セクション 234 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-235"></a>
### 保存セクション 235 { #preserve-sec-235 }

このセクションは preserve-existing e2e フィクスチャの 235 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field235Id | String | フィールド 235 の識別子 |
| body.field235Name | String | フィールド 235 の名前 |
| body.field235Count | Integer | フィールド 235 の個数 |

```json
{
  "field235Id": "id-235",
  "field235Name": "name-235"
}
```

セクション 235 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-236"></a>
### 保存セクション 236 { #preserve-sec-236 }

このセクションは preserve-existing e2e フィクスチャの 236 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field236Id | String | フィールド 236 の識別子 |
| body.field236Name | String | フィールド 236 の名前 |
| body.field236Count | Integer | フィールド 236 の個数 |

セクション 236 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-237"></a>
### 保存セクション 237 { #preserve-sec-237 }

このセクションは preserve-existing e2e フィクスチャの 237 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field237Id | String | フィールド 237 の識別子 |
| body.field237Name | String | フィールド 237 の名前 |
| body.field237Count | Integer | フィールド 237 の個数 |

セクション 237 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-238"></a>
### 保存セクション 238 { #preserve-sec-238 }

このセクションは preserve-existing e2e フィクスチャの 238 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field238Id | String | フィールド 238 の識別子 |
| body.field238Name | String | フィールド 238 の名前 |
| body.field238Count | Integer | フィールド 238 の個数 |

セクション 238 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-239"></a>
### 保存セクション 239 { #preserve-sec-239 }

このセクションは preserve-existing e2e フィクスチャの 239 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field239Id | String | フィールド 239 の識別子 |
| body.field239Name | String | フィールド 239 の名前 |
| body.field239Count | Integer | フィールド 239 の個数 |

セクション 239 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。

<a id="preserve-sec-240"></a>
### 保存セクション 240 { #preserve-sec-240 }

このセクションは preserve-existing e2e フィクスチャの 240 番目のセクションです。
以下の表とサンプルはセクションごとにフィールド名が異なります。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field240Id | String | フィールド 240 の識別子 |
| body.field240Name | String | フィールド 240 の名前 |
| body.field240Count | Integer | フィールド 240 の個数 |

```json
{
  "field240Id": "id-240",
  "field240Name": "name-240"
}
```

セクション 240 の最後の説明文です。この文は再翻訳の有無を行単位で判定するために
使われます。