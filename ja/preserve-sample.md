<!-- machine_translated: true -->

<!-- pre-align:aligned sig=e2epreserve -->

<a id="preserve-e2e"></a>
# preserve-existing e2eフィクスチャ

このドキュメントは自動生成されたe2eフィクスチャです (20260903-114126)。

<a id="preserve-sec-1"></a>
### 保存セクション 1 { #preserve-sec-1 }

このセクションはpreserve-existing e2eフィクスチャの1番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field1Id | String | 1番フィールドの識別子 |
| body.field1Name | String | 1番フィールドの名前 |
| body.field1Count | Integer | 1番フィールドの個数 |

1番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-2"></a>
### 保存セクション 2 { #preserve-sec-2 }

このセクションはpreserve-existing e2eフィクスチャの2番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field2Id | String | 2番フィールドの識別子 |
| body.field2Name | String | 2番フィールドの名前 |
| body.field2Count | Integer | 2番フィールドの個数 |

2番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-3"></a>
### 保存セクション 3 { #preserve-sec-3 }

このセクションはpreserve-existing e2eフィクスチャの3番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field3Id | String | 3番フィールドの識別子 |
| body.field3Name | String | 3番フィールドの名前 |
| body.field3Count | Integer | 3番フィールドの個数 |

3番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-4"></a>
### 保存セクション 4 { #preserve-sec-4 }

このセクションはpreserve-existing e2eフィクスチャの4番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field4Id | String | 4番フィールドの識別子 |
| body.field4Name | String | 4番フィールドの名前 |
| body.field4Count | Integer | 4番フィールドの個数 |

4番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-5"></a>
### 保存セクション 5 { #preserve-sec-5 }

このセクションはpreserve-existing e2eフィクスチャの5番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field5Id | String | 5番フィールドの識別子 |
| body.field5Name | String | 5番フィールドの名前 |
| body.field5Count | Integer | 5番フィールドの個数 |

```json
{
  "field5Id": "id-5",
  "field5Name": "name-5"
}
```

5番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-6"></a>
### 保存セクション 6 { #preserve-sec-6 }

このセクションはpreserve-existing e2eフィクスチャの6番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field6Id | String | 6番フィールドの識別子 |
| body.field6Name | String | 6番フィールドの名前 |
| body.field6Count | Integer | 6番フィールドの個数 |

6番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-7"></a>
### 保存セクション 7 { #preserve-sec-7 }

このセクションはpreserve-existing e2eフィクスチャの7番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field7Id | String | 7番フィールドの識別子 |
| body.field7Name | String | 7番フィールドの名前 |
| body.field7Count | Integer | 7番フィールドの個数 |

7番セクションの最後の説明文を20260903-114126で変更しました。このセクションのみ再翻訳する必要があり、残りのセクションは既存の翻訳がそのまま残る必要があります。

<a id="preserve-sec-8"></a>
### 保存セクション 8 { #preserve-sec-8 }

このセクションはpreserve-existing e2eフィクスチャの8番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field8Id | String | 8番フィールドの識別子 |
| body.field8Name | String | 8番フィールドの名前 |
| body.field8Count | Integer | 8番フィールドの個数 |

8番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-9"></a>
### 保存セクション 9 { #preserve-sec-9 }

このセクションはpreserve-existing e2eフィクスチャの9番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field9Id | String | 9番フィールドの識別子 |
| body.field9Name | String | 9番フィールドの名前 |
| body.field9Count | Integer | 9番フィールドの個数 |

9番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-10"></a>
### 保存セクション 10 { #preserve-sec-10 }

このセクションはpreserve-existing e2eフィクスチャの10番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field10Id | String | 10番フィールドの識別子 |
| body.field10Name | String | 10番フィールドの名前 |
| body.field10Count | Integer | 10番フィールドの個数 |

```json
{
  "field10Id": "id-10",
  "field10Name": "name-10"
}
```

10番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-11"></a>
### 保存セクション 11 { #preserve-sec-11 }

このセクションはpreserve-existing e2eフィクスチャの11番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field11Id | String | 11番フィールドの識別子 |
| body.field11Name | String | 11番フィールドの名前 |
| body.field11Count | Integer | 11番フィールドの個数 |

11番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-12"></a>
### 保存セクション 12 { #preserve-sec-12 }

このセクションはpreserve-existing e2eフィクスチャの12番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field12Id | String | 12番フィールドの識別子 |
| body.field12Name | String | 12番フィールドの名前 |
| body.field12Count | Integer | 12番フィールドの個数 |

12番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-13"></a>
### 保存セクション 13 { #preserve-sec-13 }

このセクションはpreserve-existing e2eフィクスチャの13番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field13Id | String | 13番フィールドの識別子 |
| body.field13Name | String | 13番フィールドの名前 |
| body.field13Count | Integer | 13番フィールドの個数 |

13番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-14"></a>
### 保存セクション 14 { #preserve-sec-14 }

このセクションはpreserve-existing e2eフィクスチャの14番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field14Id | String | 14番フィールドの識別子 |
| body.field14Name | String | 14番フィールドの名前 |
| body.field14Count | Integer | 14番フィールドの個数 |

14番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-15"></a>
### 保存セクション 15 { #preserve-sec-15 }

このセクションはpreserve-existing e2eフィクスチャの15番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field15Id | String | 15番フィールドの識別子 |
| body.field15Name | String | 15番フィールドの名前 |
| body.field15Count | Integer | 15番フィールドの個数 |

```json
{
  "field15Id": "id-15",
  "field15Name": "name-15"
}
```

15番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-16"></a>
### 保存セクション 16 { #preserve-sec-16 }

このセクションはpreserve-existing e2eフィクスチャの16番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field16Id | String | 16番フィールドの識別子 |
| body.field16Name | String | 16番フィールドの名前 |
| body.field16Count | Integer | 16番フィールドの個数 |

16番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-17"></a>
### 保存セクション 17 { #preserve-sec-17 }

このセクションはpreserve-existing e2eフィクスチャの17番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field17Id | String | 17番フィールドの識別子 |
| body.field17Name | String | 17番フィールドの名前 |
| body.field17Count | Integer | 17番フィールドの個数 |

17番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-18"></a>
### 保存セクション 18 { #preserve-sec-18 }

このセクションはpreserve-existing e2eフィクスチャの18番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field18Id | String | 18番フィールドの識別子 |
| body.field18Name | String | 18番フィールドの名前 |
| body.field18Count | Integer | 18番フィールドの個数 |

18番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-19"></a>
### 保存セクション 19 { #preserve-sec-19 }

このセクションはpreserve-existing e2eフィクスチャの19番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field19Id | String | 19番フィールドの識別子 |
| body.field19Name | String | 19番フィールドの名前 |
| body.field19Count | Integer | 19番フィールドの個数 |

19番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-20"></a>
### 保存セクション 20 { #preserve-sec-20 }

このセクションはpreserve-existing e2eフィクスチャの20番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field20Id | String | 20番フィールドの識別子 |
| body.field20Name | String | 20番フィールドの名前 |
| body.field20Count | Integer | 20番フィールドの個数 |

```json
{
  "field20Id": "id-20",
  "field20Name": "name-20"
}
```

20番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-21"></a>
### 保存セクション 21 { #preserve-sec-21 }

このセクションはpreserve-existing e2eフィクスチャの21番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field21Id | String | 21番フィールドの識別子 |
| body.field21Name | String | 21番フィールドの名前 |
| body.field21Count | Integer | 21番フィールドの個数 |

21番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-22"></a>
### 保存セクション 22 { #preserve-sec-22 }

このセクションはpreserve-existing e2eフィクスチャの22番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field22Id | String | 22番フィールドの識別子 |
| body.field22Name | String | 22番フィールドの名前 |
| body.field22Count | Integer | 22番フィールドの個数 |

22番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-23"></a>
### 保存セクション 23 { #preserve-sec-23 }

このセクションはpreserve-existing e2eフィクスチャの23番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field23Id | String | 23番フィールドの識別子 |
| body.field23Name | String | 23番フィールドの名前 |
| body.field23Count | Integer | 23番フィールドの個数 |

23番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-24"></a>
### 保存セクション 24 { #preserve-sec-24 }

このセクションはpreserve-existing e2eフィクスチャの24番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field24Id | String | 24番フィールドの識別子 |
| body.field24Name | String | 24番フィールドの名前 |
| body.field24Count | Integer | 24番フィールドの個数 |

24番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-25"></a>
### 保存セクション 25 { #preserve-sec-25 }

このセクションはpreserve-existing e2eフィクスチャの25番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field25Id | String | 25番フィールドの識別子 |
| body.field25Name | String | 25番フィールドの名前 |
| body.field25Count | Integer | 25番フィールドの個数 |

```json
{
  "field25Id": "id-25",
  "field25Name": "name-25"
}
```

25番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-26"></a>
### 保存セクション 26 { #preserve-sec-26 }

このセクションはpreserve-existing e2eフィクスチャの26番目のセクションです。以下の表と例はセクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field26Id | String | 26番フィールドの識別子 |
| body.field26Name | String | 26番フィールドの名前 |
| body.field26Count | Integer | 26番フィールドの個数 |

26番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定する際に使用されます。

<a id="preserve-sec-27"></a>
### 保存セクション 27 { #preserve-sec-27 }

このセクションは preserve-existing e2e フィクスチャの 27 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field27Id | String | 27 番フィールドの識別子 |
| body.field27Name | String | 27 番フィールドの名前 |
| body.field27Count | Integer | 27 番フィールドの個数 |

27 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-28"></a>
### 保存セクション 28 { #preserve-sec-28 }

このセクションは preserve-existing e2e フィクスチャの 28 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field28Id | String | 28 番フィールドの識別子 |
| body.field28Name | String | 28 番フィールドの名前 |
| body.field28Count | Integer | 28 番フィールドの個数 |

28 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-29"></a>
### 保存セクション 29 { #preserve-sec-29 }

このセクションは preserve-existing e2e フィクスチャの 29 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field29Id | String | 29 番フィールドの識別子 |
| body.field29Name | String | 29 番フィールドの名前 |
| body.field29Count | Integer | 29 番フィールドの個数 |

29 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-30"></a>
### 保存セクション 30 { #preserve-sec-30 }

このセクションは preserve-existing e2e フィクスチャの 30 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field30Id | String | 30 番フィールドの識別子 |
| body.field30Name | String | 30 番フィールドの名前 |
| body.field30Count | Integer | 30 番フィールドの個数 |

```json
{
  "field30Id": "id-30",
  "field30Name": "name-30"
}
```

30 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-31"></a>
### 保存セクション 31 { #preserve-sec-31 }

このセクションは preserve-existing e2e フィクスチャの 31 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field31Id | String | 31 番フィールドの識別子 |
| body.field31Name | String | 31 番フィールドの名前 |
| body.field31Count | Integer | 31 番フィールドの個数 |

31 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-32"></a>
### 保存セクション 32 { #preserve-sec-32 }

このセクションは preserve-existing e2e フィクスチャの 32 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field32Id | String | 32 番フィールドの識別子 |
| body.field32Name | String | 32 番フィールドの名前 |
| body.field32Count | Integer | 32 番フィールドの個数 |

32 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-33"></a>
### 保存セクション 33 { #preserve-sec-33 }

このセクションは preserve-existing e2e フィクスチャの 33 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field33Id | String | 33 番フィールドの識別子 |
| body.field33Name | String | 33 番フィールドの名前 |
| body.field33Count | Integer | 33 番フィールドの個数 |

33 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-34"></a>
### 保存セクション 34 { #preserve-sec-34 }

このセクションは preserve-existing e2e フィクスチャの 34 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field34Id | String | 34 番フィールドの識別子 |
| body.field34Name | String | 34 番フィールドの名前 |
| body.field34Count | Integer | 34 番フィールドの個数 |

34 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-35"></a>
### 保存セクション 35 { #preserve-sec-35 }

このセクションは preserve-existing e2e フィクスチャの 35 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field35Id | String | 35 番フィールドの識別子 |
| body.field35Name | String | 35 番フィールドの名前 |
| body.field35Count | Integer | 35 番フィールドの個数 |

```json
{
  "field35Id": "id-35",
  "field35Name": "name-35"
}
```

35 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-36"></a>
### 保存セクション 36 { #preserve-sec-36 }

このセクションは preserve-existing e2e フィクスチャの 36 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field36Id | String | 36 番フィールドの識別子 |
| body.field36Name | String | 36 番フィールドの名前 |
| body.field36Count | Integer | 36 番フィールドの個数 |

36 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-37"></a>
### 保存セクション 37 { #preserve-sec-37 }

このセクションは preserve-existing e2e フィクスチャの 37 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field37Id | String | 37 番フィールドの識別子 |
| body.field37Name | String | 37 番フィールドの名前 |
| body.field37Count | Integer | 37 番フィールドの個数 |

37 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-38"></a>
### 保存セクション 38 { #preserve-sec-38 }

このセクションは preserve-existing e2e フィクスチャの 38 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field38Id | String | 38 番フィールドの識別子 |
| body.field38Name | String | 38 番フィールドの名前 |
| body.field38Count | Integer | 38 番フィールドの個数 |

38 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-39"></a>
### 保存セクション 39 { #preserve-sec-39 }

このセクションは preserve-existing e2e フィクスチャの 39 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field39Id | String | 39 番フィールドの識別子 |
| body.field39Name | String | 39 番フィールドの名前 |
| body.field39Count | Integer | 39 番フィールドの個数 |

39 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-40"></a>
### 保存セクション 40 { #preserve-sec-40 }

このセクションは preserve-existing e2e フィクスチャの 40 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field40Id | String | 40 番フィールドの識別子 |
| body.field40Name | String | 40 番フィールドの名前 |
| body.field40Count | Integer | 40 番フィールドの個数 |

```json
{
  "field40Id": "id-40",
  "field40Name": "name-40"
}
```

40 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-41"></a>
### 保存セクション 41 { #preserve-sec-41 }

このセクションは preserve-existing e2e フィクスチャの 41 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field41Id | String | 41 番フィールドの識別子 |
| body.field41Name | String | 41 番フィールドの名前 |
| body.field41Count | Integer | 41 番フィールドの個数 |

41 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-42"></a>
### 保存セクション 42 { #preserve-sec-42 }

このセクションは preserve-existing e2e フィクスチャの 42 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field42Id | String | 42 番フィールドの識別子 |
| body.field42Name | String | 42 番フィールドの名前 |
| body.field42Count | Integer | 42 番フィールドの個数 |

42 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-43"></a>
### 保存セクション 43 { #preserve-sec-43 }

このセクションは preserve-existing e2e フィクスチャの 43 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field43Id | String | 43 番フィールドの識別子 |
| body.field43Name | String | 43 番フィールドの名前 |
| body.field43Count | Integer | 43 番フィールドの個数 |

43 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-44"></a>
### 保存セクション 44 { #preserve-sec-44 }

このセクションは preserve-existing e2e フィクスチャの 44 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field44Id | String | 44 番フィールドの識別子 |
| body.field44Name | String | 44 番フィールドの名前 |
| body.field44Count | Integer | 44 番フィールドの個数 |

44 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-45"></a>
### 保存セクション 45 { #preserve-sec-45 }

このセクションは preserve-existing e2e フィクスチャの 45 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field45Id | String | 45 番フィールドの識別子 |
| body.field45Name | String | 45 番フィールドの名前 |
| body.field45Count | Integer | 45 番フィールドの個数 |

```json
{
  "field45Id": "id-45",
  "field45Name": "name-45"
}
```

45 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-46"></a>
### 保存セクション 46 { #preserve-sec-46 }

このセクションは preserve-existing e2e フィクスチャの 46 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field46Id | String | 46 番フィールドの識別子 |
| body.field46Name | String | 46 番フィールドの名前 |
| body.field46Count | Integer | 46 番フィールドの個数 |

46 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-47"></a>
### 保存セクション 47 { #preserve-sec-47 }

このセクションは preserve-existing e2e フィクスチャの 47 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field47Id | String | 47 番フィールドの識別子 |
| body.field47Name | String | 47 番フィールドの名前 |
| body.field47Count | Integer | 47 番フィールドの個数 |

47 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-48"></a>
### 保存セクション 48 { #preserve-sec-48 }

このセクションは preserve-existing e2e フィクスチャの 48 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field48Id | String | 48 番フィールドの識別子 |
| body.field48Name | String | 48 番フィールドの名前 |
| body.field48Count | Integer | 48 番フィールドの個数 |

48 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-49"></a>
### 保存セクション 49 { #preserve-sec-49 }

このセクションは preserve-existing e2e フィクスチャの 49 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field49Id | String | 49 番フィールドの識別子 |
| body.field49Name | String | 49 番フィールドの名前 |
| body.field49Count | Integer | 49 番フィールドの個数 |

49 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-50"></a>
### 保存セクション 50 { #preserve-sec-50 }

このセクションは preserve-existing e2e フィクスチャの 50 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field50Id | String | 50 番フィールドの識別子 |
| body.field50Name | String | 50 番フィールドの名前 |
| body.field50Count | Integer | 50 番フィールドの個数 |

```json
{
  "field50Id": "id-50",
  "field50Name": "name-50"
}
```

50 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-51"></a>
### 保存セクション 51 { #preserve-sec-51 }

このセクションは preserve-existing e2e フィクスチャの 51 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field51Id | String | 51 番フィールドの識別子 |
| body.field51Name | String | 51 番フィールドの名前 |
| body.field51Count | Integer | 51 番フィールドの個数 |

51 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-52"></a>
### 保存セクション 52 { #preserve-sec-52 }

このセクションは preserve-existing e2e フィクスチャの 52 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field52Id | String | 52 番フィールドの識別子 |
| body.field52Name | String | 52 番フィールドの名前 |
| body.field52Count | Integer | 52 番フィールドの個数 |

52 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定するときに使用されます。

<a id="preserve-sec-53"></a>
### 保持セクション 53 { #preserve-sec-53 }

このセクションは preserve-existing e2e フィクスチャの 53 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field53Id | String | 53番フィールドの識別子 |
| body.field53Name | String | 53番フィールドの名前 |
| body.field53Count | Integer | 53番フィールドの個数 |

53番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-54"></a>
### 保持セクション 54 { #preserve-sec-54 }

このセクションは preserve-existing e2e フィクスチャの 54 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field54Id | String | 54番フィールドの識別子 |
| body.field54Name | String | 54番フィールドの名前 |
| body.field54Count | Integer | 54番フィールドの個数 |

54番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-55"></a>
### 保持セクション 55 { #preserve-sec-55 }

このセクションは preserve-existing e2e フィクスチャの 55 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field55Id | String | 55番フィールドの識別子 |
| body.field55Name | String | 55番フィールドの名前 |
| body.field55Count | Integer | 55番フィールドの個数 |

```json
{
  "field55Id": "id-55",
  "field55Name": "name-55"
}
```

55番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-56"></a>
### 保持セクション 56 { #preserve-sec-56 }

このセクションは preserve-existing e2e フィクスチャの 56 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field56Id | String | 56番フィールドの識別子 |
| body.field56Name | String | 56番フィールドの名前 |
| body.field56Count | Integer | 56番フィールドの個数 |

56番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-57"></a>
### 保持セクション 57 { #preserve-sec-57 }

このセクションは preserve-existing e2e フィクスチャの 57 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field57Id | String | 57番フィールドの識別子 |
| body.field57Name | String | 57番フィールドの名前 |
| body.field57Count | Integer | 57番フィールドの個数 |

57番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-58"></a>
### 保持セクション 58 { #preserve-sec-58 }

このセクションは preserve-existing e2e フィクスチャの 58 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field58Id | String | 58番フィールドの識別子 |
| body.field58Name | String | 58番フィールドの名前 |
| body.field58Count | Integer | 58番フィールドの個数 |

58番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-59"></a>
### 保持セクション 59 { #preserve-sec-59 }

このセクションは preserve-existing e2e フィクスチャの 59 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field59Id | String | 59番フィールドの識別子 |
| body.field59Name | String | 59番フィールドの名前 |
| body.field59Count | Integer | 59番フィールドの個数 |

59番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-60"></a>
### 保持セクション 60 { #preserve-sec-60 }

このセクションは preserve-existing e2e フィクスチャの 60 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field60Id | String | 60番フィールドの識別子 |
| body.field60Name | String | 60番フィールドの名前 |
| body.field60Count | Integer | 60番フィールドの個数 |

```json
{
  "field60Id": "id-60",
  "field60Name": "name-60"
}
```

60番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-61"></a>
### 保持セクション 61 { #preserve-sec-61 }

このセクションは preserve-existing e2e フィクスチャの 61 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field61Id | String | 61番フィールドの識別子 |
| body.field61Name | String | 61番フィールドの名前 |
| body.field61Count | Integer | 61番フィールドの個数 |

61番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-62"></a>
### 保持セクション 62 { #preserve-sec-62 }

このセクションは preserve-existing e2e フィクスチャの 62 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field62Id | String | 62番フィールドの識別子 |
| body.field62Name | String | 62番フィールドの名前 |
| body.field62Count | Integer | 62番フィールドの個数 |

62番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-63"></a>
### 保持セクション 63 { #preserve-sec-63 }

このセクションは preserve-existing e2e フィクスチャの 63 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field63Id | String | 63番フィールドの識別子 |
| body.field63Name | String | 63番フィールドの名前 |
| body.field63Count | Integer | 63番フィールドの個数 |

63番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-64"></a>
### 保持セクション 64 { #preserve-sec-64 }

このセクションは preserve-existing e2e フィクスチャの 64 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field64Id | String | 64番フィールドの識別子 |
| body.field64Name | String | 64番フィールドの名前 |
| body.field64Count | Integer | 64番フィールドの個数 |

64番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-65"></a>
### 保持セクション 65 { #preserve-sec-65 }

このセクションは preserve-existing e2e フィクスチャの 65 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field65Id | String | 65番フィールドの識別子 |
| body.field65Name | String | 65番フィールドの名前 |
| body.field65Count | Integer | 65番フィールドの個数 |

```json
{
  "field65Id": "id-65",
  "field65Name": "name-65"
}
```

65番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-66"></a>
### 保持セクション 66 { #preserve-sec-66 }

このセクションは preserve-existing e2e フィクスチャの 66 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field66Id | String | 66番フィールドの識別子 |
| body.field66Name | String | 66番フィールドの名前 |
| body.field66Count | Integer | 66番フィールドの個数 |

66番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-67"></a>
### 保持セクション 67 { #preserve-sec-67 }

このセクションは preserve-existing e2e フィクスチャの 67 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field67Id | String | 67番フィールドの識別子 |
| body.field67Name | String | 67番フィールドの名前 |
| body.field67Count | Integer | 67番フィールドの個数 |

67番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-68"></a>
### 保持セクション 68 { #preserve-sec-68 }

このセクションは preserve-existing e2e フィクスチャの 68 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field68Id | String | 68番フィールドの識別子 |
| body.field68Name | String | 68番フィールドの名前 |
| body.field68Count | Integer | 68番フィールドの個数 |

68番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-69"></a>
### 保持セクション 69 { #preserve-sec-69 }

このセクションは preserve-existing e2e フィクスチャの 69 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field69Id | String | 69番フィールドの識別子 |
| body.field69Name | String | 69番フィールドの名前 |
| body.field69Count | Integer | 69番フィールドの個数 |

69番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-70"></a>
### 保持セクション 70 { #preserve-sec-70 }

このセクションは preserve-existing e2e フィクスチャの 70 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field70Id | String | 70番フィールドの識別子 |
| body.field70Name | String | 70番フィールドの名前 |
| body.field70Count | Integer | 70番フィールドの個数 |

```json
{
  "field70Id": "id-70",
  "field70Name": "name-70"
}
```

70番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-71"></a>
### 保持セクション 71 { #preserve-sec-71 }

このセクションは preserve-existing e2e フィクスチャの 71 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field71Id | String | 71番フィールドの識別子 |
| body.field71Name | String | 71番フィールドの名前 |
| body.field71Count | Integer | 71番フィールドの個数 |

71番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-72"></a>
### 保持セクション 72 { #preserve-sec-72 }

このセクションは preserve-existing e2e フィクスチャの 72 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field72Id | String | 72番フィールドの識別子 |
| body.field72Name | String | 72番フィールドの名前 |
| body.field72Count | Integer | 72番フィールドの個数 |

72番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-73"></a>
### 保持セクション 73 { #preserve-sec-73 }

このセクションは preserve-existing e2e フィクスチャの 73 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field73Id | String | 73番フィールドの識別子 |
| body.field73Name | String | 73番フィールドの名前 |
| body.field73Count | Integer | 73番フィールドの個数 |

73番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-74"></a>
### 保持セクション 74 { #preserve-sec-74 }

このセクションは preserve-existing e2e フィクスチャの 74 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field74Id | String | 74番フィールドの識別子 |
| body.field74Name | String | 74番フィールドの名前 |
| body.field74Count | Integer | 74番フィールドの個数 |

74番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-75"></a>
### 保持セクション 75 { #preserve-sec-75 }

このセクションは preserve-existing e2e フィクスチャの 75 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field75Id | String | 75番フィールドの識別子 |
| body.field75Name | String | 75番フィールドの名前 |
| body.field75Count | Integer | 75番フィールドの個数 |

```json
{
  "field75Id": "id-75",
  "field75Name": "name-75"
}
```

75番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-76"></a>
### 保持セクション 76 { #preserve-sec-76 }

このセクションは preserve-existing e2e フィクスチャの 76 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field76Id | String | 76番フィールドの識別子 |
| body.field76Name | String | 76番フィールドの名前 |
| body.field76Count | Integer | 76番フィールドの個数 |

76番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-77"></a>
### 保持セクション 77 { #preserve-sec-77 }

このセクションは preserve-existing e2e フィクスチャの 77 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field77Id | String | 77番フィールドの識別子 |
| body.field77Name | String | 77番フィールドの名前 |
| body.field77Count | Integer | 77番フィールドの個数 |

77番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-78"></a>
### 保持セクション 78 { #preserve-sec-78 }

このセクションは preserve-existing e2e フィクスチャの 78 番目のセクションです。以下の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field78Id | String | 78番フィールドの識別子 |
| body.field78Name | String | 78番フィールドの名前 |
| body.field78Count | Integer | 78番フィールドの個数 |

78番セクションの最後の説明文です。この文は、再翻訳の判定をライン単位で行う場合に使用されます。

<a id="preserve-sec-79"></a>
### 保存セクション 79 { #preserve-sec-79 }

このセクションは preserve-existing e2e フィクスチャの 79 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field79Id | String | 79 番フィールドの識別子 |
| body.field79Name | String | 79 番フィールドの名前 |
| body.field79Count | Integer | 79 番フィールドの個数 |

79 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-80"></a>
### 保存セクション 80 { #preserve-sec-80 }

このセクションは preserve-existing e2e フィクスチャの 80 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field80Id | String | 80 番フィールドの識別子 |
| body.field80Name | String | 80 番フィールドの名前 |
| body.field80Count | Integer | 80 番フィールドの個数 |

```json
{
  "field80Id": "id-80",
  "field80Name": "name-80"
}
```

80 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-81"></a>
### 保存セクション 81 { #preserve-sec-81 }

このセクションは preserve-existing e2e フィクスチャの 81 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field81Id | String | 81 番フィールドの識別子 |
| body.field81Name | String | 81 番フィールドの名前 |
| body.field81Count | Integer | 81 番フィールドの個数 |

81 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-82"></a>
### 保存セクション 82 { #preserve-sec-82 }

このセクションは preserve-existing e2e フィクスチャの 82 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field82Id | String | 82 番フィールドの識別子 |
| body.field82Name | String | 82 番フィールドの名前 |
| body.field82Count | Integer | 82 番フィールドの個数 |

82 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-83"></a>
### 保存セクション 83 { #preserve-sec-83 }

このセクションは preserve-existing e2e フィクスチャの 83 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field83Id | String | 83 番フィールドの識別子 |
| body.field83Name | String | 83 番フィールドの名前 |
| body.field83Count | Integer | 83 番フィールドの個数 |

83 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-84"></a>
### 保存セクション 84 { #preserve-sec-84 }

このセクションは preserve-existing e2e フィクスチャの 84 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field84Id | String | 84 番フィールドの識別子 |
| body.field84Name | String | 84 番フィールドの名前 |
| body.field84Count | Integer | 84 番フィールドの個数 |

84 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-85"></a>
### 保存セクション 85 { #preserve-sec-85 }

このセクションは preserve-existing e2e フィクスチャの 85 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field85Id | String | 85 番フィールドの識別子 |
| body.field85Name | String | 85 番フィールドの名前 |
| body.field85Count | Integer | 85 番フィールドの個数 |

```json
{
  "field85Id": "id-85",
  "field85Name": "name-85"
}
```

85 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-86"></a>
### 保存セクション 86 { #preserve-sec-86 }

このセクションは preserve-existing e2e フィクスチャの 86 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field86Id | String | 86 番フィールドの識別子 |
| body.field86Name | String | 86 番フィールドの名前 |
| body.field86Count | Integer | 86 番フィールドの個数 |

86 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-87"></a>
### 保存セクション 87 { #preserve-sec-87 }

このセクションは preserve-existing e2e フィクスチャの 87 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field87Id | String | 87 番フィールドの識別子 |
| body.field87Name | String | 87 番フィールドの名前 |
| body.field87Count | Integer | 87 番フィールドの個数 |

87 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-88"></a>
### 保存セクション 88 { #preserve-sec-88 }

このセクションは preserve-existing e2e フィクスチャの 88 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field88Id | String | 88 番フィールドの識別子 |
| body.field88Name | String | 88 番フィールドの名前 |
| body.field88Count | Integer | 88 番フィールドの個数 |

88 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-89"></a>
### 保存セクション 89 { #preserve-sec-89 }

このセクションは preserve-existing e2e フィクスチャの 89 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field89Id | String | 89 番フィールドの識別子 |
| body.field89Name | String | 89 番フィールドの名前 |
| body.field89Count | Integer | 89 番フィールドの個数 |

89 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-90"></a>
### 保存セクション 90 { #preserve-sec-90 }

このセクションは preserve-existing e2e フィクスチャの 90 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field90Id | String | 90 番フィールドの識別子 |
| body.field90Name | String | 90 番フィールドの名前 |
| body.field90Count | Integer | 90 番フィールドの個数 |

```json
{
  "field90Id": "id-90",
  "field90Name": "name-90"
}
```

90 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-91"></a>
### 保存セクション 91 { #preserve-sec-91 }

このセクションは preserve-existing e2e フィクスチャの 91 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field91Id | String | 91 番フィールドの識別子 |
| body.field91Name | String | 91 番フィールドの名前 |
| body.field91Count | Integer | 91 番フィールドの個数 |

91 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-92"></a>
### 保存セクション 92 { #preserve-sec-92 }

このセクションは preserve-existing e2e フィクスチャの 92 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field92Id | String | 92 番フィールドの識別子 |
| body.field92Name | String | 92 番フィールドの名前 |
| body.field92Count | Integer | 92 番フィールドの個数 |

92 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-93"></a>
### 保存セクション 93 { #preserve-sec-93 }

このセクションは preserve-existing e2e フィクスチャの 93 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field93Id | String | 93 番フィールドの識別子 |
| body.field93Name | String | 93 番フィールドの名前 |
| body.field93Count | Integer | 93 番フィールドの個数 |

93 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-94"></a>
### 保存セクション 94 { #preserve-sec-94 }

このセクションは preserve-existing e2e フィクスチャの 94 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field94Id | String | 94 番フィールドの識別子 |
| body.field94Name | String | 94 番フィールドの名前 |
| body.field94Count | Integer | 94 番フィールドの個数 |

94 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-95"></a>
### 保存セクション 95 { #preserve-sec-95 }

このセクションは preserve-existing e2e フィクスチャの 95 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field95Id | String | 95 番フィールドの識別子 |
| body.field95Name | String | 95 番フィールドの名前 |
| body.field95Count | Integer | 95 番フィールドの個数 |

```json
{
  "field95Id": "id-95",
  "field95Name": "name-95"
}
```

95 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-96"></a>
### 保存セクション 96 { #preserve-sec-96 }

このセクションは preserve-existing e2e フィクスチャの 96 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field96Id | String | 96 番フィールドの識別子 |
| body.field96Name | String | 96 番フィールドの名前 |
| body.field96Count | Integer | 96 番フィールドの個数 |

96 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-97"></a>
### 保存セクション 97 { #preserve-sec-97 }

このセクションは preserve-existing e2e フィクスチャの 97 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field97Id | String | 97 番フィールドの識別子 |
| body.field97Name | String | 97 番フィールドの名前 |
| body.field97Count | Integer | 97 番フィールドの個数 |

97 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-98"></a>
### 保存セクション 98 { #preserve-sec-98 }

このセクションは preserve-existing e2e フィクスチャの 98 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field98Id | String | 98 番フィールドの識別子 |
| body.field98Name | String | 98 番フィールドの名前 |
| body.field98Count | Integer | 98 番フィールドの個数 |

98 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-99"></a>
### 保存セクション 99 { #preserve-sec-99 }

このセクションは preserve-existing e2e フィクスチャの 99 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field99Id | String | 99 番フィールドの識別子 |
| body.field99Name | String | 99 番フィールドの名前 |
| body.field99Count | Integer | 99 番フィールドの個数 |

99 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-100"></a>
### 保存セクション 100 { #preserve-sec-100 }

このセクションは preserve-existing e2e フィクスチャの 100 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field100Id | String | 100 番フィールドの識別子 |
| body.field100Name | String | 100 番フィールドの名前 |
| body.field100Count | Integer | 100 番フィールドの個数 |

```json
{
  "field100Id": "id-100",
  "field100Name": "name-100"
}
```

100 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-101"></a>
### 保存セクション 101 { #preserve-sec-101 }

このセクションは preserve-existing e2e フィクスチャの 101 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field101Id | String | 101 番フィールドの識別子 |
| body.field101Name | String | 101 番フィールドの名前 |
| body.field101Count | Integer | 101 番フィールドの個数 |

101 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-102"></a>
### 保存セクション 102 { #preserve-sec-102 }

このセクションは preserve-existing e2e フィクスチャの 102 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field102Id | String | 102 番フィールドの識別子 |
| body.field102Name | String | 102 番フィールドの名前 |
| body.field102Count | Integer | 102 番フィールドの個数 |

102 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-103"></a>
### 保存セクション 103 { #preserve-sec-103 }

このセクションは preserve-existing e2e フィクスチャの 103 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field103Id | String | 103 番フィールドの識別子 |
| body.field103Name | String | 103 番フィールドの名前 |
| body.field103Count | Integer | 103 番フィールドの個数 |

103 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-104"></a>
### 保存セクション 104 { #preserve-sec-104 }

このセクションは preserve-existing e2e フィクスチャの 104 番目のセクションです。下記の表と例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field104Id | String | 104 番フィールドの識別子 |
| body.field104Name | String | 104 番フィールドの名前 |
| body.field104Count | Integer | 104 番フィールドの個数 |

104 番セクションの最後の説明文です。この文は再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-105"></a>
### 保存セクション 105 { #preserve-sec-105 }

このセクションは preserve-existing e2e フィクスチャの105番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field105Id | String | 105番フィールドの識別子 |
| body.field105Name | String | 105番フィールドの名前 |
| body.field105Count | Integer | 105番フィールドの個数 |

```json
{
  "field105Id": "id-105",
  "field105Name": "name-105"
}
```

105番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-106"></a>
### 保存セクション 106 { #preserve-sec-106 }

このセクションは preserve-existing e2e フィクスチャの106番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field106Id | String | 106番フィールドの識別子 |
| body.field106Name | String | 106番フィールドの名前 |
| body.field106Count | Integer | 106番フィールドの個数 |

106番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-107"></a>
### 保存セクション 107 { #preserve-sec-107 }

このセクションは preserve-existing e2e フィクスチャの107番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field107Id | String | 107番フィールドの識別子 |
| body.field107Name | String | 107番フィールドの名前 |
| body.field107Count | Integer | 107番フィールドの個数 |

107番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-108"></a>
### 保存セクション 108 { #preserve-sec-108 }

このセクションは preserve-existing e2e フィクスチャの108番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field108Id | String | 108番フィールドの識別子 |
| body.field108Name | String | 108番フィールドの名前 |
| body.field108Count | Integer | 108番フィールドの個数 |

108番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-109"></a>
### 保存セクション 109 { #preserve-sec-109 }

このセクションは preserve-existing e2e フィクスチャの109番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field109Id | String | 109番フィールドの識別子 |
| body.field109Name | String | 109番フィールドの名前 |
| body.field109Count | Integer | 109番フィールドの個数 |

109番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-110"></a>
### 保存セクション 110 { #preserve-sec-110 }

このセクションは preserve-existing e2e フィクスチャの110番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field110Id | String | 110番フィールドの識別子 |
| body.field110Name | String | 110番フィールドの名前 |
| body.field110Count | Integer | 110番フィールドの個数 |

```json
{
  "field110Id": "id-110",
  "field110Name": "name-110"
}
```

110番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-111"></a>
### 保存セクション 111 { #preserve-sec-111 }

このセクションは preserve-existing e2e フィクスチャの111番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field111Id | String | 111番フィールドの識別子 |
| body.field111Name | String | 111番フィールドの名前 |
| body.field111Count | Integer | 111番フィールドの個数 |

111番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-112"></a>
### 保存セクション 112 { #preserve-sec-112 }

このセクションは preserve-existing e2e フィクスチャの112番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field112Id | String | 112番フィールドの識別子 |
| body.field112Name | String | 112番フィールドの名前 |
| body.field112Count | Integer | 112番フィールドの個数 |

112番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-113"></a>
### 保存セクション 113 { #preserve-sec-113 }

このセクションは preserve-existing e2e フィクスチャの113番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field113Id | String | 113番フィールドの識別子 |
| body.field113Name | String | 113番フィールドの名前 |
| body.field113Count | Integer | 113番フィールドの個数 |

113番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-114"></a>
### 保存セクション 114 { #preserve-sec-114 }

このセクションは preserve-existing e2e フィクスチャの114番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field114Id | String | 114番フィールドの識別子 |
| body.field114Name | String | 114番フィールドの名前 |
| body.field114Count | Integer | 114番フィールドの個数 |

114番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-115"></a>
### 保存セクション 115 { #preserve-sec-115 }

このセクションは preserve-existing e2e フィクスチャの115番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field115Id | String | 115番フィールドの識別子 |
| body.field115Name | String | 115番フィールドの名前 |
| body.field115Count | Integer | 115番フィールドの個数 |

```json
{
  "field115Id": "id-115",
  "field115Name": "name-115"
}
```

115番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-116"></a>
### 保存セクション 116 { #preserve-sec-116 }

このセクションは preserve-existing e2e フィクスチャの116番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field116Id | String | 116番フィールドの識別子 |
| body.field116Name | String | 116番フィールドの名前 |
| body.field116Count | Integer | 116番フィールドの個数 |

116番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-117"></a>
### 保存セクション 117 { #preserve-sec-117 }

このセクションは preserve-existing e2e フィクスチャの117番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field117Id | String | 117番フィールドの識別子 |
| body.field117Name | String | 117番フィールドの名前 |
| body.field117Count | Integer | 117番フィールドの個数 |

117番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-118"></a>
### 保存セクション 118 { #preserve-sec-118 }

このセクションは preserve-existing e2e フィクスチャの118番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field118Id | String | 118番フィールドの識別子 |
| body.field118Name | String | 118番フィールドの名前 |
| body.field118Count | Integer | 118番フィールドの個数 |

118番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-119"></a>
### 保存セクション 119 { #preserve-sec-119 }

このセクションは preserve-existing e2e フィクスチャの119番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field119Id | String | 119番フィールドの識別子 |
| body.field119Name | String | 119番フィールドの名前 |
| body.field119Count | Integer | 119番フィールドの個数 |

119番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-120"></a>
### 保存セクション 120 { #preserve-sec-120 }

このセクションは preserve-existing e2e フィクスチャの120番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field120Id | String | 120番フィールドの識別子 |
| body.field120Name | String | 120番フィールドの名前 |
| body.field120Count | Integer | 120番フィールドの個数 |

```json
{
  "field120Id": "id-120",
  "field120Name": "name-120"
}
```

120番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-121"></a>
### 保存セクション 121 { #preserve-sec-121 }

このセクションは preserve-existing e2e フィクスチャの121番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field121Id | String | 121番フィールドの識別子 |
| body.field121Name | String | 121番フィールドの名前 |
| body.field121Count | Integer | 121番フィールドの個数 |

121番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-122"></a>
### 保存セクション 122 { #preserve-sec-122 }

このセクションは preserve-existing e2e フィクスチャの122番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field122Id | String | 122番フィールドの識別子 |
| body.field122Name | String | 122番フィールドの名前 |
| body.field122Count | Integer | 122番フィールドの個数 |

122番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-123"></a>
### 保存セクション 123 { #preserve-sec-123 }

このセクションは preserve-existing e2e フィクスチャの123番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field123Id | String | 123番フィールドの識別子 |
| body.field123Name | String | 123番フィールドの名前 |
| body.field123Count | Integer | 123番フィールドの個数 |

123番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-124"></a>
### 保存セクション 124 { #preserve-sec-124 }

このセクションは preserve-existing e2e フィクスチャの124番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field124Id | String | 124番フィールドの識別子 |
| body.field124Name | String | 124番フィールドの名前 |
| body.field124Count | Integer | 124番フィールドの個数 |

124番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-125"></a>
### 保存セクション 125 { #preserve-sec-125 }

このセクションは preserve-existing e2e フィクスチャの125番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field125Id | String | 125番フィールドの識別子 |
| body.field125Name | String | 125番フィールドの名前 |
| body.field125Count | Integer | 125番フィールドの個数 |

```json
{
  "field125Id": "id-125",
  "field125Name": "name-125"
}
```

125番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-126"></a>
### 保存セクション 126 { #preserve-sec-126 }

このセクションは preserve-existing e2e フィクスチャの126番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field126Id | String | 126番フィールドの識別子 |
| body.field126Name | String | 126番フィールドの名前 |
| body.field126Count | Integer | 126番フィールドの個数 |

126番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-127"></a>
### 保存セクション 127 { #preserve-sec-127 }

このセクションは preserve-existing e2e フィクスチャの127番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field127Id | String | 127番フィールドの識別子 |
| body.field127Name | String | 127番フィールドの名前 |
| body.field127Count | Integer | 127番フィールドの個数 |

127番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-128"></a>
### 保存セクション 128 { #preserve-sec-128 }

このセクションは preserve-existing e2e フィクスチャの128番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field128Id | String | 128番フィールドの識別子 |
| body.field128Name | String | 128番フィールドの名前 |
| body.field128Count | Integer | 128番フィールドの個数 |

128番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-129"></a>
### 保存セクション 129 { #preserve-sec-129 }

このセクションは preserve-existing e2e フィクスチャの129番目のセクションです。下記のテーブルと例は、セクションごとにフィールド名が異なるため、互いに区別できます。

| 値 | タイプ | 説明 |
|---|---|---|
| body.field129Id | String | 129番フィールドの識別子 |
| body.field129Name | String | 129番フィールドの名前 |
| body.field129Count | Integer | 129番フィールドの個数 |

129番セクションの最後の説明文です。この文は再翻訳の是非を行単位で判定する際に使用されます。

<a id="preserve-sec-130"></a>
### 保存セクション 130 { #preserve-sec-130 }

このセクションは preserve-existing e2e フィクスチャの 130 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field130Id | String | フィールド 130 の識別子 |
| body.field130Name | String | フィールド 130 の名前 |
| body.field130Count | Integer | フィールド 130 の個数 |

```json
{
  "field130Id": "id-130",
  "field130Name": "name-130"
}
```

フィールド 130 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-131"></a>
### 保存セクション 131 { #preserve-sec-131 }

このセクションは preserve-existing e2e フィクスチャの 131 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field131Id | String | フィールド 131 の識別子 |
| body.field131Name | String | フィールド 131 の名前 |
| body.field131Count | Integer | フィールド 131 の個数 |

フィールド 131 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-132"></a>
### 保存セクション 132 { #preserve-sec-132 }

このセクションは preserve-existing e2e フィクスチャの 132 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field132Id | String | フィールド 132 の識別子 |
| body.field132Name | String | フィールド 132 の名前 |
| body.field132Count | Integer | フィールド 132 の個数 |

フィールド 132 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-133"></a>
### 保存セクション 133 { #preserve-sec-133 }

このセクションは preserve-existing e2e フィクスチャの 133 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field133Id | String | フィールド 133 の識別子 |
| body.field133Name | String | フィールド 133 の名前 |
| body.field133Count | Integer | フィールド 133 の個数 |

フィールド 133 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-134"></a>
### 保存セクション 134 { #preserve-sec-134 }

このセクションは preserve-existing e2e フィクスチャの 134 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field134Id | String | フィールド 134 の識別子 |
| body.field134Name | String | フィールド 134 の名前 |
| body.field134Count | Integer | フィールド 134 の個数 |

フィールド 134 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-135"></a>
### 保存セクション 135 { #preserve-sec-135 }

このセクションは preserve-existing e2e フィクスチャの 135 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field135Id | String | フィールド 135 の識別子 |
| body.field135Name | String | フィールド 135 の名前 |
| body.field135Count | Integer | フィールド 135 の個数 |

```json
{
  "field135Id": "id-135",
  "field135Name": "name-135"
}
```

フィールド 135 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-136"></a>
### 保存セクション 136 { #preserve-sec-136 }

このセクションは preserve-existing e2e フィクスチャの 136 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field136Id | String | フィールド 136 の識別子 |
| body.field136Name | String | フィールド 136 の名前 |
| body.field136Count | Integer | フィールド 136 の個数 |

フィールド 136 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-137"></a>
### 保存セクション 137 { #preserve-sec-137 }

このセクションは preserve-existing e2e フィクスチャの 137 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field137Id | String | フィールド 137 の識別子 |
| body.field137Name | String | フィールド 137 の名前 |
| body.field137Count | Integer | フィールド 137 の個数 |

フィールド 137 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-138"></a>
### 保存セクション 138 { #preserve-sec-138 }

このセクションは preserve-existing e2e フィクスチャの 138 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field138Id | String | フィールド 138 の識別子 |
| body.field138Name | String | フィールド 138 の名前 |
| body.field138Count | Integer | フィールド 138 の個数 |

フィールド 138 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-139"></a>
### 保存セクション 139 { #preserve-sec-139 }

このセクションは preserve-existing e2e フィクスチャの 139 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field139Id | String | フィールド 139 の識別子 |
| body.field139Name | String | フィールド 139 の名前 |
| body.field139Count | Integer | フィールド 139 の個数 |

フィールド 139 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-140"></a>
### 保存セクション 140 { #preserve-sec-140 }

このセクションは preserve-existing e2e フィクスチャの 140 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field140Id | String | フィールド 140 の識別子 |
| body.field140Name | String | フィールド 140 の名前 |
| body.field140Count | Integer | フィールド 140 の個数 |

```json
{
  "field140Id": "id-140",
  "field140Name": "name-140"
}
```

フィールド 140 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-141"></a>
### 保存セクション 141 { #preserve-sec-141 }

このセクションは preserve-existing e2e フィクスチャの 141 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field141Id | String | フィールド 141 の識別子 |
| body.field141Name | String | フィールド 141 の名前 |
| body.field141Count | Integer | フィールド 141 の個数 |

フィールド 141 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-142"></a>
### 保存セクション 142 { #preserve-sec-142 }

このセクションは preserve-existing e2e フィクスチャの 142 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field142Id | String | フィールド 142 の識別子 |
| body.field142Name | String | フィールド 142 の名前 |
| body.field142Count | Integer | フィールド 142 の個数 |

フィールド 142 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-143"></a>
### 保存セクション 143 { #preserve-sec-143 }

このセクションは preserve-existing e2e フィクスチャの 143 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field143Id | String | フィールド 143 の識別子 |
| body.field143Name | String | フィールド 143 の名前 |
| body.field143Count | Integer | フィールド 143 の個数 |

フィールド 143 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-144"></a>
### 保存セクション 144 { #preserve-sec-144 }

このセクションは preserve-existing e2e フィクスチャの 144 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field144Id | String | フィールド 144 の識別子 |
| body.field144Name | String | フィールド 144 の名前 |
| body.field144Count | Integer | フィールド 144 の個数 |

フィールド 144 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-145"></a>
### 保存セクション 145 { #preserve-sec-145 }

このセクションは preserve-existing e2e フィクスチャの 145 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field145Id | String | フィールド 145 の識別子 |
| body.field145Name | String | フィールド 145 の名前 |
| body.field145Count | Integer | フィールド 145 の個数 |

```json
{
  "field145Id": "id-145",
  "field145Name": "name-145"
}
```

フィールド 145 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-146"></a>
### 保存セクション 146 { #preserve-sec-146 }

このセクションは preserve-existing e2e フィクスチャの 146 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field146Id | String | フィールド 146 の識別子 |
| body.field146Name | String | フィールド 146 の名前 |
| body.field146Count | Integer | フィールド 146 の個数 |

フィールド 146 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-147"></a>
### 保存セクション 147 { #preserve-sec-147 }

このセクションは preserve-existing e2e フィクスチャの 147 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field147Id | String | フィールド 147 の識別子 |
| body.field147Name | String | フィールド 147 の名前 |
| body.field147Count | Integer | フィールド 147 の個数 |

フィールド 147 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-148"></a>
### 保存セクション 148 { #preserve-sec-148 }

このセクションは preserve-existing e2e フィクスチャの 148 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field148Id | String | フィールド 148 の識別子 |
| body.field148Name | String | フィールド 148 の名前 |
| body.field148Count | Integer | フィールド 148 の個数 |

フィールド 148 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-149"></a>
### 保存セクション 149 { #preserve-sec-149 }

このセクションは preserve-existing e2e フィクスチャの 149 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field149Id | String | フィールド 149 の識別子 |
| body.field149Name | String | フィールド 149 の名前 |
| body.field149Count | Integer | フィールド 149 の個数 |

フィールド 149 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-150"></a>
### 保存セクション 150 { #preserve-sec-150 }

このセクションは preserve-existing e2e フィクスチャの 150 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field150Id | String | フィールド 150 の識別子 |
| body.field150Name | String | フィールド 150 の名前 |
| body.field150Count | Integer | フィールド 150 の個数 |

```json
{
  "field150Id": "id-150",
  "field150Name": "name-150"
}
```

フィールド 150 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-151"></a>
### 保存セクション 151 { #preserve-sec-151 }

このセクションは preserve-existing e2e フィクスチャの 151 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field151Id | String | フィールド 151 の識別子 |
| body.field151Name | String | フィールド 151 の名前 |
| body.field151Count | Integer | フィールド 151 の個数 |

フィールド 151 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-152"></a>
### 保存セクション 152 { #preserve-sec-152 }

このセクションは preserve-existing e2e フィクスチャの 152 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field152Id | String | フィールド 152 の識別子 |
| body.field152Name | String | フィールド 152 の名前 |
| body.field152Count | Integer | フィールド 152 の個数 |

フィールド 152 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-153"></a>
### 保存セクション 153 { #preserve-sec-153 }

このセクションは preserve-existing e2e フィクスチャの 153 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field153Id | String | フィールド 153 の識別子 |
| body.field153Name | String | フィールド 153 の名前 |
| body.field153Count | Integer | フィールド 153 の個数 |

フィールド 153 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-154"></a>
### 保存セクション 154 { #preserve-sec-154 }

このセクションは preserve-existing e2e フィクスチャの 154 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field154Id | String | フィールド 154 の識別子 |
| body.field154Name | String | フィールド 154 の名前 |
| body.field154Count | Integer | フィールド 154 の個数 |

フィールド 154 セクションの最後の説明文です。この文は行単位で再翻訳の有無を判定する際に使用されます。

<a id="preserve-sec-155"></a>
### 保存セクション 155 { #preserve-sec-155 }

このセクションは preserve-existing e2e フィクスチャーの 155 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field155Id | String | 155 番フィールドの識別子 |
| body.field155Name | String | 155 番フィールドの名前 |
| body.field155Count | Integer | 155 番フィールドの個数 |

```json
{
  "field155Id": "id-155",
  "field155Name": "name-155"
}
```

155 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-156"></a>
### 保存セクション 156 { #preserve-sec-156 }

このセクションは preserve-existing e2e フィクスチャーの 156 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field156Id | String | 156 番フィールドの識別子 |
| body.field156Name | String | 156 番フィールドの名前 |
| body.field156Count | Integer | 156 番フィールドの個数 |

156 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-157"></a>
### 保存セクション 157 { #preserve-sec-157 }

このセクションは preserve-existing e2e フィクスチャーの 157 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field157Id | String | 157 番フィールドの識別子 |
| body.field157Name | String | 157 番フィールドの名前 |
| body.field157Count | Integer | 157 番フィールドの個数 |

157 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-158"></a>
### 保存セクション 158 { #preserve-sec-158 }

このセクションは preserve-existing e2e フィクスチャーの 158 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field158Id | String | 158 番フィールドの識別子 |
| body.field158Name | String | 158 番フィールドの名前 |
| body.field158Count | Integer | 158 番フィールドの個数 |

158 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-159"></a>
### 保存セクション 159 { #preserve-sec-159 }

このセクションは preserve-existing e2e フィクスチャーの 159 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field159Id | String | 159 番フィールドの識別子 |
| body.field159Name | String | 159 番フィールドの名前 |
| body.field159Count | Integer | 159 番フィールドの個数 |

159 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-160"></a>
### 保存セクション 160 { #preserve-sec-160 }

このセクションは preserve-existing e2e フィクスチャーの 160 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field160Id | String | 160 番フィールドの識別子 |
| body.field160Name | String | 160 番フィールドの名前 |
| body.field160Count | Integer | 160 番フィールドの個数 |

```json
{
  "field160Id": "id-160",
  "field160Name": "name-160"
}
```

160 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-161"></a>
### 保存セクション 161 { #preserve-sec-161 }

このセクションは preserve-existing e2e フィクスチャーの 161 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field161Id | String | 161 番フィールドの識別子 |
| body.field161Name | String | 161 番フィールドの名前 |
| body.field161Count | Integer | 161 番フィールドの個数 |

161 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-162"></a>
### 保存セクション 162 { #preserve-sec-162 }

このセクションは preserve-existing e2e フィクスチャーの 162 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field162Id | String | 162 番フィールドの識別子 |
| body.field162Name | String | 162 番フィールドの名前 |
| body.field162Count | Integer | 162 番フィールドの個数 |

162 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-163"></a>
### 保存セクション 163 { #preserve-sec-163 }

このセクションは preserve-existing e2e フィクスチャーの 163 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field163Id | String | 163 番フィールドの識別子 |
| body.field163Name | String | 163 番フィールドの名前 |
| body.field163Count | Integer | 163 番フィールドの個数 |

163 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-164"></a>
### 保存セクション 164 { #preserve-sec-164 }

このセクションは preserve-existing e2e フィクスチャーの 164 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field164Id | String | 164 番フィールドの識別子 |
| body.field164Name | String | 164 番フィールドの名前 |
| body.field164Count | Integer | 164 番フィールドの個数 |

164 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-165"></a>
### 保存セクション 165 { #preserve-sec-165 }

このセクションは preserve-existing e2e フィクスチャーの 165 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field165Id | String | 165 番フィールドの識別子 |
| body.field165Name | String | 165 番フィールドの名前 |
| body.field165Count | Integer | 165 番フィールドの個数 |

```json
{
  "field165Id": "id-165",
  "field165Name": "name-165"
}
```

165 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-166"></a>
### 保存セクション 166 { #preserve-sec-166 }

このセクションは preserve-existing e2e フィクスチャーの 166 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field166Id | String | 166 番フィールドの識別子 |
| body.field166Name | String | 166 番フィールドの名前 |
| body.field166Count | Integer | 166 番フィールドの個数 |

166 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-167"></a>
### 保存セクション 167 { #preserve-sec-167 }

このセクションは preserve-existing e2e フィクスチャーの 167 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field167Id | String | 167 番フィールドの識別子 |
| body.field167Name | String | 167 番フィールドの名前 |
| body.field167Count | Integer | 167 番フィールドの個数 |

167 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-168"></a>
### 保存セクション 168 { #preserve-sec-168 }

このセクションは preserve-existing e2e フィクスチャーの 168 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field168Id | String | 168 番フィールドの識別子 |
| body.field168Name | String | 168 番フィールドの名前 |
| body.field168Count | Integer | 168 番フィールドの個数 |

168 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-169"></a>
### 保存セクション 169 { #preserve-sec-169 }

このセクションは preserve-existing e2e フィクスチャーの 169 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field169Id | String | 169 番フィールドの識別子 |
| body.field169Name | String | 169 番フィールドの名前 |
| body.field169Count | Integer | 169 番フィールドの個数 |

169 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-170"></a>
### 保存セクション 170 { #preserve-sec-170 }

このセクションは preserve-existing e2e フィクスチャーの 170 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field170Id | String | 170 番フィールドの識別子 |
| body.field170Name | String | 170 番フィールドの名前 |
| body.field170Count | Integer | 170 番フィールドの個数 |

```json
{
  "field170Id": "id-170",
  "field170Name": "name-170"
}
```

170 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-171"></a>
### 保存セクション 171 { #preserve-sec-171 }

このセクションは preserve-existing e2e フィクスチャーの 171 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field171Id | String | 171 番フィールドの識別子 |
| body.field171Name | String | 171 番フィールドの名前 |
| body.field171Count | Integer | 171 番フィールドの個数 |

171 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-172"></a>
### 保存セクション 172 { #preserve-sec-172 }

このセクションは preserve-existing e2e フィクスチャーの 172 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field172Id | String | 172 番フィールドの識別子 |
| body.field172Name | String | 172 番フィールドの名前 |
| body.field172Count | Integer | 172 番フィールドの個数 |

172 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-173"></a>
### 保存セクション 173 { #preserve-sec-173 }

このセクションは preserve-existing e2e フィクスチャーの 173 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field173Id | String | 173 番フィールドの識別子 |
| body.field173Name | String | 173 番フィールドの名前 |
| body.field173Count | Integer | 173 番フィールドの個数 |

173 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-174"></a>
### 保存セクション 174 { #preserve-sec-174 }

このセクションは preserve-existing e2e フィクスチャーの 174 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field174Id | String | 174 番フィールドの識別子 |
| body.field174Name | String | 174 番フィールドの名前 |
| body.field174Count | Integer | 174 番フィールドの個数 |

174 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-175"></a>
### 保存セクション 175 { #preserve-sec-175 }

このセクションは preserve-existing e2e フィクスチャーの 175 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field175Id | String | 175 番フィールドの識別子 |
| body.field175Name | String | 175 番フィールドの名前 |
| body.field175Count | Integer | 175 番フィールドの個数 |

```json
{
  "field175Id": "id-175",
  "field175Name": "name-175"
}
```

175 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-176"></a>
### 保存セクション 176 { #preserve-sec-176 }

このセクションは preserve-existing e2e フィクスチャーの 176 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field176Id | String | 176 番フィールドの識別子 |
| body.field176Name | String | 176 番フィールドの名前 |
| body.field176Count | Integer | 176 番フィールドの個数 |

176 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-177"></a>
### 保存セクション 177 { #preserve-sec-177 }

このセクションは preserve-existing e2e フィクスチャーの 177 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field177Id | String | 177 番フィールドの識別子 |
| body.field177Name | String | 177 番フィールドの名前 |
| body.field177Count | Integer | 177 番フィールドの個数 |

177 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-178"></a>
### 保存セクション 178 { #preserve-sec-178 }

このセクションは preserve-existing e2e フィクスチャーの 178 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field178Id | String | 178 番フィールドの識別子 |
| body.field178Name | String | 178 番フィールドの名前 |
| body.field178Count | Integer | 178 番フィールドの個数 |

178 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-179"></a>
### 保存セクション 179 { #preserve-sec-179 }

このセクションは preserve-existing e2e フィクスチャーの 179 番目のセクションです。以下の表とサンプルは、セクションごとに異なるフィールド名を持ち、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field179Id | String | 179 番フィールドの識別子 |
| body.field179Name | String | 179 番フィールドの名前 |
| body.field179Count | Integer | 179 番フィールドの個数 |

179 番セクションの最後の説明文です。この文は、再翻訳の有無を行単位で判定する際に使用されます。

<a id="preserve-sec-180"></a>
### 保存セクション 180 { #preserve-sec-180 }

このセクションは preserve-existing e2e フィクスチャーの 180 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field180Id | String | 180 番フィールドの識別子 |
| body.field180Name | String | 180 番フィールドの名前 |
| body.field180Count | Integer | 180 番フィールドの個数 |

```json
{
  "field180Id": "id-180",
  "field180Name": "name-180"
}
```

180 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-181"></a>
### 保存セクション 181 { #preserve-sec-181 }

このセクションは preserve-existing e2e フィクスチャーの 181 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field181Id | String | 181 番フィールドの識別子 |
| body.field181Name | String | 181 番フィールドの名前 |
| body.field181Count | Integer | 181 番フィールドの個数 |

181 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-182"></a>
### 保存セクション 182 { #preserve-sec-182 }

このセクションは preserve-existing e2e フィクスチャーの 182 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field182Id | String | 182 番フィールドの識別子 |
| body.field182Name | String | 182 番フィールドの名前 |
| body.field182Count | Integer | 182 番フィールドの個数 |

182 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-183"></a>
### 保存セクション 183 { #preserve-sec-183 }

このセクションは preserve-existing e2e フィクスチャーの 183 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field183Id | String | 183 番フィールドの識別子 |
| body.field183Name | String | 183 番フィールドの名前 |
| body.field183Count | Integer | 183 番フィールドの個数 |

183 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-184"></a>
### 保存セクション 184 { #preserve-sec-184 }

このセクションは preserve-existing e2e フィクスチャーの 184 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field184Id | String | 184 番フィールドの識別子 |
| body.field184Name | String | 184 番フィールドの名前 |
| body.field184Count | Integer | 184 番フィールドの個数 |

184 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-185"></a>
### 保存セクション 185 { #preserve-sec-185 }

このセクションは preserve-existing e2e フィクスチャーの 185 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field185Id | String | 185 番フィールドの識別子 |
| body.field185Name | String | 185 番フィールドの名前 |
| body.field185Count | Integer | 185 番フィールドの個数 |

```json
{
  "field185Id": "id-185",
  "field185Name": "name-185"
}
```

185 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-186"></a>
### 保存セクション 186 { #preserve-sec-186 }

このセクションは preserve-existing e2e フィクスチャーの 186 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field186Id | String | 186 番フィールドの識別子 |
| body.field186Name | String | 186 番フィールドの名前 |
| body.field186Count | Integer | 186 番フィールドの個数 |

186 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-187"></a>
### 保存セクション 187 { #preserve-sec-187 }

このセクションは preserve-existing e2e フィクスチャーの 187 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field187Id | String | 187 番フィールドの識別子 |
| body.field187Name | String | 187 番フィールドの名前 |
| body.field187Count | Integer | 187 番フィールドの個数 |

187 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-188"></a>
### 保存セクション 188 { #preserve-sec-188 }

このセクションは preserve-existing e2e フィクスチャーの 188 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field188Id | String | 188 番フィールドの識別子 |
| body.field188Name | String | 188 番フィールドの名前 |
| body.field188Count | Integer | 188 番フィールドの個数 |

188 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-189"></a>
### 保存セクション 189 { #preserve-sec-189 }

このセクションは preserve-existing e2e フィクスチャーの 189 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field189Id | String | 189 番フィールドの識別子 |
| body.field189Name | String | 189 番フィールドの名前 |
| body.field189Count | Integer | 189 番フィールドの個数 |

189 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-190"></a>
### 保存セクション 190 { #preserve-sec-190 }

このセクションは preserve-existing e2e フィクスチャーの 190 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field190Id | String | 190 番フィールドの識別子 |
| body.field190Name | String | 190 番フィールドの名前 |
| body.field190Count | Integer | 190 番フィールドの個数 |

```json
{
  "field190Id": "id-190",
  "field190Name": "name-190"
}
```

190 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-191"></a>
### 保存セクション 191 { #preserve-sec-191 }

このセクションは preserve-existing e2e フィクスチャーの 191 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field191Id | String | 191 番フィールドの識別子 |
| body.field191Name | String | 191 番フィールドの名前 |
| body.field191Count | Integer | 191 番フィールドの個数 |

191 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-192"></a>
### 保存セクション 192 { #preserve-sec-192 }

このセクションは preserve-existing e2e フィクスチャーの 192 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field192Id | String | 192 番フィールドの識別子 |
| body.field192Name | String | 192 番フィールドの名前 |
| body.field192Count | Integer | 192 番フィールドの個数 |

192 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-193"></a>
### 保存セクション 193 { #preserve-sec-193 }

このセクションは preserve-existing e2e フィクスチャーの 193 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field193Id | String | 193 番フィールドの識別子 |
| body.field193Name | String | 193 番フィールドの名前 |
| body.field193Count | Integer | 193 番フィールドの個数 |

193 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-194"></a>
### 保存セクション 194 { #preserve-sec-194 }

このセクションは preserve-existing e2e フィクスチャーの 194 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field194Id | String | 194 番フィールドの識別子 |
| body.field194Name | String | 194 番フィールドの名前 |
| body.field194Count | Integer | 194 番フィールドの個数 |

194 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-195"></a>
### 保存セクション 195 { #preserve-sec-195 }

このセクションは preserve-existing e2e フィクスチャーの 195 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field195Id | String | 195 番フィールドの識別子 |
| body.field195Name | String | 195 番フィールドの名前 |
| body.field195Count | Integer | 195 番フィールドの個数 |

```json
{
  "field195Id": "id-195",
  "field195Name": "name-195"
}
```

195 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-196"></a>
### 保存セクション 196 { #preserve-sec-196 }

このセクションは preserve-existing e2e フィクスチャーの 196 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field196Id | String | 196 番フィールドの識別子 |
| body.field196Name | String | 196 番フィールドの名前 |
| body.field196Count | Integer | 196 番フィールドの個数 |

196 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-197"></a>
### 保存セクション 197 { #preserve-sec-197 }

このセクションは preserve-existing e2e フィクスチャーの 197 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field197Id | String | 197 番フィールドの識別子 |
| body.field197Name | String | 197 番フィールドの名前 |
| body.field197Count | Integer | 197 番フィールドの個数 |

197 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-198"></a>
### 保存セクション 198 { #preserve-sec-198 }

このセクションは preserve-existing e2e フィクスチャーの 198 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field198Id | String | 198 番フィールドの識別子 |
| body.field198Name | String | 198 番フィールドの名前 |
| body.field198Count | Integer | 198 番フィールドの個数 |

198 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-199"></a>
### 保存セクション 199 { #preserve-sec-199 }

このセクションは preserve-existing e2e フィクスチャーの 199 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field199Id | String | 199 番フィールドの識別子 |
| body.field199Name | String | 199 番フィールドの名前 |
| body.field199Count | Integer | 199 番フィールドの個数 |

199 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-200"></a>
### 保存セクション 200 { #preserve-sec-200 }

このセクションは preserve-existing e2e フィクスチャーの 200 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field200Id | String | 200 番フィールドの識別子 |
| body.field200Name | String | 200 番フィールドの名前 |
| body.field200Count | Integer | 200 番フィールドの個数 |

```json
{
  "field200Id": "id-200",
  "field200Name": "name-200"
}
```

200 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-201"></a>
### 保存セクション 201 { #preserve-sec-201 }

このセクションは preserve-existing e2e フィクスチャーの 201 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field201Id | String | 201 番フィールドの識別子 |
| body.field201Name | String | 201 番フィールドの名前 |
| body.field201Count | Integer | 201 番フィールドの個数 |

201 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-202"></a>
### 保存セクション 202 { #preserve-sec-202 }

このセクションは preserve-existing e2e フィクスチャーの 202 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field202Id | String | 202 番フィールドの識別子 |
| body.field202Name | String | 202 番フィールドの名前 |
| body.field202Count | Integer | 202 番フィールドの個数 |

202 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-203"></a>
### 保存セクション 203 { #preserve-sec-203 }

このセクションは preserve-existing e2e フィクスチャーの 203 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field203Id | String | 203 番フィールドの識別子 |
| body.field203Name | String | 203 番フィールドの名前 |
| body.field203Count | Integer | 203 番フィールドの個数 |

203 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-204"></a>
### 保存セクション 204 { #preserve-sec-204 }

このセクションは preserve-existing e2e フィクスチャーの 204 番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、互いに区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field204Id | String | 204 番フィールドの識別子 |
| body.field204Name | String | 204 番フィールドの名前 |
| body.field204Count | Integer | 204 番フィールドの個数 |

204 番セクションの最後の説明文です。この文は、行単位での再翻訳の有無を判定するときに使用されます。

<a id="preserve-sec-205"></a>
### 保存セクション 205 { #preserve-sec-205 }

このセクションはpreserve-existing e2eフィクスチャの205番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field205Id | String | 205番フィールドの識別子 |
| body.field205Name | String | 205番フィールドの名前 |
| body.field205Count | Integer | 205番フィールドの個数 |

```json
{
  "field205Id": "id-205",
  "field205Name": "name-205"
}
```

205番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-206"></a>
### 保存セクション 206 { #preserve-sec-206 }

このセクションはpreserve-existing e2eフィクスチャの206番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field206Id | String | 206番フィールドの識別子 |
| body.field206Name | String | 206番フィールドの名前 |
| body.field206Count | Integer | 206番フィールドの個数 |

206番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-207"></a>
### 保存セクション 207 { #preserve-sec-207 }

このセクションはpreserve-existing e2eフィクスチャの207番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field207Id | String | 207番フィールドの識別子 |
| body.field207Name | String | 207番フィールドの名前 |
| body.field207Count | Integer | 207番フィールドの個数 |

207番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-208"></a>
### 保存セクション 208 { #preserve-sec-208 }

このセクションはpreserve-existing e2eフィクスチャの208番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field208Id | String | 208番フィールドの識別子 |
| body.field208Name | String | 208番フィールドの名前 |
| body.field208Count | Integer | 208番フィールドの個数 |

208番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-209"></a>
### 保存セクション 209 { #preserve-sec-209 }

このセクションはpreserve-existing e2eフィクスチャの209番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field209Id | String | 209番フィールドの識別子 |
| body.field209Name | String | 209番フィールドの名前 |
| body.field209Count | Integer | 209番フィールドの個数 |

209番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-210"></a>
### 保存セクション 210 { #preserve-sec-210 }

このセクションはpreserve-existing e2eフィクスチャの210番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field210Id | String | 210番フィールドの識別子 |
| body.field210Name | String | 210番フィールドの名前 |
| body.field210Count | Integer | 210番フィールドの個数 |

```json
{
  "field210Id": "id-210",
  "field210Name": "name-210"
}
```

210番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-211"></a>
### 保存セクション 211 { #preserve-sec-211 }

このセクションはpreserve-existing e2eフィクスチャの211番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field211Id | String | 211番フィールドの識別子 |
| body.field211Name | String | 211番フィールドの名前 |
| body.field211Count | Integer | 211番フィールドの個数 |

211番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-212"></a>
### 保存セクション 212 { #preserve-sec-212 }

このセクションはpreserve-existing e2eフィクスチャの212番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field212Id | String | 212番フィールドの識別子 |
| body.field212Name | String | 212番フィールドの名前 |
| body.field212Count | Integer | 212番フィールドの個数 |

212番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-213"></a>
### 保存セクション 213 { #preserve-sec-213 }

このセクションはpreserve-existing e2eフィクスチャの213番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field213Id | String | 213番フィールドの識別子 |
| body.field213Name | String | 213番フィールドの名前 |
| body.field213Count | Integer | 213番フィールドの個数 |

213番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-214"></a>
### 保存セクション 214 { #preserve-sec-214 }

このセクションはpreserve-existing e2eフィクスチャの214番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field214Id | String | 214番フィールドの識別子 |
| body.field214Name | String | 214番フィールドの名前 |
| body.field214Count | Integer | 214番フィールドの個数 |

214番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-215"></a>
### 保存セクション 215 { #preserve-sec-215 }

このセクションはpreserve-existing e2eフィクスチャの215番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field215Id | String | 215番フィールドの識別子 |
| body.field215Name | String | 215番フィールドの名前 |
| body.field215Count | Integer | 215番フィールドの個数 |

```json
{
  "field215Id": "id-215",
  "field215Name": "name-215"
}
```

215番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-216"></a>
### 保存セクション 216 { #preserve-sec-216 }

このセクションはpreserve-existing e2eフィクスチャの216番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field216Id | String | 216番フィールドの識別子 |
| body.field216Name | String | 216番フィールドの名前 |
| body.field216Count | Integer | 216番フィールドの個数 |

216番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-217"></a>
### 保存セクション 217 { #preserve-sec-217 }

このセクションはpreserve-existing e2eフィクスチャの217番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field217Id | String | 217番フィールドの識別子 |
| body.field217Name | String | 217番フィールドの名前 |
| body.field217Count | Integer | 217番フィールドの個数 |

217番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-218"></a>
### 保存セクション 218 { #preserve-sec-218 }

このセクションはpreserve-existing e2eフィクスチャの218番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field218Id | String | 218番フィールドの識別子 |
| body.field218Name | String | 218番フィールドの名前 |
| body.field218Count | Integer | 218番フィールドの個数 |

218番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-219"></a>
### 保存セクション 219 { #preserve-sec-219 }

このセクションはpreserve-existing e2eフィクスチャの219番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field219Id | String | 219番フィールドの識別子 |
| body.field219Name | String | 219番フィールドの名前 |
| body.field219Count | Integer | 219番フィールドの個数 |

219番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-220"></a>
### 保存セクション 220 { #preserve-sec-220 }

このセクションはpreserve-existing e2eフィクスチャの220番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field220Id | String | 220番フィールドの識別子 |
| body.field220Name | String | 220番フィールドの名前 |
| body.field220Count | Integer | 220番フィールドの個数 |

```json
{
  "field220Id": "id-220",
  "field220Name": "name-220"
}
```

220番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-221"></a>
### 保存セクション 221 { #preserve-sec-221 }

このセクションはpreserve-existing e2eフィクスチャの221番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field221Id | String | 221番フィールドの識別子 |
| body.field221Name | String | 221番フィールドの名前 |
| body.field221Count | Integer | 221番フィールドの個数 |

221番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-222"></a>
### 保存セクション 222 { #preserve-sec-222 }

このセクションはpreserve-existing e2eフィクスチャの222番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field222Id | String | 222番フィールドの識別子 |
| body.field222Name | String | 222番フィールドの名前 |
| body.field222Count | Integer | 222番フィールドの個数 |

222番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-223"></a>
### 保存セクション 223 { #preserve-sec-223 }

このセクションはpreserve-existing e2eフィクスチャの223番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field223Id | String | 223番フィールドの識別子 |
| body.field223Name | String | 223番フィールドの名前 |
| body.field223Count | Integer | 223番フィールドの個数 |

223番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-224"></a>
### 保存セクション 224 { #preserve-sec-224 }

このセクションはpreserve-existing e2eフィクスチャの224番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field224Id | String | 224番フィールドの識別子 |
| body.field224Name | String | 224番フィールドの名前 |
| body.field224Count | Integer | 224番フィールドの個数 |

224番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-225"></a>
### 保存セクション 225 { #preserve-sec-225 }

このセクションはpreserve-existing e2eフィクスチャの225番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field225Id | String | 225番フィールドの識別子 |
| body.field225Name | String | 225番フィールドの名前 |
| body.field225Count | Integer | 225番フィールドの個数 |

```json
{
  "field225Id": "id-225",
  "field225Name": "name-225"
}
```

225番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-226"></a>
### 保存セクション 226 { #preserve-sec-226 }

このセクションはpreserve-existing e2eフィクスチャの226番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field226Id | String | 226番フィールドの識別子 |
| body.field226Name | String | 226番フィールドの名前 |
| body.field226Count | Integer | 226番フィールドの個数 |

226番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-227"></a>
### 保存セクション 227 { #preserve-sec-227 }

このセクションはpreserve-existing e2eフィクスチャの227番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field227Id | String | 227番フィールドの識別子 |
| body.field227Name | String | 227番フィールドの名前 |
| body.field227Count | Integer | 227番フィールドの個数 |

227番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-228"></a>
### 保存セクション 228 { #preserve-sec-228 }

このセクションはpreserve-existing e2eフィクスチャの228番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field228Id | String | 228番フィールドの識別子 |
| body.field228Name | String | 228番フィールドの名前 |
| body.field228Count | Integer | 228番フィールドの個数 |

228番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-229"></a>
### 保存セクション 229 { #preserve-sec-229 }

このセクションはpreserve-existing e2eフィクスチャの229番目のセクションです。以下のテーブルと例は、セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field229Id | String | 229番フィールドの識別子 |
| body.field229Name | String | 229番フィールドの名前 |
| body.field229Count | Integer | 229番フィールドの個数 |

229番セクションの最後の説明文です。この文は行単位で再翻訳の必要性を判定するときに使用されます。

<a id="preserve-sec-230"></a>
### 保存セクション 230 { #preserve-sec-230 }

このセクションはpreserve-existing e2eフィクスチャの230番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field230Id | String | 230番フィールドの識別子 |
| body.field230Name | String | 230番フィールドの名前 |
| body.field230Count | Integer | 230番フィールドの数 |

```json
{
  "field230Id": "id-230",
  "field230Name": "name-230"
}
```

230番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-231"></a>
### 保存セクション 231 { #preserve-sec-231 }

このセクションはpreserve-existing e2eフィクスチャの231番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field231Id | String | 231番フィールドの識別子 |
| body.field231Name | String | 231番フィールドの名前 |
| body.field231Count | Integer | 231番フィールドの数 |

231番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-232"></a>
### 保存セクション 232 { #preserve-sec-232 }

このセクションはpreserve-existing e2eフィクスチャの232番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field232Id | String | 232番フィールドの識別子 |
| body.field232Name | String | 232番フィールドの名前 |
| body.field232Count | Integer | 232番フィールドの数 |

232番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-233"></a>
### 保存セクション 233 { #preserve-sec-233 }

このセクションはpreserve-existing e2eフィクスチャの233番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field233Id | String | 233番フィールドの識別子 |
| body.field233Name | String | 233番フィールドの名前 |
| body.field233Count | Integer | 233番フィールドの数 |

233番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-234"></a>
### 保存セクション 234 { #preserve-sec-234 }

このセクションはpreserve-existing e2eフィクスチャの234番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field234Id | String | 234番フィールドの識別子 |
| body.field234Name | String | 234番フィールドの名前 |
| body.field234Count | Integer | 234番フィールドの数 |

234番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-235"></a>
### 保存セクション 235 { #preserve-sec-235 }

このセクションはpreserve-existing e2eフィクスチャの235番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field235Id | String | 235番フィールドの識別子 |
| body.field235Name | String | 235番フィールドの名前 |
| body.field235Count | Integer | 235番フィールドの数 |

```json
{
  "field235Id": "id-235",
  "field235Name": "name-235"
}
```

235番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-236"></a>
### 保存セクション 236 { #preserve-sec-236 }

このセクションはpreserve-existing e2eフィクスチャの236番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field236Id | String | 236番フィールドの識別子 |
| body.field236Name | String | 236番フィールドの名前 |
| body.field236Count | Integer | 236番フィールドの数 |

236番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-237"></a>
### 保存セクション 237 { #preserve-sec-237 }

このセクションはpreserve-existing e2eフィクスチャの237番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field237Id | String | 237番フィールドの識別子 |
| body.field237Name | String | 237番フィールドの名前 |
| body.field237Count | Integer | 237番フィールドの数 |

237番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-238"></a>
### 保存セクション 238 { #preserve-sec-238 }

このセクションはpreserve-existing e2eフィクスチャの238番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field238Id | String | 238番フィールドの識別子 |
| body.field238Name | String | 238番フィールドの名前 |
| body.field238Count | Integer | 238番フィールドの数 |

238番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-239"></a>
### 保存セクション 239 { #preserve-sec-239 }

このセクションはpreserve-existing e2eフィクスチャの239番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field239Id | String | 239番フィールドの識別子 |
| body.field239Name | String | 239番フィールドの名前 |
| body.field239Count | Integer | 239番フィールドの数 |

239番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。

<a id="preserve-sec-240"></a>
### 保存セクション 240 { #preserve-sec-240 }

このセクションはpreserve-existing e2eフィクスチャの240番目のセクションです。以下の表と例は
セクションごとにフィールド名が異なり、相互に区別されます。

| 値 | 型 | 説明 |
|---|---|---|
| body.field240Id | String | 240番フィールドの識別子 |
| body.field240Name | String | 240番フィールドの名前 |
| body.field240Count | Integer | 240番フィールドの数 |

```json
{
  "field240Id": "id-240",
  "field240Name": "name-240"
}
```

240番セクションの最後の説明文です。この文は再翻訳の必要性を行単位で判定するときに
使用されます。