.claude/rules/mkdocs-syntax.md
__pycache__/

# 실제 토큰을 담는다 (DASHBOARD_API_TOKEN, 선택적으로 JENKINS_TOKEN).
# CLAUDE.md 가 "never commit it" 이라고 적어 두었지만 규율만으로는 새고 있었다 —
# 2026-08-20 의 e2e 커밋 9099632 가 `git add -A` 로 이 파일을 히스토리에 넣었다
# (원격에는 안 올라갔고 로컬 세션 브랜치에만 남았다). 무시 목록에 넣어 재발을 막는다.
load_env.sh
